# 密钥与本地配置 —— 绝不入库
config/*.local.json
.env
.env.local
*.key

# Node
node_modules/
# package-lock.json 入库以锁定 pi 验证环境的精确版本

# Python
__pycache__/
*.pyc
.pytest_cache/
.coverage

# 外部工具的临时产物，不入库
.understand-anything/
