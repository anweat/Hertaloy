# Hertaloy 模型：只有容器

> 状态：**2026-09-12 定稿**。本文是**单一概念基线**：系统是什么、为什么长这样、
> 内核边界在哪、现在实现到哪。
>
> 与既有文档的关系：
> - `ONESHOT.md` → **本文的规格化摘录**，自包含、不带论证、不挂代码位置。
>   用于设计指导、一次投喂给模型、以及用它的 §11「不许做的事」测设计边界。
>   两者冲突时以本文为准 —— 漂移只可能是那边没跟上。
> - `V6_MODEL.md` → **降为设计史**。它的诊断（§1）与复合图论证（§2）是本文的上游；
>   它的 §10 记录的是一条**被排除的中间路线**，见附录 A。
> - `FOUNDATION_V5.md` / `V5_WORKPLAN.md` / `RENDERING.md` / `OBSERVATION_V5.md`
>   / `FRONTEND_V5.md` → 设计史与专项参考。
> - `DEVELOPING.md` → 现行开发指南，不被取代。
> - 凡本文与它们冲突，**以本文为准**；凡本文与代码冲突，**以代码为准并修订本文**。
>
> 防漂移纪律（沿用并加严）：
> **每条声称成立的不变量，必须挂代码位置；位置写符号名，不写行号。**
> 上一版的不变量表有五条指向已不存在的东西（`locks.ts`、三个漂掉的行号、
> 一条被删的 GC 旋钮）—— 行号会漂，符号名不会。

---

## 1. 命题与判据

### 1.1 命题

> **把长任务切成多个短上下文的 agent 执行，用容器图编排它们、用版本化产物缝合它们，
> 从而避免单 agent 上下文膨胀与注意力涣散。**

推论（整个系统的存在理由，剧本帧 14）：**上下文预算从"运行时救火"变成"注册期拒绝"**。
一个 agent 能看到什么 = 它的端口与 `bind` 段声明的变量集合，注册期就是有限且已知的。

四个产品目标：

| # | 目标 | 状态 |
|---|---|---|
| G1 | AI 经 MCP 自动搭建工作流 | 🚧 注册期校验 + CLI + MCP 工具层已有；提案/审批流未做 |
| G2 | 自动进化（版本继承 + 标注检索 + 提案） | 📋 机制地基在，闭环未做 |
| G3 | 人有完整权限 | 🚧 强制截断 ✅；授权表有了，审批链用 status+send 兜底 |
| G4 | 画布渲染 + 实时进度 | 🚧 scene 包有静态投影；`_layout` 独立版本与实时事件未做 |

### 1.2 判据

这个系统的每一次演进都是同一件事：**把 N 套并存机制证明成一套**。
任何提案先过这四句：

1. **删掉它，剧本的哪一帧无法正确表达？**
2. **它是在减少还是在增加并存的机制数量？**
3. **文档写了"不变量 X"，它的强制方式在哪个代码位置、哪条测试？**
4. **它要落在 §3 字段表的哪一行？** —— 落不下就是过度设计。

第四句是本版新加的。前十次归约删掉的东西里，相当一部分当初就是"多了一个字段"
开始的；先问字段，比事后再删便宜。

判断某样东西该不该开放给外部替换时，判据不是"能不能被替换"，而是
**换掉它，哪条不变量会破**（原文见 `scheduling.ts` 文件头）。

---

## 2. 本体：只有容器

### 2.1 五条

| | |
|---|---|
| **B1 本体** | 只有**容器**一类元素。容器自身含模板 / 网关 / 容器工具 / 安全工具。不存在容器之外的实例，也不存在节点 |
| **B2 运行** | 模板是存储，实例化后是内存里跑的一条命令；同一模板允许并行多实例 |
| **B3 通信** | 不声明边，而是按网络协议**占用容器端口**，经内网环回或容器间路由；嵌套地址 = 逐层索引 |
| **B4 组织** | 容器构建本身也是命令行 ⇒ 嵌套与子流程是推论 |
| **B5 资产** | 大段信息给索引 → 变量 → 模板构命令时注入 |

一句话模型：

```
容器           = 虚拟机（可嵌套虚拟机）
网关/资源/工具  = 带钩子的模拟 OS
模板           = 存储（磁盘上的可执行文件）
实例           = 内存里的进程
消息           = 被"量化"的连接 —— 有生命周期的那一段
名字           = 宏定义：使用处只写名字，正文在别处
```

### 2.2 唯一的关系：声明 → 实例

这个关系套在每一层，而且只有三行：

| 声明（存储 / 磁盘） | 实例（运行 / 内存，有生命周期） |
|---|---|
| 容器模板 | 容器实例 |
| 连接声明（端口 + 路由） | 消息 |
| 子实例声明 | 子实例 |

V5 的第三行是空的 —— 「内网实例」被做成了 `nodes` 字段，「纯转发边」被做成了
`edges` 字段，两个字段都是**永远不会被实例化的声明**。填上这一格，四个下游空洞一起补：

| 空洞（V5） | 补上之后 |
|---|---|
| 没有生命周期 ⇒ `Cell.phase` 只能默认 `idle`（而 idle 是正面断言） | 有相位 |
| 没有执行记录 ⇒ agent 节点硬塞一条，handler 节点没有 | 统一有 |
| 没有对象命名空间 ⇒ 产物挂容器上，`node_id` 降格成 provenance 的字段 | 各有各的命名空间 |
| 没有作用域 ⇒ `ctx.spawn` 建的子容器挂到**父亲**名下 | 挂自己名下 |

### 2.3 位点持久，执行瞬时

全文用得最多的一对词，先定死：

| | 是什么 | 生命周期 | 标识 |
|---|---|---|---|
| **位点** | 一个可寻址的执行点 = 实例路径 | 持久：声明在模板里，画布画得出，一个实例都没起时也在 | `TraceId` |
| **执行** | 位点上的一次命令运行 | 瞬时：命令结束它就结束 | `executionId` |

代码里本来就有这个词：`InstanceRegistry.sites(trace)` 的注释写着
「这个容器有哪些**执行位点**」。

**OS 根据**：inetd / systemd socket activation —— 监听地址常驻，每个连接交给一次
处理。所以一个端口能服务无限连接，而并发单位是处理进程，不是端口。

### 2.4 C3 失去前提，L5 回到一行

朴素地"节点就是常驻子实例"是**可证伪地错的**：

```
节点成实例 ⇒ 每个节点给容器记一条 child 义务
C3（内网实例不自行判定终止）⇒ 节点永远 OPEN
⇒ 容器名下永远有 child 义务 ⇒ 任何容器都永远 settle 不了
```

不是不优雅，是死锁。B2 跳过这个中间态：**实例是一条命令的一次执行，命令会结束。**
于是 `child` 义务自然清空，不需要区分"线程"与"子进程"，L5 退回一行。

而 **C3 的前提消失了**。C3 的理由是「否则第一条消息传完边就没了，图散架」——
而在 B1/B4 下，**拓扑从来不由实例承载，它在模板（存储）里**。图不是由活着的东西
拼出来的，所以散不了。C3 与 M4 同命：不是被删，是不再有它要解决的问题。

> 被排除的中间路线（节点成实例但不许自行终止，及其逼出的"线程 vs 子进程"、
> 递归 L5、`child` 双职责）记在附录 A。排除本身要留档，否则会有人再走一遍。

### 2.5 「默认起 / 按需 spawn」这个标记留着

统一之后子实例声明只有一张表，但表里仍有一个标记：**容器构建时起，还是按需 spawn**。

它**不再承担终止判定的职责**，只回答一个问题：构建时起不起。
别把它读成"两种生命周期还在"。

---

## 3. 内核对象模型

> 本章是 §1.2 第四句判据的落点。**任何新提案先在这张表上落字段，落不下就是过度设计。**

### 3.1 声明侧（contracts —— 进对象库的存储）

| 类 | 字段 | 说明 |
|---|---|---|
| `ContainerTemplate` | `ports` `routes` `children` | 容器暴露端口；路由表连端口；子实例声明表（含默认起/按需标记） |
| `Port` = Receive \| Emit | receive: `direction` `servo?` `contract?`<br>emit: `direction` `contract?` `alias?` `callback?` `reply?` `unavailable?` `protocol?` | `protocol?` 是阶段 4 才加的具名协议；在那之前网关四模式由字段组合互斥判定 |
| `ObjectVersion` | `object_id` `version` `kind` `content_hash` `body` `provenance` | append-only，内容寻址 |
| `Provenance` | `instance?` `execution_id?` `derived_from[]` | 原 `traceid` + `node_id` 两字段合并为一（同一事实存两份） |
| 标识 | `Ref` `TraceId` `AliasName` `PortName` `Principal` `Grant` | 全是带正则的字符串；节点 id 与 traceid 段**同一套字符集** |

**字段收敛**：`nodes` `edges` `children` `bindings` `selfBindings` `entry` `exit`
**7 → 3**（`ports` / `routes` / `children`）。
`NodeDefinition` 整类消失 —— 它并进子实例声明。

> ⚠️ **本表不完整（2026-09-15 发现）**：`NodeDefinition` 上的执行体 `handler | agent`、
> `bind`、`budget` 在上表没有落点。它们不会消失，要上移到容器模板 ——
> 所以"7 → 3"的计数不对，真实形状待 `ITERATION.md` 第十三轮 Q1–Q5 定案后修正。
> 净减的结论不受影响：上移的是已有字段，不是新增。

### 3.2 运行侧（kernel —— 实例）

| 类 | 字段 | 存哪 |
|---|---|---|
| `ContainerInstance` | `traceid` `templateRef` `status` `generation` `seq` `slot?` `bindings[]` | 头（终态归库是阶段 5 剩的那半） |
| `Message` | `id` `target` `payload` `state` `failure?` `alias?` `source?` `requestId?` `attempts` | 活的在头，终态进 `$msg` |
| `ExecutionRecord` | `executionId` `instance` `status` `termination?` `claimed[]` `generation` `usage?` | 在途在头，终态进 `$exec` |
| `Obligation` | `kind` `holder` `waitingOn?` `key` `originNode?` | **纯派生，不存** |
| `Candidate` | `message` `position` | **纯派生** |
| 待回复请求 | `requestId` `requester` `waitingOn?` | 头 |

### 3.3 状态机（四个，全部保留）

| 状态机 | 值 |
|---|---|
| `InstanceStatus` | `OPEN` `TERMINAL` |
| `MessageState` | `QUEUED` `CLAIMED` `CONSUMED` `FAILED` `DISCARDED` |
| `ExecutionStatus` | `RUNNING` `SETTLED` `VOIDED` |
| `Termination` | `DONE` `CANCELLED` `BUDGET` `INVALID_OUTPUT` `FAILED` |

**删除两个判别式**：`NodeDefinition.kind`（单值 —— 只有一类实体，没有要判别的）、
`Cell.kind`（3 值 → 1）。

`ObligationKind` **不是状态机**，是派生描述符。原文纪律照抄：
**加新的等待形态时加的是事实来源，不是往某个 `as const` 数组里再塞一个字符串
然后满世界找记账点。**

### 3.4 本次模型变更对字段的净影响

| 决定 | 字段影响 |
|---|---|
| 只有容器一类元素 | **减一类**（`NodeDefinition`） |
| 端口升到容器 | **7 → 3** |
| 位点 / 执行 | 0 —— 位点是 `TraceId`，执行是 `ExecutionRecord` |
| 网关是保留位点 | 0 —— 复用 `$exec`/`$msg` 那套保留名 |
| L5 回一行 | **减**（递归三合取消失） |
| 能力单调不增 | 0（落在执行面校验器，内核不认识） |
| 读裁剪 / 预绑句柄 / 快照注入 | 0（改实现，不加字段） |
| 七相位 | 0（描述已有挂点） |
| usage 补测 | 0 —— `ExecutionRecord.usage` **字段已经在了**，只是硬编码 0 |
| `Provenance` 合并 | **减一** |

**净减，零新增。**

---

## 4. 内核边界

### 4.1 三类：位点 / 底座 / 环境

系统里每样东西先归这三类之一。判别式只有三问：

```
实例能选择不用它吗？
  ├─ 能                            → 位点（被寻址、被调用）
  └─ 不能 → 换掉它破不变量吗？
              ├─ 破               → 底座（内核，在所有容器之下）
              └─ 不破，但强度可声明 → 环境（包着实例，实例看不见正文）
```

| 类 | 成员 | OS 对应 |
|---|---|---|
| **位点** | 网关、容器工具、协议、安全工具（脱敏/扫描类） | 系统调用 |
| **底座** | 队列机制、提交事务、义务枚举、别名解析、执行记录、**授权决策** | 内核态 |
| **环境** | 沙箱、git 观察、脱敏执行、资源别名 | 进程的地址空间 / 命名空间 / fd 表 / 挂载表 |

**安全必须切两半**：安全**工具**（脱敏、凭据扫描、产物检查）可以是位点；
授权**决策**不能 —— `Principal` 必须由可信边界注入，Runtime 保持纯引擎，
**它不知道谁在调它，也就不可能被骗**。若授权决策成了可寻址的位点，
就等于承认"可以向它发消息问我有没有权限"，那条结构保证当场失效。

### 4.2 递归终止条件

队列、调度、协议都可以是"规则文件经命令行启动成实例"—— 那启动它们的是什么？
**必须有一层不是模板实例。** 这与 C1 同形：

> 根容器唯一，**不由任何模板创建** —— 它是递归的终止条件。

内核（提交事务、对象库、义务枚举、队列机制）就是那个底座，**它不是容器**。
不写这条，B4 会无限退化。

### 4.3 组件不是缝

`scheduling.ts` 文件头那张判据表，原样收进模型：

```
换掉队列     → 投递一次、冻结对象（回滚依赖它）、order 语义 全破 → 不开
换掉执行记录 → 孤儿判定破                                      → 不开
换掉义务枚举 → L1 / L5 破                                      → 不开
换掉别名解析 → M2 / A1 / A2 / A3 破                            → 不开
换掉先跑哪条 → 一条都不破                                      → 开
```

> 前四样是**组件**（内部分解，为了可测与可读），只有这一样是**缝**。
> 把内部分解当成可插拔点，等于把"我能替换它"误当成"别人应该被允许替换它"。

**OS 根据**：eBPF —— 受限语言 + 验证器，程序造不出指针、只能返回决策；
而它只能挂 hook，换不掉调度器本体。

### 4.4 一条边界，四层强制

| 层 | 手段 |
|---|---|
| 哲学 | 内核不是容器（§4.2） |
| 分类 | 底座（§4.1） |
| 判据 | 换掉它哪条不变量会破（§4.3） |
| **语法** | `#` 私有字段 —— 外部**写不出** `runtime.#queue` |

第四层值得单说：`#name` 是 ECMAScript 私有字段（ES2022，`target` 已是 ES2022，
编译成原生 `#` 而非 WeakMap 模拟），**引擎强制**，不是 TypeScript 的 `private`
（后者编译期擦除，`obj["x"]` 就绕过）。全 src 树只有一处 `private` 关键字
（`RunState` 的私有构造函数，构造函数没法写成 `#`）。

它在这里承重的三件事：

1. **决定了快照必须手写。** 私有字段不参与 `{...obj}` / `Object.keys` /
   `JSON.stringify`，所以"浅拷贝即快照"对持有 `#` 状态的类不成立 ——
   `snapshot()` 是一份**显式列出的清单**，不是"对象里有什么就带什么"。
2. **让"某个字段不进快照"成为结构事实。** `ExecutionLedger.#driving` 是进程本地的，
   `restore` 刻意不恢复它，于是上个进程留下的 `RUNNING` 自动显形为孤儿。
   `#` 保证的那一半是：任何通用序列化都看不到它，**不可能被顺手带上**。
3. **把"这不是缝"从文档变成编译不过。**

不要和 `$` 混：`#queue` 是 JS 类的私有字段（进程内存），`$exec` / `$msg` / `$run`
是对象命名空间里的内核保留名（对象库），两者无关。

### 4.5 可执行的验收判据

内核对执行面**零知识**。原判据写作 `grep 'argv|workspace|profile' = 0 命中`，
今天实测 7 处命中 —— **全在注释里**，不变量成立而判据失真。改成可执行的形式：

> **kernel 不 import sandbox；且 `argv` / `workspace` / `capabilities` / `profile`
> / `image` / `isolates` 在 kernel 的非注释代码中零命中。**

这条自身就是"答不出代码位置的就是愿望"那条纪律的自指案例：判据写成了一条
今天会失败的 grep。

---

## 5. 地址与通信

### 5.1 traceid 是路径，寻址是逐层索引

`job-1 / job-1/coder / job-1/coder/review-1` —— 实例路径，段边界前缀匹配
（`job-1` 捞不到 `job-10`）。**要"按范围过滤"时先用它，不发明新索引。**

复用处：实例身份与所有权 / 别名绑定可见范围 / 观测投影子树查询 / 强制截断级联 /
对象命名空间（行级安全）/ 权限 scope / `$exec` `$msg` `$run` / docker 内网名。

traceid **不表达因果**：因果由 RunSnapshot 的 `consumed[] → produced[]` 承担，
消息信封因此不存 `causation_ids`（同一事实不存两份，存两份必然漂移）。

地址是**一段**：`Endpoint = { instance, port }`。容器由 `parentTrace(instance)` 派生，
不在地址里单独存一份。

### 5.2 端口属于容器，是槽，单一占有

- **端口属于容器**，不属于实例。实例占用容器端口（谁在占是运行时状态）。
- **一个端口单一占有。** 要多个监听者就开多个端口。
- **端口是模板声明的槽。** 实例只能占用已声明的端口，占不到就是错。

第三条必须显式写反，因为"网络协议"这个比喻自带一个默认：进程可以 bind 任意空闲
端口。照搬就破第一不变量（只能选不能构造）。第一不变量在本模型下**扩展**为：
**连接也要有槽 —— 模板声明可以连哪些，运行时实例化其中若干。**

扇出归**路由表**，不归端口：一个别名解析出 N 个地址（DNS 多条 A 记录），
每个地址是一个独占端口。`dangling`（零解析）不算错误。

### 5.3 两个轴：接口宽度与并发度

```
端口数    = 接口宽度（能连什么）    ← 声明期定，写死，归节点库设计
实例段数  = 并发度（能同时跑多少）  ← 运行期定，由 spawn 决定
```

**并发的挡板按实例路径挡，不按端口挡** —— `ExecutionLedger.isDriving(instance)`，
而 `scheduling.ts` 明说「同一位点的并发由执行记录的 driving 标记挡住，与调度无关」。
所以在一个位点上多开端口换不来任何并行；要并行只能多开实例段。

用端口数表达并行度会把一个运行期的量硬编进声明：想把并行度从 4 调到 8 就得改模板、
发新版本、起新实例（C4 实例终身 pin）—— 而并行度恰恰是最该能调的那种东西。

### 5.4 一律经网关，同容器内是环回

```
实例 --send--> 自己容器的网关
                 ├─ 目标在本容器 → 环回到本地端口
                 ├─ 目标在子树   → 下行
                 └─ 目标在外面   → 上行到公共祖先，再下行
```

**内网边不是第二套机制，是环回口。** M2 早就是这条规则：别名沿 traceid 向上解析，
可见性由绑定的**放置位置**决定 —— 放置位置就是公共祖先。

**路径是派生的，不是走出来的。** 源地址与目标地址一确定，经过哪些网关就算得出来；
不必让消息每层物理停一次。判据与全系统一致：**能派生的就不要记账**。

### 5.5 网关两档

网关是一个**保留位点**（复用 `$exec` / `$msg` 那套保留名约定，不发明新索引）。
地址一直在，实例起不起是另一件事：

| | 网关地址 | 消息 | 提交次数 | 并发 |
|---|---|---|---|---|
| **默认** | 在（画布画得出、钩子挂得上、协议可声明） | 路径派生，零跳 | 1 | 不涉及（没有执行） |
| **声明代理** | 在，且物化成实例 | 真停一次 | **N** | **该位点串行** |

进入第二档的条件是协议的性质，不是架构的选择：**需要中途改写过路消息**
（而不只是观察）的协议必须走存储转发。就像明文穿代理与 TLS 穿代理的区别，
取决于是否给权限/密钥。

串行是这一档的第二半代价，要一起标价。解法与 §5.3 通则一致：多开网关位点段。

**OS 根据**：netfilter 内核态 hook（零跳观察）vs **NFQUEUE**（排队到用户态改写）；
NFQUEUE 确实单队列串行，而 Linux 给的解正是 `--queue-balance` 多队列。

代理档的声明**不需要新字段**：它跟着阶段 4 的具名 `protocol` 走。
在那之前不实现代理档。

### 5.6 方向三分

最容易读出矛盾的地方 —— M2 说"向上解析"，而读数据不许向上。不矛盾，因为是三样东西：

| 什么 | 方向 | 依据 |
|---|---|---|
| **名字**（别名解析） | **向上**到公共祖先 | M2、A1 |
| **数据**（产物、工作区） | **向下**拷贝 / chroot | §7.2、§10.4 |
| **通信**（跨容器、兄弟、向上） | **横向**，经消息 | A3 |

**向上找的是名字，向下流的是数据，横向走的是消息。**

跨容器、兄弟、上层要读东西时，**造实例去那边处理，不开放权限**。
这比"读可以跨树但要鉴权"少一套机制：读类工具**根本不需要 scope 裁剪逻辑**，
因为它不跨命名空间。

**OS 根据**：进程隔离 + IPC，而不是共享内存加权限位。

---

## 6. 生命周期与义务

### 6.1 三条底线

**L0 内核无时钟。L1 义务全部派生，没有记账点。L2 owner 唯一是容器。**

L2 在本模型下**不用改**：统一之后只有一类实体，前半句自动成立，原文括号里
"含纯转发边"那句排除条款**失去了对象**。不是改规则，是同一条规则在新本体下的读数。

### 6.2 四种义务，事实来源本来就有

| 义务 | 权威来源 |
|---|---|
| `message` | `Message.state ∈ {QUEUED, CLAIMED}` |
| `execution` | `ExecutionRecord.status === "RUNNING"` |
| `request` | 待回复请求表 |
| `child` | 子实例 `status === "OPEN"` |

`outstanding(facts)` 是**纯函数**，每次现算、只读，顺序稳定
（message → execution → request → child），观测方可以依赖。

`child` 只对 **spawn 出来的**子实例产生。默认实例化的子实例是瞬时执行，
不产生常驻的 `child` 义务（§2.4）。

### 6.3 L5：一行

> **可终止 ⟺ 名下义务为空。**

`terminationBlockers` 是义务枚举的投影。**义务不承担正确性** —— 强制截断永远可用，
义务算错只导致"该自动回收的没回收"，是安全方向的失败。这是"等待策略可以外置"
成立的前提。

**L4：终态 ≠ 回收。** 永远不为正确性删，只为存储删。
（OS 根据：僵尸进程 —— 需要一个"已死但表项还在"的状态，等人收尸。）

### 6.4 强制截断六步

外层事务，级联子树：

```
0 推进 generation（气密性唯一依据，L3）
1 best-effort cancel
2 未消费消息丢弃留计数
3 自己发的请求销账
4 自己承接的请求代服务方发了结通知（请求必得一个了结）
5 子实例级联
6 写终态
```

气密性靠 generation fence，不靠 `backend.cancel` —— 后者只是 best effort，
远端可能已经在返回路上。**冲突域 = generation + claim 集**，两半都查。

（OS 根据：PID 重用防护 —— 为什么气密性不能建立在"取消成功了没"上。）

### 6.5 回边与循环由容器的命令行判定

帧 13（回边返工，epoch = 版本号）是本模型唯一吃紧的一帧：纯 dataflow 一旦有环，
EOF 传不下去（Kahn 网络的老问题）。而常驻节点已经没有了。

**判定落在容器的命令行里** —— 它是程序，可以判定。这与"逻辑在受信 handler 里"
同构，只是换了落点。

**不要给端口加环检测。** 那是凭空多一套机制。

### 6.6 事务、并发与三段式

- **提交是一次事务**：消息状态、实例状态、pending 表、下游消息、产物版本、
  RunSnapshot 一起原子落。能实现是因为可变容器里全是冻结对象。
- **三段式**：claim（同步临界区，**当场落盘**）→ execute（await，锁外）→
  apply（同步临界区）。"锁外执行"由 JS 单线程结构保证。
- **claim 必须当场落盘**：崩在 claim 与 apply 之间，恢复后要知道外面有个 agent 在跑。
  （OS 根据：让出 CPU 前先存好上下文。）
- **崩溃恢复 = 孤儿认领**：拿到锁时看到的 `RUNNING` 必然是孤儿，复用既有失败路径
  计数重试，不新增状态机。`#driving` **绝不能落盘** —— 句柄表是每进程的。
- **调度缝是唯一策略注入点**：内核先筛候选、调度器只排序、挑完按引用复核。
  写坏的调度器造不出"跑一条不该跑的消息"。默认 FIFO（唯一天然无饥饿）。

---

## 7. 版本与对象命名空间

### 7.1 版本层

`ObjectStore`：内容寻址、append-only、版本分配唯一权威（V1–V4）。
定义层全部并入 store，没有平行注册表。**资产不需要锁**：append-only 结构上
不可能丢失更新。

**C5 版本历史即状态。** 循环计数用版本号而不是节点里的变量；汇聚靠读版本历史。
节点里没有 persistent 状态 —— 而这条正是 §2.4 那条归约的伏笔：
**一个无状态的东西为什么要常驻。**
（OS 根据：进程无状态、状态在文件系统。）

读法三种：`read(ref)` 精确版本 / `history(name)` 本命名空间 / `collect(prefix, name)`
子树汇聚。

### 7.2 命名空间就是文件系统

`ctx.put("results", …)` 实际写 `<本实例 traceid>/results` —— **写不出自己的命名空间**，
`..` / 前导 `/` / 空段一律拒绝。这是对象存储上的行级安全。
（OS 根据：chroot。）

行级安全的**读那半**同样要有（V5 只做了写那半）。**读写同一个根**：

```
ctx.history / ctx.put  → chroot 到自己
ctx.collect(prefix)    → prefix 必须落在自己的子树里（段边界）
ctx.read(ref)          → ref 的对象必须落在自己的子树里（段边界）
横向 / 向上             → 不给权限，造实例 + 发消息（§5.6）
```

**要引入别处的产物，不需要"允许读注入进来的 ref"这种例外。** 在端口上声明
servo `type: "ref"`，内核在 prepare 时就解引用、按 long 计入预算，handler 拿到的
**已经是正文**（`context.ts` 的 `dereference`）—— 它从头到尾不需要自己去 `read`
一个别处的引用。原先设想的"注入 ref 白名单"因此没有消费方，不做。

实现：`Runtime.#handlerContext` 的 `read` / `collect` 过模块私有的
`withinNamespace`（`isDescendantOf` 段边界判定）。落地时全仓**零条既有用例变红**
—— 仓库里从来没有代码在跨命名空间读，缺口只是没被人撞上。

### 7.3 保留 kind 与保留名

内核保留 kind（`run` / `annotation` / `container_template` …）受信 handler 与 backend
**都不得伪造**——受信不等于可以伪造，那会污染因果记录。

对象命名空间里的保留名：`<instance>/$exec`（执行观测）、`<instance>/$msg`（消息落库）、
`<traceid>/$run`（因果快照）。网关代理位点若实现，走同一套。

### 7.4 已知边界

**聚合判定只能在同步 handler 里做。** agent 的 execute 期间读 history 是快照，
apply 时可能已变 —— 节点级 fence 不管"两个不同节点写同一对象"。
这条不是限制，是分工判据：**要新鲜状态的那一步就该是 handler，不是 agent。**

---

## 8. 工具面

### 8.1 五类，按"内核必须为它做什么"分

| 类 | 内核必须提供的保证 | 成员 |
|---|---|---|
| **读** | 命名空间裁剪（§7.2） | `ctx.read` / `ctx.history` / `ctx.collect`；DQL |
| **写（入事务）** | 命名空间强制 + 保留 kind 拦截 + 随提交回滚 | `ctx.put` |
| **造实例** | 槽已声明 + 入口已声明 + 义务记账 + 随提交回滚 | `ctx.spawn`、`ControlPlane.spawn` |
| **排期（不提交）** | 返回的地址必须恰好是 `resolve` 给它的那批（按引用比对） | `scheduler`、`stageOutputs` |
| **fiat（明认破不变量）** | 单独操作类 + 外层事务 + 留记录 + 自述三件事 | `truncate`、`settle`、`reconcile`、`move` |

**新工具先落行，再问那一行的保证够不够。** 可预见的新工具（`move`、多租户转交、
`propose`/`approve`、标注检索）都落得进这五类，不需要第六类。

### 8.2 三条纪律

1. **chroot 向下** —— 读写都不跨命名空间，跨就发消息。
2. **按引用比对** —— 排期类返回的目标必须是 `resolve` 给它的那批（`acceptedPick` 的写法）。
   安全性靠结构，不靠作者自觉。
3. **只排期不提交** —— 排期类是纯函数，返回 plan，零部分提交。

### 8.3 fiat 类要自述三件事

每样 fiat 工具必须自己说清：

1. 它**破了哪条**（例如 `move` 改 traceid，而 traceid 既是身份又是命名空间前缀）
2. 它需要**哪一类授权**（单独的操作类，不复用别人的）
3. 它留下**什么记录**（效果必须可观测，不能静默）

`truncate` 是先例。**今天只有它走完了** —— `reconcile`（把别人进程留下的 `RUNNING`
单方面认领成孤儿）、`settleAll`、`reclaim` 都还挂在别人的操作类名下，是欠账。
（OS 根据：特权系统调用 —— kill / mount / setuid 各有各的 capability。）

### 8.4 agent 面：独立，而且必须独立

agent 的工具不是 `ctx`，**这不是分层偏好，是三段式的推论**：`ctx` 是在事务里的
函数调用（`ctx.put` / `ctx.spawn` 随本次提交回滚），而 agent 在 execute 期间跑，
那时内核不在临界区。agent 的工具要么得进一个已经不存在的事务，要么得自己开一个
新事务，后者当场破掉"提交是一次事务"。

所以 agent 侧是**写日志文件**（journal，序号文件名），内核在 apply 期间读它。
没有 IPC，没有回调，没有通道。

**读改在注入期解决，不要令牌。** 令牌是「路径 + 凭据」形状：agent 从不可信边界
内部递出一个字符串，服务端验票 —— 撞上"Principal 由可信边界注入，不能自称 uid"。
正确形状是**预绑句柄**：

| 要读的 | 怎么给 | B1 数得到吗 |
|---|---|---|
| **产物** | 注入期预绑那批 `id@version` | ✅ |
| **容器与实例状态** | 注入期写入 scene 投影的快照 | ✅ |
| **execute 期间的新鲜状态** | **不给** —— 那是 handler 的活（§7.4） | — |

要读更多 = 模板声明得不够 → 改模板 → 新版本 → 新实例。与"热更新彻底没有"同一哲学。
agent 侧读工具只能读预绑的那批，按引用比对 —— §8.2 第二条的第三次应用。

**OS 根据**：fd 表 —— 内核在 exec 时把能力装进进程，进程只能用、造不出新的。
第一不变量「只能选不能构造」就是 fd 模型，不是令牌模型。

令牌属于**控制面外展**（人/AI 经 MCP 进来、服务端常驻多会话），
那一侧 Principal 由可信边界注入，今天就是对的。

---

## 9. 扩展面

### 9.1 扩展按相位分，不按功能分

因为能力上界是相位给的。同一个功能挂在不同相位，能干的事完全不同 ——
"是什么工具"回答不了"它能干什么"，"挂在哪"才能。

```
registerTemplate ──┬─ ① 注册期      校验器：只能拒绝
                   │
spawn ─────────────┼─ ② 实例化期    物化器：把声明展开成实例本地副本
                   │
claim ─────────────┼─ ③ 注入期（入） 环境注入：只能往环境加
  ↓ execute（锁外，内核不在临界区）
apply ─────────────┼─ ④ 收口期（出） 出口过滤：产物收名、脱敏、命名空间强制
                   ├─ ⑤ 排期期      纯函数：只返回 plan
                   ├─ ⑥ 提交期      事务内
                   └─ ⑦ 观测期      只读外送
```

| 相位 | 能做 | 绝不能做 | 失败语义 | 现有成员 | OS 对应 |
|---|---|---|---|---|---|
| ① 注册期 | 拒绝注册 | 改写声明 | 不落版本 | `validateExecutionSpec`、B1、A2 递归、密钥扫描、M3 | ELF 加载校验 |
| ② 实例化期 | 把声明物化成实例本地副本 | 引入声明里没有的东西 | spawn 失败 | 绑定 eager 物化、C4 pin | fork 的 COW + fd 表继承 |
| ③ 注入期 | 往环境加（文件 / env / 挂载） | 碰编排状态 | 执行失败（可重试档） | profile 渲染、资源物化、沙箱建立、git 基线 | exec 建环境 |
| ④ 收口期 | 过滤、改名、强制命名空间 | 放行未过滤的 | **必须阻断落库** | 产物收名、脱敏、`namespacedId` | egress filter |
| ⑤ 排期期 | 返回 plan | 提交、造地址 | 本次 apply 拒绝 | `scheduler`、`stageOutputs` | 调度器 hook |
| ⑥ 提交期 | **只因落盘失败**否决 | 因策略否决 | 整次提交回滚 | `CommitHook` | fsync / journal commit |
| ⑦ 观测期 | 读、外送 | 裁决、阻断 | **不影响任何事** | `$exec` diagnostics、scene 投影 | auditd |

④ 与 ⑦ 必须分开：**脱敏不是观测**。观测失败无所谓，脱敏失败必须阻断 ——
否则密钥进不可变对象库，而那是删不掉的。

### 9.2 相位不是状态机

**这是本章最容易被实现错的一条，先钉住。**

相位是**代码位置的分类**（挂点在哪个函数里），不是实例上的一个状态。
不要给 `ContainerInstance` 加 `phase` 字段来"表示当前在哪个相位"——
那会立刻造出两个凭空的问题：这个字段进不进快照、崩溃恢复时它是什么。
相位是调用栈的属性，不是持久状态。**七个相位在 §3 字段表上落零个字段。**

### 9.3 准入三问

1. **挂哪个相位？** 一个扩展只能挂一个相位。若它既要改环境又要否决提交，
   那不是一个扩展，是两个。
2. **失败语义是哪一档？** 选错档是最常见的错：监控挂成阻断，脱敏挂成不影响。
3. **留什么记录？** 效果必须可观测，不能静默。

### 9.4 ⑥ 相位今天的漏

`CommitHook` 是**唯一一个能否决提交的扩展点**，而"只允许因落盘失败否决"这条
只写在注释里。一旦有人往这儿挂策略（配额检查、审批拦截、监控告警），
那就是在用耐久性钩子做裁决 —— 破「观测不裁决」，而且破得很安静：
表现是"偶发提交回滚"。

收窄办法与 scheduler 的按引用复核同一路子：**把 `CommitEvent` 的载荷收窄到
它不可能拿来做策略判断的程度**，或者把这条契约从注释升成一条钉住的用例。

### 9.5 「改写」没有相位

注入有七个挂点，**改写一个都没有**，这是故意的：

```
改模板 → 新版本 → 新实例。不做热恢复，做重建。
实例终身 pin 创建时的定义版本，永不迁移。（C4）
```

"改写一个运行中的容器"在本模型里不可表达。唯一的例外是 **§8.3 的 fiat 工具**，
而它必须自述三件事。

---

## 10. 执行面（内核边界之外）

**agent 就是一条命令行。** `claude` / `codex` / `hertaloy agent` 三者平权，
内核不认识它们各自的工具。做法是**注入 + 自由 + 记录**。

### 10.1 唯一契约

```ts
interface ExecutionBackend {
  run(request: ExecutionRequest): Promise<ExecutionResult>;
  cancel(executionId: string): Promise<void>;  // best effort，气密靠 generation fence
}
```

`agentSpec` 不透明穿过内核；backend 是**不可信边界**，返回值运行期三查：
形状校验、executionId 防串号、禁伪造内核保留 kind。产物地址由内核命名空间强制，
不是 agent 报什么写什么。

### 10.2 环境类的实现形式（规范）

| | 落点 | 现有实现 |
|---|---|---|
| **声明** | 模板，**只能选强度，不许选"要不要"** | `capabilities: { network, wallClockSeconds, retain }`；省略 = backend 缺省 |
| **强制** | 执行面，两堵墙分开认 | `isolates` / `enforcesNetwork` 两个**必填**字段 |
| **结果** | 可寻址的对象，但**不可调用** | `$exec` 的 diagnostics |

第三条是「观测不裁决」的对偶：**裁决者不可调用，结果可查。**
画布上看得见"这次执行在什么策略下跑、git 观察到了什么"，而 agent 拿不到任何句柄
去改它。

"只能选强度"有先例：拒绝按域名放行的理由是**假装支持不强制的东西比没有更危险**。
同一条判据适用 —— 不允许 `sandbox: false`，只允许 `local | wsl | docker`，
且 `local` 必须如实报 `isolates=false`。

原则一句话：**能力声明在模板里（可读、可渲染、agent 碰不到），能力使用在工具里
（可记录、有范围）。放进 agent 自己能调的工具面，就等于让它松开自己的笼子。**

### 10.3 能力单调不增（新不变量，📋 未实现）

> **子容器声明的强度只能等于或严于父容器。**

理由是虚拟机模型本身：**一台 VM 不能给它的客户机比自己更高的权限。**
不写这条，`network: "none"` 的容器里建一个 `network: "open"` 的子容器就逃出去了。

**强制点必须是 `checkAgentSpec`，不是 kernel 的注册期。** `capabilities` 住在
执行面，kernel 不认识它；直接在 kernel 加这条校验，`grep capabilities` 在 kernel
里就有代码命中，破掉 §4.5 那条可验证的边界。接线已经有了：CLI 把执行面的
`checkAgentSpec` 注入 `registerContainerTemplate`。代价是校验器需要看到父模板 ——
这是这条不变量唯一的真实成本。

例外走 §8.3 的 fiat 层：提权是 fiat 操作，自述三件事。

**OS 根据**：`no_new_privs` / capability bounding set。Linux 把它做成进程的一个位，
一旦置上不可清除且被子进程继承 —— 对应到这里就是"注册期递归判定 + 实例终身 pin"，
两样都已经有实现。

### 10.4 工作区

- 模板只写**名字**（`workspace: { source: "primary" }`），真实位置由 backend 配置的
  注册表给 —— 与密钥同构：模板放取值方式不放值。
- 物化选「预先复制」不选「只读挂载」：沙箱一次性，多一个挂载面多一个攻击面。
- **交接方向**：工作区只能沿树**向下**传一份拷贝。父给子可以（拷贝，不共享），
  子取父不行，跨父的兄弟不行。同父之内的交接由"声明名是模板局部的"这条性质
  天然限定，不靠额外检查。

### 10.5 观察与脱敏

git 观察在**沙箱外**：`git --git-dir=<record.git> --work-tree=<workspace>`，
`record.git` 不挂载，agent 够不着。物化与渲染**先于**基线，于是 diff 里只剩
agent 干的事。

脱敏是 ④ 收口期：精确遮蔽全部已注入 env 值（≥8 字符）+ 常见密钥模式。
**真正的保证来自"密钥只经 env 注入、不落任何配置文件"**，脱敏是最后一道，不是唯一一道。

### 10.6 runner 三种强度

| | local | wsl | docker |
|---|---|---|---|
| 文件系统隔离 | ❌ 仅目录限定 | ✅ | ✅ |
| 强制出网 | ❌ | ❌ 与宿主同网 | ✅ |
| 用途 | 本机开发/离线测试 | Windows 上拿真 Linux 语义 | 真安全边界 |

`isolates` / `enforcesNetwork` 两个必填字段 —— **两堵墙分开认，不许合成"安全吗"**。
运行器只报事实（退出码/超时/被杀），语义分类归 backend。

---

## 11. 不变量总表

> 位置写符号名，不写行号。

| 编号 | 内容 | 状态 | 位置 |
|---|---|---|---|
| 首要 | Agent 只能选不能构造；**连接也要有槽** | ✅ 扩展 | `routing.ts`、`contracts/port.ts` |
| C1 | 根唯一，不由模板创建 | ✅ | `kernel/instances.ts` |
| C2 | traceid 是实例路径，段边界前缀 | ✅ | `contracts/identity.ts`（`TRACE_SEGMENT_PATTERN`） |
| ~~C3~~ | ~~内网实例不自行判定终止~~ | **不再需要** | 前提消失（§2.4） |
| C4 | 实例终身 pin；注册时 eager 物化 | ✅ | `ContainerInstance.templateRef`、`aliases/materialize.ts` |
| C5 | 版本历史即状态 | ✅ 结构 | `kernel/store.ts` |
| L0 | 内核无时钟 | ✅ | 时间在 runner |
| L1 | 义务全部派生 | ✅ | `obligations.ts` `outstanding()` |
| L2 | owner 唯一是容器 | ✅ 不用改 | 同上 |
| L3 | generation fence；请求两侧各自了结 | ✅ | `ContainerInstance.generation` + `ExecutionRecord.generation` |
| L4 | 终态 ≠ 回收 | ✅ 结构 | `InstanceStatus` |
| L5 | **可终止 ⟺ 名下义务为空**（一行） | ✅ | `Runtime.canTerminate` / `terminationBlockers` |
| M1 | 编排权威属于**路由表** | ✅ 重述 | handler 返回值里没有目标字段（结构性） |
| M2 | 别名向上解析，成为唯一解析规则 | ✅ | `kernel/aliases/` |
| M3 | callback 落已声明端点 | ✅ 注册期 | `kernel/routing.ts` |
| ~~M4~~ | ~~信号吃得下~~ | **不再需要** | 地址与协议同属一个宏定义，无两半可对账 |
| A1 | 绑定 spawn 时物化，解析只读本地 | ✅ | `aliases/materialize.ts` |
| A2 | 用到的名字必须绑上（覆盖面含内网边） | ✅ 注册期 | `aliases/check.ts` |
| A3 | 跨租户不枚举对面 | ✅ | `aliases/resolve.ts` |
| S1/S2 | servo 纯提取、无控制流 | ✅ 结构 | `contracts/port.ts`（`Servo`）、`kernel/extract.ts` |
| **B1** | **变量袋预算**注册期校验，运行期直接失败 | ⚠️ 见下 | `kernel/context.ts` |
| X | 稳定前缀不因运行期发现而改变 | ✅ 结构 | `contracts/template.ts` |
| V1–V4 | 版本分配/独立/幂等/精确引用 | ✅ | `kernel/store.ts` |
| — | 提交是事务 | ✅ | `kernel/tx.ts` |
| — | 冲突域 = generation + claim 集 | ✅ | `Runtime.#apply` |
| — | 观测不裁决 | ✅ 结构 | scene 包只读 |
| — | 对象读写受命名空间约束（读写同一个根） | ✅ | 写：`instances.ts` `namespacedId`；读：`Runtime.#handlerContext` → `withinNamespace` |
| — | 授权默认拒绝 | ✅ | `kernel/control.ts` |
| — | 半状态不可表达 | ✅ 结构 | — |
| — | 请求必得一个了结 | ✅ 两侧 | `Runtime.#settleRequest` |
| — | backend 不可信边界三查 | ✅ | `contracts/execution.ts` |
| — | 密钥不进对象库 | ✅ 注册期递归扫描 | `contracts/template.ts` |
| — | **能力单调不增** | 📋 | 落点：`checkAgentSpec`（§10.3） |

**B1 改名说明**：它管的是**变量袋**，不是全部上下文。资源别名挂载与 agent 自己
的工具读文件都不在其中 —— 那一格本来就是"进环境不占上下文"。这不是 bug，
是命名与承诺的落差；本表按后者处理：**承认上下文有两半，只有变量袋那半可预算**。
另一半的可观测性靠 `ExecutionRecord.usage`（今天硬编码 0，见 §12 轨 B）。

---

## 12. 路线

### 12.1 两条轨，可并行

```
轨 A（结构）  contracts 重写 ──→ kernel 47 处编译器驱动跟随 ──→ 队列按端口分
                  │
                  └─ 合并原 V6 阶段 1余下 + 2 + 3（它们碰同一批调用点）

轨 B（预算/可观测）  B1 改名说明 ──→ usage 补测 ──→ 预绑句柄清单 ──→ 资源监控
```

轨 B 不依赖轨 A：它碰的是变量层与执行面的测量，不碰地址。

### 12.2 轨 A 的做法

实测：要删的字段在 kernel 的 src 里共 **47 处读点，散在 7 个文件**；
**14 个文件零命中（1554 行，30% 的内核一行都不碰）**，而零命中的恰好是最贵的
那些 —— `store` / `tx` / `queue` / `executions` / `obligations` / `facts` /
`invariants` / `scheduling` / `extract` / `context`。

所以：

1. **`contracts` 推倒重写** —— 它本身就是字段与接口，按 §3 从零推导。
2. **`kernel` 编译器驱动跟随** —— 改名让每一处读都报错，**编译器就是那份清单**。
3. **调整过大的文件（`instances.ts` 21 处、`aliases/check.ts` 8 处）可以重写**；
   零命中的那 14 个文件不动。
4. **测试机械更新，不重写** —— 失败 = 真语义断裂，这是迁移相对重写的唯一优势。

已知的清单缺口，必须人看：**改名只覆盖「读字段」，不覆盖「把字段传给别的函数」**。
上一轮在这半踩了三脚，全都类型合法（`registry.has(req.requester)` 永远 false、
`req.requester === trace` 一条都删不掉、`waitingOn` 两边不同域导致环永远找不到
而且不报错）。**改公共形状时，tsconfig 之外的脚本要单独扫一遍** —— 那里的"绿"
不是编译器给的，是没人问过。

### 12.3 验收（每条可证伪）

| 项 | 验收 |
|---|---|
| 字段收敛 | `entry` / `exit` / `selfBindings` / `edges` 四个字段删除，别名与信号用例不改仍绿 |
| M4 退场 | `validateSignalPayloads` **整个删掉**，信号用例不改仍绿 |
| 判别式退场 | `NodeDefinition.kind` 删除、`Cell.kind` 3 → 1、`phaseOfNode` 删除，可见性用例一条不改仍绿（`RunSnapshot.commits` 与 `Cell.result` 已在阶段 1a 删除） |
| 读裁剪 | ☑ 已落地（第十三轮 PA1）：加裁剪后零条既有用例变红；新增四条反例钉住跨树、段边界与本树可读 |
| 队列按端口分 | drain 每条成本不再随 M 增长（今天 M=200→1600 是 0.108ms → 0.472ms） |
| 内核边界 | §4.5 那条 grep 在非注释代码上零命中 |
| usage | `$exec` 里 usage 非零 |

### 12.4 欠账登记

| # | 项 | 处置 |
|---|---|---|
| D-1 | **预注入协议包**（性能优化） | **现在不做。** 实测瓶颈是队列 O(M) 全表扫，协议解析开销**没测过**。触发条件：队列那个二次项消掉之后重测，若解析成为新的头名再做；落点现成（`ContainerInstance.bindings` 已是"创建时算一次、此后不变"的物化表，协议静态半是同类，泛化它而不是加字段）。OS 根据：prelink / 静态链接 |
| D-2 | `Provenance` 的 `traceid` + `node_id` 合并为 `instance` | 搭轨 A 一起做（改已落库对象正文，破坏性） |
| D-3 | fiat 自述三件事 | `reconcile` / `settleAll` / `reclaim` 补上独立操作类 |
| D-4 | `reconcile` / `settleAll` 授权了 scope 但不限范围 | 多租户之前必须修 |
| D-5 | `CommitHook` 载荷收窄 | §9.4 |
| D-6 | 执行租约、executionId 全局唯一 | 跨进程驱动之前 |
| D-7 | 沙箱侧 A 类漂移 12 条 | 见 `ITERATION.md` 末尾「漂移登记表」，原样挂账 |

---

## 附录 A：被排除的中间路线

V6 曾定案过一条路线：**节点成为实例，但仍受 C3 约束不自行终止**。它逼出了三样东西：

1. 「线程 vs 子进程」的实体区分
2. 递归三合取的 L5（自己 ∧ 默认实例化后代 ∧ spawn 子实例 TERMINAL）
3. `child` 义务"作为判据退役、作为投影保留"的双职责

**这条路线不错，但它是个中间态。** 那三样复杂度全部来自"实例常驻却不许自己判定
终止"这一个假设；一旦承认实例是一条命令的一次执行（B2），三样一起消失。

排除它的记录留在这里，因为：**排除本身是信息。** 不留档的话，将来有人从
"节点该不该是实例"出发，会再推导一遍同一条死路。

完整论证见 `V6_MODEL.md` §10。

## 附录 B：归约史

| # | 归约 | 消掉了什么 |
|---|---|---|
| 1 | 容器 is-a 实例 | slot / subscription / children / pool_cursor / overflow → 一棵实例树 |
| 2 | 资产即变量 | 卡片系统 + 四段上下文编译器 + 运行时预算裁剪 → 一套变量 |
| 3 | 等待即锁 | 五种等待 → 一张锁表（后被 #8 再归约） |
| 4 | 版本历史即状态 | 策略节点 / 表达式语言 / 节点内 persistent / 内核时钟 |
| 5 | agent 就是一条命令行 | 内核工具桥 / 线协议 / backend 矩阵 → 端口 + 变量 + 沙箱外 git |
| 8 | 锁账本即派生 | `LockLedger` 四处记账 → 一条「未了结的义务」枚举 |
| 9 | 隧道即别名 | 隧道标签 / 全树扫描 / 订阅 scope → 绑定表 + 向上解析 |
| 10 | Lock 即 Obligation 的第二套词汇 | `Lock` 类型与 `LockView` 整个删除 |
| **11** | **节点即容器** | 节点 / 边 / `entry` / `exit` / `selfBindings` / 两个判别式 / C3 / M4 / 递归 L5 |

## 附录 C：这个模型顺带解释了什么

一个模型是不是抓对了东西，一个检验是：**它能不能解释那些在没有它的时候
就已经做对的决定。** 下面这些全部是在 V5 里独立做出、各自有过论证的：

| 现有决定 | OS 里的名字 |
|---|---|
| L4 终态 ≠ 回收 | 僵尸进程 |
| 孤儿 = 记录 RUNNING ∧ 本进程没在驱动 | 孤儿进程 + init 收养 |
| 截断靠 generation fence，不靠 cancel | PID 重用防护 |
| claim 当场落盘，不等批量提交 | 让出 CPU 前先存好上下文 |
| Principal 由可信边界注入 | 不能自称 uid |
| `ctx.put` 写不出自己的命名空间 | chroot |
| S1/S2 servo 纯提取、无控制流 | 调用约定的参数解包 |
| `unavailable` 形状归请求方 | 返回值类型由调用方的栈帧决定 |
| 沙箱杀进程要按 pgid | VM 边界：那是另一个内核里的另一棵树 |
| `resources.json`：agent 只看到 note 不看到 path | fstab / 挂载表 |
| `head.lock` 与 `driver.lock` 是两把 | 写互斥 vs 调度权 |
| C5 版本历史即状态 | 进程无状态、状态在文件系统 |

本版新增的对应：

| 本版决定 | OS 里的名字 |
|---|---|
| 位点持久 / 执行瞬时 | inetd / socket activation |
| 网关两档 | netfilter hook vs NFQUEUE（含多队列解串行） |
| 三类：位点 / 底座 / 环境 | 系统调用 / 内核态 / 地址空间与命名空间 |
| fiat 类工具 | 特权系统调用 |
| 七相位 | ELF 校验 / fork COW / exec 建环境 / egress / 调度 hook / fsync / auditd |
| 预绑句柄 | fd 表 |
| 能力单调不增 | `no_new_privs` / capability bounding set |
| 规则文件的按引用复核 | eBPF verifier |
| 队列不能开 | eBPF 换不掉调度器本体 |
| 造实例而不开权限 | 进程隔离 + IPC，而非共享内存 |

**模型解释得通的地方，代码本来就是对的；模型解释不通的地方，代码正在反复打补丁。**
