# Hertaloy 模型

> **这里只写"是什么"。** 为什么长这样在 [REDUCTIONS.md](./REDUCTIONS.md)，
> 改了什么在 [CHANGES.md](./CHANGES.md)，怎么写才不踩坑在代码注释里。
>
> 本文是**设计基线**，描述第十二次归约之后的形状。代码尚未走完这次归约，
> 差异由 [V6_REDESIGN.md](./V6_REDESIGN.md) §10 的批次承担；**逐项状态不在本文维护**
> —— 符号是否存在、枚举值是否被读、边界是否被破，由 `pnpm docs-check` 回答。

---

## 1. 命题

> **把长任务切成多个短上下文的执行，用容器图编排它们、用版本化产物缝合它们，
> 从而避免单次执行的上下文膨胀与注意力涣散。**

推论，也是整个系统的存在理由：**上下文预算从"运行时救火"变成"注册期拒绝"**。
一个执行体能看到什么 = 它的端口与声明的变量集合，注册期就是有限且已知的。

判据见 [REDUCTIONS.md §1](./REDUCTIONS.md)。**任何提案先在 §3 的字段表上落字段，落不下就是过度设计。**

---

## 2. 本体

### 2.1 五条

| | |
|---|---|
| **B1 本体** | 只有**实例**一类元素。容器与程序是同一张实例表上的两种**派生读数**，不是两类记录 |
| **B2 运行** | 定义是存储，实例化后是内存里跑的一条命令；同一定义允许并行多实例。**实例是一条命令的一次执行，命令会结束** |
| **B3 通信** | 不声明"边"，而是占用容器端口，按名字解析路由。嵌套地址 = 逐层索引 |
| **B4 组织** | 容器把资产提取成变量，再把变量配置进它所描述的各个位点的命令行 ⇒ 嵌套与子流程是推论 |
| **B5 资产** | 大段信息给一个索引 → 表示为变量 → 构命令时注入 |

一句话模型：

```
容器   = 虚拟机（可嵌套）          实例 = 内存里的进程
定义   = 存储（磁盘上的可执行文件）  消息 = 被"量化"的连接
名字   = 宏定义：使用处只写名字，正文在别处
```

### 2.2 容器与程序：派生，不是字段

```
isContainer(p) = parentTrace(p) === null || !(lastSegment(p) in def(parentTrace(p)).nodes)
```

零字段派生。**容器不带执行体**：它负责"提取与配置"，命令属于位点；要执行就声明一个位点。
"容器也是一条命令"在本体上承认同源（一类实体、一份定义、一条生命周期规则），形状上分开。

### 2.3 唯一的关系：声明 → 实例

| 声明（存储） | 实例（运行，有生命周期） |
|---|---|
| 容器定义 | 容器实例 |
| 连接声明（端口 + 路由） | 消息 |
| 子实例声明 | 子实例 |

**任何时候发现自己在写"一个永远不会被实例化的声明字段"，就是把一类实体做成了字段。**

### 2.4 位点持久，执行瞬时

| | 是什么 | 生命周期 | 标识 |
|---|---|---|---|
| **位点** | 一个可寻址的执行点 = 实例路径 | 持久：一个实例都没起时也在，画布画得出 | `TraceId` |
| **执行** | 位点上的一次命令运行 | 瞬时：命令结束它就结束 | `executionId` |

**拓扑在存储里，不在实例里。** 图不是由活着的东西拼出来的，所以实例退出不会让图散架。

### 2.5 子容器描述内联

子容器的全部描述保留在父定义里直接声明，**不经资产传递、不用 Ref 引用**。
一份定义对象 = 一整棵子树。于是跨定义校验变成本地校验，子实例不需要自己的定义引用
（C4 由父那一次 pin 承担）。**复用走覆盖层**（`extends` + 路径 `override`），不走引用。

---

## 3. 对象模型

> **判据的落点。任何新提案先在这张表上落字段。**

### 3.1 声明侧（进对象库的存储）

| 类 | 字段 | 谁解释 |
|---|---|---|
| `ContainerTemplate` | `vars` `nodes` `slots` `wires` | 内核 |
| `Node` | `ports` `bind` `budget` `exec` | 内核（`exec.with` 除外） |
| `Port` | receive：`direction` `contract?` `vars?`<br>emit：`direction` `contract?` `to?` `await?` `reply?` | 内核 |
| `Var` | `type` `max_tokens?` + 来源五选一：`literal` `card` `resource` `from` `gather` | 内核 |
| `Wire` | `name` `inherit?` + 目标二选一：`{node, port}` `{slot, port, fresh?, max?}` | 内核 |
| `Slot` | `def`（内联定义）`wires?` `on_exit?` | 内核 |
| `exec` | `use`（挑 runner）`with`（命令、注入、限额） | `use` 内核；**`with` 只插值与扫凭据，不理解键名** |
| `ObjectVersion` | `object_id` `version` `kind` `content_hash` `body` `provenance` | 内核 |
| `Provenance` | `instance?` `execution_id?` `derived_from[]` | 内核 |
| 标识 | `Ref` `TraceId` `WireName` `PortName` `Principal` `Grant` | 内核 |

### 3.2 运行侧（记录，不是图实体）

| 类 | 字段 | 存哪 |
|---|---|---|
| `ContainerInstance` | `traceid` `defRef` `status` `slot?` `bindings[]` | 头 |
| `Message` | `id` `target` `payload` `failure?` `source?` `requestId?` `attempts` | 在途在头；终态进 `<instance>/$msg` |
| `ExecutionRecord` | `executionId` `instance` `inbound` `termination?` `claimed` `usage?` | 在途在头；了结进 `<instance>/$exec` |
| 待回复请求 | `requestId` `requester` `callbackPort` `waitingOn` | 头 |
| `Obligation` · `Candidate` | —— | **纯派生，不存** |

**「在途 vs 了结」由它住在哪表达，不由 `status` 字段表达。**

### 3.3 执行的输入与输出

```
ExecutionRequest  = { executionId, instance, inbound{port, requestId?}, vars, allowedEmitPorts, limits, with }
ExecutionOutput   = { emits: [{port, payload}], puts: [{name, kind, body}], spawns: [{slot, segment, port, payload}], termination }
```

**执行体不能观察、也不能改变任何没有注入给它的东西。** 它的全部输出是这三张清单。
`emits` 与 `spawns` 是**列表**（一个端口可以出现多次），因为"给三个子实例派三份不同的活"是硬需求。

---

## 4. 内核边界

### 4.1 三类

```
实例能选择不用它吗？
  ├─ 能                          → 位点（被寻址、被调用）
  └─ 不能 → 换掉它破不变量吗？
              ├─ 破             → 底座（内核，在所有容器之下）
              └─ 不破，强度可声明 → 环境（包着实例，实例看不见正文）
```

**安全必须切两半**：安全工具（脱敏、凭据扫描）可以是位点；**授权决策不能** ——
`Principal` 由可信边界注入，引擎保持纯净：**它不知道谁在调它，也就不可能被骗**。
授权决策若成了可寻址位点，等于承认"可以发消息问我有没有权限"。

### 4.2 什么是缝，什么是组件

```
换掉队列     → 投递一次、冻结对象、order 语义 全破 → 不开
换掉执行记录 → 孤儿判定破                        → 不开
换掉义务枚举 → 终止判定破                        → 不开
换掉名字解析 → 可见性与租户约束破                 → 不开
换掉先跑哪条 → 一条都不破                        → 开
```

**把内部分解当成可插拔点，等于把"我能替换它"误当成"别人应该被允许替换它"。**

### 4.3 递归终止条件

队列、调度、协议都可以是"规则文件启动成实例"—— 那启动它们的是什么？
**必须有一层不是实例。** 与"根容器唯一、不由任何定义创建"同形：内核就是那个底座，**它不是容器**。

### 4.4 内核对执行面零知识

沙箱、工作区、镜像、隔离强度是"怎么跑"，编排内核不认识它们。可验证判据：

> 内核不 import 执行面包；且 `argv` / `workspace` / `capabilities` / `profile` / `image` /
> `isolates` 在内核的非注释代码中零命中。

要在注册期校验执行面字段，走**注入的校验器**，不要让内核直接认识那些字段。

---

## 5. 地址与通信

### 5.1 路径就是地址

`job-1 / job-1/coder / job-1/coder/review-1` —— 实例路径，**段边界前缀匹配**
（`job-1` 捞不到 `job-10`）。一套前缀机制复用于身份与所有权、可见范围、子树查询、
截断级联、对象命名空间、权限 scope、观测对象名、内网名。
**要"按范围过滤"时先用它，不发明新索引。**

地址是**一段**：`{instance, port}`。容器由取父派生，不在地址里单独存一份。
路径**不表达因果**：因果由快照的 `consumed[] → produced[]` 承担。

### 5.2 端口：属于容器，是槽，单一占有

- 端口属于容器，实例占用它。一个端口单一占有；要多个监听者就多开端口。
- **端口是声明的槽** —— 只能占用已声明的，占不到就是错。

第三条必须显式写反：照搬"进程可以 bind 任意空闲端口"就破第一不变量。更硬的理由是
**端口还是变量的出入口** —— 提取规则与变量上界挂在端口上，注册期的预算求和就是把一个位点
所有 receive 端口的声明加起来；动态分配出来的端口没有声明处，预算求和当场失去意义。

**扇出归路由，不归端口**：一个名字解析出 N 个地址（DNS 多条 A 记录），每个地址是一个独占端口。

```
端口数   = 接口宽度（能连什么）   ← 声明期定
实例段数 = 并发度（能同时跑多少） ← 运行期定
```

**并发的挡板按实例路径挡，不按端口挡。**

### 5.3 一张 `wires` 表

内网边与别名是同一件事：**一个名字解析到 0..N 个端点**。所以只有一张表、一套解析。

- 放置决定可见性：容器级 `wires`（自己 + 子树可见）、`slots.<s>.wires`（只这一支可见）。
- `inherit: false` = 只对自己可见。
- 解析**最近优先**（对位 PATH 与 environ）；扇出来自单条 wire 的 slot 展开，不来自多层并集。

一律经网关，同容器内是环回 —— **内网边不是第二套机制，是环回口**：

```
实例 --emit--> 自己容器的网关
                 ├─ 目标在本容器 → 环回到本地端口
                 ├─ 目标在子树   → 下行
                 └─ 目标在外面   → 上行到公共祖先，再下行
```

**路径是派生的，不是走出来的。** 源与目标一确定，经过哪些网关就算得出来；
不必让消息每层物理停一次。**能派生的就不要记账。**

### 5.4 方向三分

| 什么 | 方向 |
|---|---|
| **名字**（wire 解析） | **向上**到公共祖先 |
| **数据**（产物、工作区） | **向下**拷贝 / chroot |
| **通信** | **横向**，经消息 |

跨容器、兄弟、上层要读东西时，**造实例去那边处理，不开放权限**。

### 5.5 请求与回执

emit 端口的 `await` 有无，决定记不记一份 `request` 义务；`reply` 有无，决定回程从哪来。
两者与"目标是本地还是别名"正交。回程地址由内核从触发这次执行的那条消息里取 ——
**执行体输出里没有地址**。

"把回复对回那一次调用"用的是 `requestId`（内核分配），对位 TCP 的临时源端口：

| TCP | 这里 |
|---|---|
| 四元组 | (请求方实例, `requestId`, 服务方实例, 端口) |
| 临时源端口 | `requestId` |
| 连接表 | 待回复请求表 |
| 对端消失 → EPIPE | 服务方终态 → 内核代投声明的替代回复 |

### 5.6 子实例入口是容器端口

子定义自己声明容器端口（`ports: { in: { to: {node, port} } }`），父投到 `<child>/in`。
容器端口是**一层转发**，创建时就能物化，**不产生额外消息、不产生额外执行记录**。

### 5.7 内核代投的通知

三种情形走**普通消息**（差事 + 错误响应），不是第二套机制：子实例进终态（自然终止**与截断都发**）、
请求永久等不到回复、被截断。**形状由声明方给，注册期一处校验**：

> 凡内核代投的载荷，注册期必须能被目标端口的 `contract` 与变量提取吃下。

这条校验不能省 —— 内核自造形状会在提取那步被拒、消息进终态、而**接收方根本不会被叫醒**。

---

## 6. 生命周期与义务

### 6.1 三条底线

**内核无时钟。义务全部派生，没有记账点。owner 唯一是容器。**

### 6.2 四种义务，事实来源本来就有

| 义务 | 权威来源 |
|---|---|
| `message` | 消息还在队列里 |
| `execution` | 执行记录还在头里（没进 `$exec`） |
| `request` | 待回复请求表 |
| `child` | 子实例 `status === "OPEN"` |

枚举函数是**纯函数**，每次现算、只读，顺序稳定，观测方可以依赖。

### 6.3 终止判定

> **可终止 ⟺ 名下义务为空。**

**义务不承担正确性** —— 强制截断永远可用；义务算错只导致"该自动回收的没回收"，
是安全方向的失败。**终态 ≠ 回收**：永远不为正确性删，只为存储删。

### 6.4 强制截断

外层事务，级联子树：推进气密性依据 → best-effort cancel → 未消费消息丢弃留计数 →
自己发的请求销账 → 自己承接的请求代服务方发了结通知 → 子实例级联（**并发通知**）→ 写终态。

气密性不靠 cancel 成功了没 —— 后者只是 best effort，远端可能已经在返回路上。

### 6.5 循环与回边

纯 dataflow 一旦有环，EOF 传不下去，而常驻节点已经没有了。**判定落在容器的命令行里** ——
它是程序，可以判定。**不要给端口加环检测。** 循环计数用**到达计数**或对象版本号，不用节点里的变量。

---

## 7. 版本、命名空间与新鲜性

### 7.1 版本历史即状态

对象库：内容寻址、append-only、版本分配唯一权威。定义层全部并入它，**没有平行注册表**。
资产不需要锁 —— append-only 结构上不可能丢失更新。**位点里没有 persistent 状态。**

### 7.2 命名空间就是文件系统，根是位点

写：`put("results", …)` 实际写 `<本位点路径>/results` —— **写不出自己的命名空间**，
`..` / 前导 `/` / 空段一律拒绝。读同根：只够得着自己的子树。

**根是位点，不是容器**（`$exec` / `$msg` 一直如此，产物跟上）。两条有强制点的不变量：

- **N1 写者唯一**：一个对象只有一个位点写得到它。
- **N2 位点串行**：一个位点同时至多一次在途执行，**强制点在候选过滤**。

由 N1 + N2：**自读自写不可能读到过期值**，无论执行体同步还是跨了 `await`。
于是"聚合判定只能在同步路径里做"这条规矩失去对象 —— 它的成因本来就是根取成了容器。

### 7.3 跨位点汇聚是声明，不是运行期读

N1 之后，合法的跨位点读只剩**一种形状**：父容器名下、指名某个子槽的汇聚。
它在声明期完全表达得出：

```yaml
vars: { results: { gather: { slot: coder, name: result } } }
```

**授权来自容器，取数由内核在注入期代做。** 所以它是注入，不是执行体自己调的工具。

活性由**单调 + 每源恰好触发一次**保证；**幂等由边沿触发保证** ——
判定要写成"到达计数 === 期望"，不是"已收集 ≥ 期望"。

### 7.4 保留名

`<instance>/$exec`（执行观测）、`<instance>/$msg`（消息落库）、`<traceid>/$run`（因果快照）。
内核保留 kind 任何执行体都不得伪造 —— 受信不等于可以伪造，那会污染因果记录。

---

## 8. 执行

### 8.1 一条路径

```
挑候选  → QUEUED 消息，目标实例 OPEN，且该位点名下无在途执行（N2）
准备    → 入站契约 → 变量提取 → 编上下文        （纯函数，不改一个字节）
执行    → runner = 宿主注册表按 exec.use 挑
  ├ 返回非 Promise → claim 与 apply 合成一次事务，不写在途记录
  └ 返回 Promise   → 先落 claim → await → apply
收口    → 不可信边界三查 → 出站契约 → 路由排期 → puts / spawns → 一次提交
```

### 8.2 同步与异步由返回类型判定，不由声明

> 需要落盘的窗口存在 ⟺ 执行跨越了 `await` ⟺ `run()` 返回 Promise

这是结构性等价，由运行时承担，不需要声明侧的字段。**单向安全**：谎报异步只是降级走慢路径；
谎报同步构造不出来。配一条 runner 作者契约：**同步 runner 必须无外部副作用**；
一旦有 I/O，就返回 Promise。

### 8.3 失败语义只有一套

所有 runner 都是不可信边界，输出一律过三查，越界端口判为输出违规。
重试与否**不由错误种类决定，由"重试会不会改变结果"决定** —— 确定性执行重试无意义。

### 8.4 基底与声明面

> **只有容器基底（默认注入、环境相关配置与对应安全策略）和命令行工具自己实现的部分不声明；
> 其余基本都要声明，且要对执行体开放。**

由此得一条原则：**声明即注入（基底除外）**。声明过的东西，注入期必须让执行体看得见；
执行体看得见的东西，除基底外都必须在定义里找得到出处。

**"开放"是可读可用，不是可改**：能力强度声明在定义里，执行体读得到（它才好决定花多少力气），
但碰不到 —— 放进它自己能调的工具面，就等于让它松开自己的笼子。

### 8.5 命令合成

`${名字}` 递归替换 `exec.with` 里的字符串。**execve 数组语义，不是 shell 展开**：
只在单个字符串内部替换，替换结果不再分词、不 glob、不做命令替换 ——
注入安全是结构性的，不靠转义。

凭据扫描覆盖**整份定义正文**；**插值结果不扫**（值来自宿主环境或对象库，可能合法地含凭据），
而落盘的只有定义本身。

### 8.6 环境类

| | 落点 |
|---|---|
| **声明** | 定义里，**只能选强度，不许选"要不要"** |
| **强制** | 执行面，**两堵墙分开认**（文件系统隔离 / 网络强制各一个必填字段，不许合成"安全吗"） |
| **结果** | 可寻址的对象，但**不可调用** |

**假装支持不强制的东西比没有更危险。** 工作区只能沿树**向下**传一份拷贝；
观察在沙箱外，记录仓不挂载；物化与渲染**先于**基线，于是 diff 里只剩执行体干的事。

**能力单调不增**：子容器声明的强度只能等于或严于父容器 —— 一台 VM 不能给客户机比自己更高的权限。

---

## 9. 扩展

### 9.1 按相位分，不按功能分

能力上界是相位给的。同一个功能挂在不同相位，能干的事完全不同。真实的注入点只有三个，
其余四个相位是**代码位置的分类**，不是挂点：

| 注入点 | 能做 | 绝不能做 | 失败语义 |
|---|---|---|---|
| **注册期校验器** | 拒绝注册 | 改写声明 | 不落版本 |
| **调度器** | 返回排序 | 提交、造地址 | 本次收口拒绝 |
| **提交钩子** | **只因落盘失败**否决 | 因策略否决 | 整次提交回滚 |

另外两条承重规则：**出口过滤与观测必须分开**（脱敏不是观测：观测失败无所谓，脱敏失败必须阻断，
否则密钥进不可变对象库，而那是删不掉的）；**相位不是状态机** —— 不要给实例加 `phase` 字段，
它是调用栈的属性。

### 9.2 「改写」没有相位

```
改定义 → 新版本 → 新实例。不做热恢复，做重建。实例终身 pin 创建时的定义版本。
```

"改写一个运行中的容器"在本模型里不可表达。唯一例外是 fiat 工具，而它必须自述三件事：
**破了哪条** · 需要**哪一类授权** · 留下**什么记录**。

### 9.3 工具面五类

按"内核必须为它做什么"分：**读**（命名空间裁剪）· **写**（命名空间强制 + 保留 kind 拦截 +
随提交回滚）· **造实例**（槽已声明 + 入口已声明 + 义务记账）· **排期**（返回的地址必须**恰好是**
解析给它的那批，按引用比对）· **fiat**（单独操作类 + 外层事务 + 留记录）。

**新工具先落行，再问那一行的保证够不够。**

---

## 10. 状态机

**只有两个。**

| | 值 | 为什么不能派生 |
|---|---|---|
| `InstanceStatus` | `OPEN` `TERMINAL` | 终止是一个需要被记住的**决定**（终态 ≠ 回收） |
| `Termination` | `DONE` `RETRYABLE` `FINAL` | 内核只需要回答"应用输出吗""重试吗" |

人读的终止原因（取消 / 预算 / 配置错 / 格式错）是 `$exec` 里的**字符串**，不是枚举值。

其余一律派生：消息状态由"它住在哪"派生；执行状态由"在头里还是在 `$exec` 里"+ 有无终止原因派生。
**义务种类不是状态机**，是派生描述符 —— 加新的等待形态时加的是**事实来源**，不是枚举值。

> **纪律：新增枚举值之前，必须答出它被哪一行分支读。** 答不出就是分类，
> 分类归诊断字符串或渲染层。`pnpm docs-check` 的 `enums` 项强制这条。

---

## 11. 不变量

> 位置写符号名，不写行号。`pnpm docs-check` 核对每个符号是否存在。

| 编号 | 内容 | 位置 |
|---|---|---|
| 首要 | 只能选不能构造；**连接也要有槽** | `routing.ts`、`contracts/port.ts` |
| C1 | 根唯一，不由定义创建 | `kernel/instances.ts` |
| C2 | traceid 是实例路径，段边界前缀 | `contracts/identity.ts`（`TRACE_SEGMENT_PATTERN`） |
| C4 | 实例终身 pin 定义版本；绑定注册时物化 | `ContainerInstance.templateRef`、`aliases/materialize.ts` |
| C5 | 版本历史即状态 | `kernel/store.ts` |
| L0 | 内核无时钟 | 时间在执行面 |
| L1 | 义务全部派生 | `obligations.ts` `outstanding()` |
| L2 | owner 唯一是容器 | 同上 |
| L3 | 截断气密性；请求两侧各自了结 | `Runtime.#apply`、`Runtime.#settleRequest` |
| L4 | 终态 ≠ 回收 | `InstanceStatus` |
| L5 | 可终止 ⟺ 名下义务为空 | `Runtime.canTerminate` |
| M1 | 编排权威属于路由 | 输出清单里没有目标地址（结构性） |
| M2 | 名字沿路径向上解析，唯一解析规则 | `kernel/aliases/` |
| M3 | 回执落已声明端点 | `contracts/template.ts` |
| A1 | 绑定创建时物化，解析只读本地 | `aliases/materialize.ts` |
| A2 | 用到的名字必须绑上 | `aliases/check.ts` |
| A3 | 跨租户不枚举对面 | `aliases/resolve.ts` |
| S1/S2 | 变量提取纯粹、无控制流 | `contracts/port.ts`（`Servo`）、`kernel/extract.ts` |
| **N1** | **写者唯一**：对象命名空间的根是位点 | `namespacedId` |
| **N2** | **位点串行**：一个位点同时至多一次在途执行 | `Runtime.#pickWork` |
| B1 | 变量袋预算注册期求和，运行期超界即失败 | `kernel/context.ts` |
| X | 稳定前缀不因运行期发现而改变 | `contracts/template.ts` |
| V1–V4 | 版本分配 / 独立 / 幂等 / 精确引用 | `kernel/store.ts` |
| — | 提交是一次事务，零部分提交 | `kernel/tx.ts` |
| — | 观测不裁决 | scene 包只读 |
| — | 对象读写受命名空间约束（读写同根） | `namespacedId`、`withinNamespace` |
| — | 授权默认拒绝；Principal 由可信边界注入 | `kernel/control.ts` |
| — | 半状态不可表达 | 结构性 |
| — | 请求必得一个了结 | `Runtime.#settleRequest` |
| — | 执行面是不可信边界（运行期三查） | `contracts/execution.ts` |
| — | 密钥不进对象库 | `contracts/template.ts` |
| — | 内核对执行面零知识 | `pnpm docs-check` 的 `boundary` 项 |

**B1 的范围**：它管的是**变量袋**，不是全部上下文 —— 资源挂载与执行体自己读文件都不在其中。
承认上下文有两半，只有变量袋那半可预算；另一半的可观测性靠执行记录里的用量。

---

## 12. 主线剧本

**所有对象必须能指回下面某一帧。指不回的，删。**

场景：给现有项目加一个「导出」功能。

| # | 发生什么 | 逼出什么 |
|---|---|---|
| 0 | 人在根容器处提需求（根暴露 MCP，人与助手 AI 同层） | 根容器唯一 |
| 1 | 助手 AI 提案定义，**注册期校验通过才落提案** | 自我修正内循环 |
| 2 | 人在画布改坐标 + 覆盖一条定义路径，**改坐标不产生新版本** | 布局独立版本 |
| 3 | 人批准 → `export-flow@1`；实例化为 `job-1` | 提案 / 审批；实例化 |
| 4 | Plan 位点由 3 张卡编译 | `bind` 段；稳定前缀 |
| 5 | 有人把卡片改成 `@4`，**在途的 `job-1` 仍用 `@3`** | 实例 pin，活跃期不漂移 |
| 6 | 向控制面发请求 → 记一份 `request` 义务 | 审批不是新等待种类 |
| 7 | 回执到达 → 销账 | 义务释放 |
| 8 | 容器工具按 `plan@2` 建 **3 个 coder 实例**并各派一份活 | 容器嵌套；扇出（输出是列表） |
| 9 | coder#2 向 `skill.discovery` 发请求 | 名字解析；绑定到子槽 |
| 10 | 发现服务回复 → skill 作为 long 变量进上下文 | 资产即变量 |
| 11 | metrics 位点**收自己子树发的** | 向上解析（不需要作用域） |
| 12 | 三路各写一版 `results`；merge 用声明的 `gather` 汇聚 | 版本历史即汇聚；边沿触发 |
| 13 | 判定位点选 `again` 沿回边返工；epoch 是计数 | 循环靠回边 |
| 14 | **epoch 2 的变量只含失败切片，稳定前缀未动** | **命题核心** |
| 15 | 人强制截断 `job-1`，级联 + 反向清账 | 截断；通知也发给被截断的那支 |
| 16 | 助手 AI 检索历史标注 → 提案改进版 | 进化闭环 |

**第 14 帧是整个系统存在的理由。取舍冲突时优先保它。**
