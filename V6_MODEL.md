# V6 实体模型：一切都是实例

> **这是一份提案，不是现状。**全篇 📋。
>
> 它不取代 `FOUNDATION_V5.md` —— 恰恰相反：**V5 的不变量绝大多数原样成立，
> 而且其中两条本来就是按这个模型写的，只是实现没跟上**（见 §1）。
> 这份文档只做一件事：把"容器里有哪些东西"这一层重述清楚，
> 然后逐条对照 V5 的不变量哪些保留、哪些重述、哪些不再需要。

---

## 0. 一句话模型

```
容器      = 虚拟机（可嵌套虚拟机）
网关/资源/工具 = 带钩子的模拟 OS
模板      = 存储（磁盘上的可执行文件）
实例      = 内存里的进程
消息      = 被"量化"的连接 —— 有生命周期的那一段
名字      = 宏定义：使用处只写名字，正文在别处
```

通信一律经网关代理；同容器内的实例之间也走容器端口，由网关**环回**。
嵌套逐级路由，写好地址索引即可。

### 0.1 完整表述

> 用**互相隔离、同属继承**的沙箱组成的**容器图**来做命令流，
> 用脚本和程序处理各种信息流转与加工；
> **资产**作为类似环境变量注入；
> **别名**是注册器与宏的组件；
> **快照与 git** 记录和处理历史，再加上日志。
> 各个实例就是从存储加载进内存的程序，通过**网关**与**内网**互相交换信息。

多租户不是另加的一层：**隔离来自沙箱，寻址来自网关，可见性来自绑定的放置位置**
（M2）—— 三者都已经是本模型的组成部分，所以多租户是它的推论，不是补丁。

---

## 1. 诊断：文档说的与代码做的不是一回事

V5 的不变量里有两条，**已经是按本模型写的**：

> **C3 —— 内网实例不自行判定终止，决策权在容器。**否则第一条消息传完边就没了，图散架。
>
> **L2 —— owner 唯一是容器**；容器内实例（**含纯转发边**）永不背义务。

文档里**没有"节点"这个实体**。它说的是「内网实例」，而且 L2 明确把**边**也算作
"容器内实例"的一种。

而代码做成了：

```ts
ContainerTemplate = { nodes, edges, children, bindings, selfBindings }
//                    ↑ 两个字段替掉了一类实体
```

于是「内网实例」变成了 `nodes` 字段，「纯转发边」变成了 `edges` 字段。
两个字段都是**永远不会被实例化的声明**。

### 1.1 这一格空着的下游后果

这个系统只有一种关系，套在每一层：

| 声明（存储） | 实例（运行，有生命周期） | 现状 |
|---|---|---|
| 容器模板 | 容器实例 | ✅ |
| 连接声明 | 消息 | ⚠️ 声明劈成 port + binding 两处；另有一条 `edges` 绕过网关 |
| 节点定义 | —— | ❌ **空的** |

第三行空着，直接造出这些：

- 没有生命周期 ⇒ `Cell.phase` 只能默认成 `idle`，而 idle 是一句**正面断言**
- 没有执行记录 ⇒ agent 节点硬塞一条 `ExecutionRecord`，handler 节点没有
- 没有对象命名空间 ⇒ 产物挂在容器上，`node_id` 降格成 provenance 的一个字段
- 没有作用域 ⇒ `ctx.spawn(slot)` 建的子容器挂到**父亲**名下，因为节点自己没地方挂

### 1.2 三条结构证据

**（一）`edges` 绕过网关，而且跨不出实例。**

```ts
// routing.ts，内网边分支
messages.push({ target: { traceid: ctx.traceid, node: edge.to.node, port: edge.to.port } });
//                        ↑ 同一个 traceid，不解析别名，不查绑定表
```

而 `FOUNDATION §5` 第 4 部分写着「内网 = **实例间**通信 = 边」。
**文档说实例间，代码做实例内。**

**（二）容器模板没有 `ports`。**端口只长在节点上，所以复合顶点无法作为顶点
参与父层的图，只好用 `entry` / `exit` 手工指进它内部。

**（三）`AliasBinding.node` 是必填的**，连指向子槽的形态也要写节点名 ——
同样是"容器不可寻址"的绕法。

---

## 2. 结构：这是一张复合图

树 = 嵌套，图 = 连接，顶点自己又能是一张图。这在图论与工程里有专名：
**复合图 / clustered graph**。它的主流解法就那么几种：

| 方法 | 代表 | 拓扑在哪 | 能画未发生的吗 |
|---|---|---|---|
| 邻接表在容器里 | 今天的 `edges` | 容器内一张表 | 能，但只覆盖单容器 |
| **端口 + 边连端口** | Ptolemy、Simulink、**Node-RED subflow**、LabVIEW | 每层一张表，复合顶点暴露端口 | 能 |
| 名字 + 解析 | 链接器 / DNS | 散在绑定里，要拼 | 能，但读不出全图 |
| 连接是实体 | FBP（有界队列）、Erlang link | 运行时状态 | ❌ |
| 能力传递 | actor / ocap | 完全涌现 | ❌ |

后两种被一条真实约束排除**作为拓扑的唯一表示**：

> **画布必须画得出还没发生的连接。**

这是项目自己的话（"声明本身是信息，不该因为还没发生就消失"、
"从没命中过的订阅仍然要画"）。注意判据是**可声明**，不是"注册期可判定" ——
后者只是达成它的一种手段。

**结论：端口模型。**复合顶点（容器）暴露端口，边连端口。
`entry` / `exit` 随之消失 —— 它们本来就是"复合顶点缺端口"的补丁。

---

## 3. 通信：一律经网关，同容器内是环回

```
实例 --send--> 自己容器的网关
                 ├─ 目标在本容器 → 环回到本地端口（今天的"内网边"）
                 ├─ 目标在子树   → 下行
                 └─ 目标在外面   → 上行到公共祖先，再下行
```

**内网边不是第二套机制，是环回口。**

而 M2 早就是这条规则了：「别名沿 traceid 向上解析，可见性由绑定的**放置位置**决定」
—— 放置位置就是公共祖先。

### 3.1 路径是派生的，不是走出来的

traceid 是路径，所以**源地址与目标地址一确定，经过哪些网关就算得出来**。
不需要让消息在每层物理停一次：

- 画布照样画得出跳数与路径
- 钩子在解析时沿同一条路径施加

判据与全系统一致：**能派生的就不要记账**（义务这么归约的，锁这么删的，
因果这么查的）。让消息真走 N 跳，等于把可派生的路径变成 N 条要维护的记录。

**例外条件写明**：如果某个协议需要中途**改写**过路消息（而不只是观察），
那条协议必须走存储转发，并接受 N 次提交。**这是协议的性质，不是架构的选择** ——
就像明文穿代理与 TLS 穿代理的区别，取决于是否给权限/密钥。

### 3.2 队列按端口分

今天是一条全局队列，`#pickWork` 每步全表扫：

```ts
for (const msg of this.pending()) {   // O(M)，每一步一次
```

实测（`experiments/2026-09-07-perf/`）：M=1600 时 drain 755ms，每条成本随
队列长度线性增长。**端口成为地址之后队列天然按端口分**，调度变成
"哪些端口有待收且有监听者"，是索引不是扫描 —— 那个二次项从结构上消失。

> 这不是巧合。全局队列 + 每步全扫，正是"没有把连接当成有身份的东西"的直接后果。

---

## 4. 端口：属于容器，单一占有

- **端口属于容器**，不属于实例。实例 `bind` 到容器端口上（谁在监听是运行时状态）。
- **一个端口单一占有。**要多个监听者就开多个端口。

于是**扇出归路由表，不归端口**：一个别名解析出 N 个地址（DNS 多条 A 记录），
每个地址是一个独占端口。

今天 `AliasBinding {slot}` → 槽里每个 OPEN 实例 —— **语义一个字都不用改**，
只是换个说法：不是"一个端口多人听"，是"一个名字解析出多个地址"。
`dangling`（零订阅）自然是空解析，而它今天本来就不算错误。

单一占有同时关掉两个口子：不需要在端口上区分广播/取一，
也不会有端口命名冲突的歧义（撞了就是错，像 EADDRINUSE）。

---

## 5. 名字：一套宏系统，两个轴

别名**不只是 DNS**。DNS 只答"在哪儿"；宏答"这一大坨是什么，而使用处只写个名字"。
目的是**减少上下文**。

系统里已经有**六套**这样的名字：

| 名字 | 展开成 | 何时展开 | 正文去哪 |
|---|---|---|---|
| 别名 `AliasBinding` | 端点（0..N） | spawn 物化 + 发送时解析活实例 | —— |
| 资源别名 `resources.json` | 外部 git/dir/skill/mcp | 执行时注入沙箱 | **环境** |
| 对象引用 `id@N` | 对象正文 | `read` / `dereference` | 上下文 |
| 模板引用 `ChildSlot.template` | 模板正文 | 注册 / 实例化 | —— |
| handler 名 | 内置函数 | 执行时查表 | 进程内 |
| `bind.card` | 卡片正文 | 编译上下文时 | 上下文 |

资源别名那条的注释是直接证据：

```ts
/** 宿主机路径。**agent 永远看不到这个值。** */          path
/** 会出现在 agent 的环境交代里，帮它判断该不该用。 */    note
```

**名字进上下文，正文进环境。**这就是"减少上下文"本身。

### 5.1 但这六套压不成一套 —— 有两条相反的要求

- **C4** 要求早绑定且不可变：「实例终身 pin 创建时的定义版本，永不迁移」。
  所以 `Ref` 必须精确到版本（V4），展开结果一旦定就不许变。
- **M2 / A1** 要求晚绑定：「换实例仍然正确」。绑定表 spawn 时物化，
  但**解析读的是当下活着的实例**。

**这两条在同一个轴上指向相反方向。**所以统一的是"宏"这个概念，
不能统一的是展开时机 —— 而时机之差正是 C4 与 M2 各自能成立的原因。

> 这与 `Lock`/`Obligation` 那类归约不同：那些是同一件事的两种写法；
> 这里是**同一个概念的两种必要形态**。归约到此为止，再压就要破不变量。

真正的结构是两个轴：

```
何时展开   早（pin 住，C4 靠它）   ←→   晚（跟活实例，M2 靠它）
展开到哪   进上下文（占 token）    ←→   进环境（不占）
```

六套名字各自落在这个 2×2 的某一格，而且**格子本身是有意义的**。

### 5.2 「展开到哪」今天是散的

同一个问题 —— **这个名字的正文进不进上下文** —— 今天由四套东西各自回答：

| | 谁答的 | B1 管吗 |
|---|---|---|
| `bind.card` | 正文进 prompt | ✅ |
| `servo type:"ref"` | 解引用后进 prompt | ✅ |
| 资源别名 | 挂进沙箱，正文不进 | ❌ |
| agent 自己的工具读文件 | 内核不中介（§14） | ❌ |

后两条不被预算看见**是有理由的**（内核不中介 agent 的工具调用），
但这意味着 **B1 的名字"上下文预算"比它实际管的宽** —— 它管的是**变量袋**预算。
一个节点可以通过挂载或工具引入任意大的内容，B1 一点都看不见。

**这不是 bug，是命名与承诺的落差。**要么改名，要么承认"上下文"有两半
而只有一半可预算 —— 建议后者，但要写明。

---

## 6. 协议：宏能展开出的东西之一

协议不是内核的可插拔模块，它是**宏能展开出来的东西之一**。
一个别名宏展开成 `{ 目标, 契约, servo, 协议, unavailable, 能否被中途改写 }`。

**M4 因此不再需要。**今天：

```
emit 端口： { alias, callback, unavailable }   ← 处理方式
routes：    { alias, slot?, node, port }        ← 地址
            └─── 靠字符串对上，M4 负责在注册期对账 ───┘
```

如果两半本来就是同一个宏定义的两部分，**没有两处，就没有对账**。
M4 不是被删掉的，是不再有它要解决的问题。

### 6.1 缝已经有雏形

```ts
stageOutputs(ctx: StageContext, outputs) → StageOutcome
StageContext = { …, resolve(alias), lookupRequest(id), nextRequestId() }
StagePlan    = { messages, requests, dangling, resolved }
```

**纯函数、只排期不提交、外部依赖全靠注入。**这正是协议接口该有的样子。
今天里面硬编码着 reply / alias / 内网边三条。

### 6.2 协议契约（六条，全部来自现有不变量）

判据用项目自己的那条：**换掉它，哪条不变量会破？**

| 不变量 | 对协议的约束 |
|---|---|
| 第一不变量 / M1 | 只能**解析**已声明的名字，**不能造地址** |
| M3 | callback 只能落回已声明端点，不能改投 |
| A3 | 只读本地事实，不许"去问对面还活着没" |
| L1 / L5 | 未了结的状态**必须能被枚举成义务** |
| L2 | 那份状态必须挂在某个容器名下 |
| 零部分提交 | 只返回 plan，**不许自己提交** |

在这六条之内，换掉协议**一条都不破** —— 所以它是缝，不是组件。

**安全性靠结构，不靠协议作者自觉**，照抄 `acceptedPick` 的写法：

> 协议返回的目标，必须**恰好是 `resolve` 给它的那批地址里的**（按引用比对）。

这样一个写坏的协议造不出新地址，第一不变量与 M1 不靠它守规矩。
不加这条，"允许自定义"就等于把内核的地址空间交出去了。

### 6.3 一条来自实测的补充条款

协议契约必须要求：**声明两端各自消失时如何了结。**

依据是本轮修的第一个 bug（`b574fc3`）：请求方被截断 → 服务方回复时
`lookupRequest` 落空 → 整批排期失败、agent 服务方按 INVALID_OUTPUT **重跑三次**。
对称的那半（服务方被截断 → 代发 `unavailable`）早就写好了，反方向漏了。

**契约里明写这条，这个 bug 构造不出来。**

---

## 7. 容器工具：主流程之外的余地

`move` 与同类操作（跨生命周期的搬迁、分布式多租户的转交）归**容器工具**层，
与 `ctx.spawn` / `ctx.put` 同属系统调用表。它们是**辅助**，不在主流程上。

这一层允许做主流程不变量之外的事 —— 但每样工具必须自己说清三件事：

1. 它**破了哪条**（例如 `move` 改 traceid，而 traceid 既是身份又是对象命名空间前缀）
2. 它需要**哪一类授权**（与 `truncate` 同例：fiat 操作，单独的操作类）
3. 它留下**什么记录**（效果必须可观测，不能静默）

`truncate` 已经是这个形状的先例：它是 fiat、单独授权、结果有记录。

---

## 8. 规模：全量记录，密度归渲染

内核侧**不做取舍，全量记录**。信息密度是渲染的事（视口、序号窗口、增量流
—— 场景里已有 `range` 与 delta）。

但「全量记录」与现状有一处直接冲突，必须配一条结构变更：

> 今天 `keepConsumedMessages` 默认 200，会**删掉**已消费消息。
> 队列自己的注释：「这是头唯一无界增长的来源，而头**每次提交全量重写** ——
> 累计写入是消息数的平方级。实测 2000 条已消费消息 → head.json 1.1 MB。」

**解法：头只装活的，终态的归 append-only 的对象库。**

对象库本来就是 append-only（`#log.push` / `appended(0)`），所以这不是新建机制，
是把历史挪到它本来该在的地方。挪完之后：

- `prune` 不再是策略旋钮，而是"头里天然只有活的"的自然结果
- 全量重写的对象只剩活状态，规模与在途工作量成正比，与历史无关
- 全量记录与"头很小"**同时成立**

---

## 9. 不变量存废

| | 结论 | 说明 |
|---|---|---|
| **第一不变量** 只能选不能构造 | 保留并**扩展** | 连接也要有槽：模板声明可以连哪些，运行时实例化其中若干 |
| **C1** 根容器唯一 | 保留 | |
| **C2** traceid 是实例路径 | 保留 | 路径变长一层（节点成实例）；`move` 是工具层的显式例外 |
| **C3** 内网实例不自行判定终止 | 保留，**终于有实现** | 今天"内网实例"是字段，没有终止判定这回事；统一后才有约束对象 |
| **C4** 实例终身 pin 定义版本 | 保留并**扩展** | 节点也 pin 自己的模板 ref（今天节点内联，压根没有版本） |
| **C5** 版本历史即状态 | 保留 | |
| **L0** 内核无时钟 | 保留 | |
| **L1** 义务全部派生 | 保留 | 协议契约第 4 条就是它 |
| **L2** owner 唯一是容器 | **不用改** | 统一后只有一类实体，前半句自动成立；括号里的排除条款失去对象（§10.5） |
| **L3** generation fence + 请求两侧各自了结 | 保留 | 本轮刚补齐两侧（`b574fc3`） |
| **L4** 终态 ≠ 回收 | 保留 | |
| **L5** 可终止 ⟺ 名下义务为空 | **重述"名下"的范围** | 自己 + 默认实例化后代；spawn 子实例仍等 TERMINAL（§10.5）|
| **M1** 编排权威属于内网边 | **重述** | 内网边不再是独立机制 ⇒「编排权威属于**路由表**」。核心性质（消息不选边、不创建边）原样保留 |
| **M2** 别名沿 traceid 向上解析 | 保留，成为**唯一**解析规则 | 内网边并进来 |
| **M3** callback 落已声明端点 | 保留 | 成为协议契约一条 |
| **M4** 信号吃得下 | **不再需要** | 地址与协议在同一个宏定义里，没有两半要对账 |
| **A1** 绑定 spawn 时物化，解析只读本地 | 保留 | 即"早展开"那一轴 |
| **A2** 用到的别名必须绑上 | 保留，**覆盖面变大** | 内网边也成了别名，一并受它约束 |
| **A3** 跨租户不枚举对面 | 保留 | 成为协议契约一条 |
| **S1 / S2** servo 纯提取、无控制流 | 保留 | |
| **B1** 预算注册期校验 | ⚠️ **需改名或说明** | 它管的是变量袋，不是全部上下文（§5.2） |
| **X** 稳定前缀不因运行期发现而改变 | 保留 | |
| **V1–V4** 版本分配/独立/幂等/精确引用 | 保留 | |
| 提交是事务 | 保留 | |
| 冲突域 = generation + claim 集 | 保留 | |
| 观测不裁决 | 保留 | |
| 判别式 `kind: "handler"` 单值 | **不再需要** | 只有一类实体，没有要判别的 |
| 对象写入受 traceid 命名空间约束 | 保留 | 节点成实例后粒度变细（各有各的命名空间） |
| 授权 = (principal, scope, 操作类)，默认拒绝 | 保留 | scope 粒度变细 |
| 半状态不可表达 | 保留 | |
| 请求必得一个了结 | 保留并**上升为协议契约** | §6.3 |

**统计：保留 23、重述 3、不再需要 2、扩展 3。**（原先待定的 L2/L5 已在 §10 定案）

---

## 10. L2 / L5：从模型推出来的结论

原来这一节是"还没定，两条路二选一"。用虚拟机模型推一遍之后，
**它不是二选一，是有唯一答案**，而且推导过程本身抓到了一个构造性死锁。

### 10.1 今天四种义务，三种已经是"关于自己"的

```ts
message   holder = m.target.traceid   ← 我的收件箱里还有东西
execution holder = e.traceid          ← 我自己在跑
request   holder = r.requester        ← 我阻塞在一个调用上
child     holder = parentTrace(i)     ← 我的子进程还活着   ★ 唯一"关于别人"的
```

所以"holder 是父还是自己"这个问题，四分之三本来就已经是自己了。
真正要定的只有 `child`。

### 10.2 L5 不是 POSIX 的 exit，是 structured concurrency 的 scope

OS 里"能不能结束"有三个不同的东西：进程退出（自己决定）、僵尸被回收（父 wait）、
**一棵树静止了没有**（递归判定）。

L5 说「可终止 ⟺ 名下义务为空」，而 POSIX 里父可以先于子退出、子被 init 收养。
**C3 明确禁止收养**。所以这个系统是 **nursery / coroutineScope / StructuredTaskScope**：

> 作用域不能退出，直到它里面 spawn 出来的一切都完成。

这个模型对 holder 的答案是现成的：**每个任务自己知道自己有没有干完；
作用域的完成是它子树上的递归谓词。**

C3 那句"否则第一条消息传完边就没了，图散架"，说的正是没有 nursery 的后果。

### 10.3 朴素的统一是**可证伪地错的**

假设节点就是普通子实例：

- 节点成实例 ⇒ 每个节点给容器记一条 `child` 义务
- C3 ⇒ 节点不自行判定终止，它**永远是 OPEN**
- ⇒ 容器名下永远有 `child` 义务
- ⇒ **任何容器都永远 settle 不了**

不是不优雅，是死锁。模型因此**逼出**一个区分。

### 10.4 那个区分是「线程 vs 子进程」

| | 默认实例化（今天的节点 / 边） | spawn 出来的子容器 |
|---|---|---|
| OS 对应 | **线程** | **子进程** |
| 生命周期 | 与容器同生共死 | 独立 |
| 有独立完成条件吗 | **没有** —— 它是个服务，处理到来的东西 | **有** —— 领了任务，干完通知 |
| 谁判定它完了 | 容器（**C3 就是这句**） | 它自己（`settle` 检查自己的义务） |
| 退出通知 | 无 | `exit` 端点 |

**你不会去问一个线程"你干完了吗"。**线程干完了进程也就该结束了，
判断在进程这一级 —— 这正是 C3。

而这个区分**不是新枚举**：它就是声明处的差别（默认实例化 vs 写在槽里按需 spawn）。

### 10.5 结论

**L2 不用改。**原文「owner 唯一是容器；容器内实例（含纯转发边）永不背义务」——
统一之后**只有一类实体**，前半句自动成立；括号里那句排除条款**失去了对象**。
不是改规则，是同一条规则在新本体下的读数。

**L5 改的是"名下"的范围：**

```
可终止(X) ⟺ X 自己名下义务为空
          ∧ X 的所有默认实例化后代名下义务为空      ← 线程，算进来
          ∧ X 的所有 spawn 子实例已 TERMINAL        ← 子进程，等它自己判
```

**`child` 义务只对 spawn 出来的产生** —— 那正是 §10.3 那个死锁的解。

**C3 从一条没有实现的话，变成"线程 vs 子进程"这条分界本身。**

### 10.6 副产品：`child` 承担着两个职责

1. 终止判据的一个合取项
2. 画布上"谁挡着谁"那根 `waits` 线

第 1 条在递归 L5 下是冗余的（子树量词已覆盖）；第 2 条不是 ——
去掉它画布上"父等子"那根线就没了。

**所以 `child` 作为投影保留，作为判据由递归代替。**
这与「`describeObligations` 是 L5 的投影」是同一个关系，只是方向反过来：
**这次是投影比判据活得久。**

### 10.7 那节点实例的 `templateRef` 是什么？—— 不用新字段

§10.4 定了「线程 vs 子进程」之后，落地时立刻冒出一个问题：
`ContainerInstance` 有 `templateRef`（终身 pin，C4），而节点定义是**内联**在
父模板的 `nodes` 里的，它没有自己的对象。节点成实例，`templateRef` 填什么？

**模型直接给答案**：子进程有自己的映像（`execve(path)`），线程没有 ——
`pthread_create(&t, attr, start_routine, arg)` 共享进程映像，只多一个**入口**。
所以节点实例的 `templateRef` **就是父容器的 `templateRef`**，加一个选中入口的名字。

我原来打算为那个名字加一个字段（`entry?: string`），理由是
`templateRef === parent.templateRef` 不能当判据 —— **递归容器会撞**：

```
children: { self: { template: "same@1" } }   ← spawn 出来的子进程，ref 与父相同
```

这一步是对的，ref 相等确实判不出来。但**下一步错了**：那个名字不必存。

```
判定「job-1/coder 是线程吗」
  = parentTemplate(job-1).nodes["coder"] !== undefined
```

因为默认实例化**不经过调用方**：spawn 的路径段是调用方给的
（`slot` 命名声明、`segment` 命名实例，两者解耦，所以 `slot` 必须存），
而节点没有调用方，段只能是节点 id 本身 —— **声明名与路径段被钉成同一个东西**。
于是"我是不是线程"这件事已经完整地写在父模板里了，往实例上再存一份，
就是这份文档一路在删的那种第二份拷贝。

> 递归容器那个反例仍然成立，只是它反的是 **ref 相等**，不是**可派生性**。
> 我差点让一个正确的反例给一个错误的结论背书。

段唯一性顺带保证了这个派生不含糊：`job-1/coder` 只可能有一个实例
（`#instances.has(trace)` 挡着），节点在容器创建时就物化，spawn 抢不到同名段。

**代价**：每次判定多两跳（父实例 → 模板 → `nodes`）。模板解析已经按版本 memoize
（`instances.ts` 的 `#parsed`），这两跳是 map 查找。

**将来会不会又变**：会，而且是继续减。阶段 2/3 把 `nodes` 与 `children`
并成一张表之后，"默认起 / 按需 spawn"落到**声明**上，`slot` 也跟着消失 ——
到那时实例侧一个这类字段都不剩。所以现在不加字段，方向是对的。

---

## 11. 迁移面（现状测量）

| | 数量 |
|---|---|
| 读 `template.nodes` | 28 处 |
| 读 `template.children` | 17 处 |
| 读 `template.edges` | 5 处 |
| 类型里带 `node` 的地址点 | 96 处 |
| 字段收敛 | `nodes`/`edges`/`bindings`/`selfBindings`/`entry`/`exit` **6 → 3**（`ports`/`children`/`routes`） |
| `Cell.kind` | 3 值 → 1 值 |
| `MessageSource` | 3 情形 → 2 情形（走了哪条名字 / 图外注入） |
| head 格式 | **破坏性变更**，现有 run 装不进来 |

head 破坏性变更按 §5.1 的既有立场可接受：「**热更新彻底没有。**改模板 → 新版本
→ 新实例。不做热恢复，做重建。」

### 11.1 两件事分开做，顺序不能倒

**（一）数据模型**：端口升到容器、实例 bind、名字统一、六字段收三字段。
**（二）协议缝**：`stageOutputs` 三分支 → 可选配的具名协议。

**（一）必须先做** —— 协议接口的第一个参数是"地址"，而地址是什么正是（一）在定。
先开缝再改地址，等于让所有自定义协议返工一次。

---

## 12. 这个模型顺带解释了什么

一个模型是不是抓对了东西，一个检验是：**它能不能解释那些在没有它的时候
就已经做对的决定。**

下面这些决定全部是在 V5 里独立做出、并且各自有过论证的。虚拟机模型没有参与
其中任何一条，但每一条在它下面都有一个现成的名字，而且那个名字**解释了
论证本身**：

| 现有决定 | OS 里的名字 | 模型多解释了什么 |
|---|---|---|
| **L4** 终态 ≠ 回收 | 僵尸进程 | 为什么需要一个"已死但表项还在"的状态 —— 等人收尸 |
| 孤儿 = 记录 `RUNNING` ∧ 本进程没在驱动 | 孤儿进程 + init 收养 | 为什么 `driving` **绝不能**落盘：句柄表是**每进程**的 |
| 截断靠 generation fence，不靠 cancel | PID 重用防护 | 为什么气密性不能建立在"取消成功了没"上 |
| claim **当场**落盘，不等批量提交 | 让出 CPU 前先存好上下文 | 为什么这一步的耐久性不能推迟 |
| Principal 由可信边界注入，不从 payload 取 | 不能自称 uid | 为什么这是结构保证而不是口头约定 |
| `ctx.put` 写不出自己的命名空间 | chroot | 为什么它既是修 bug 也是行级安全 |
| **S1/S2** servo 纯提取、无控制流 | 调用约定的参数解包 | 为什么 ABI 里不能有逻辑 |
| `unavailable` 形状归**请求方** | 返回值类型由调用方的栈帧决定 | 为什么服务方声明一份满足不了所有人 |
| 沙箱杀进程要按 pgid，杀 `wsl.exe` 不算 | VM 边界：那是另一个内核里的另一棵树 | 为什么"外面报 CANCELLED、里面还活着"会发生 |
| `resources.json`：agent 看不到 `path`，只看到 `note` | fstab / 挂载表 | 为什么这是"减少上下文"而不只是安全 |
| `head.lock` 与 `driver.lock` 是**两把** | 写互斥 vs 调度权 | 为什么不能合成一把 |
| C5 版本历史即状态，不设节点内 persistent | 进程无状态、状态在文件系统 | 为什么循环计数用版本号而不是节点里的变量 |

**这十二条的存在，是这个模型的证据。**

而对照之下，唯一**不**符合模型的那一块——`nodes` / `edges` 两个字段替掉了
一类实体——恰恰就是本文档 §1 指出的那处，也恰恰是本轮连打三块补丁的地方
（`commits`、相位修正、`Cell.result`）。

> 模型解释得通的地方，代码本来就是对的；模型解释不通的地方，代码正在反复打补丁。

---

## 13. 重构路线（提案）

每一阶段都有一条**可证伪的验收**：不是"做完了"，而是**能删掉什么**或者
**哪个测量变了**。任何一阶段之后都能停下来，套件保持全绿。

### 阶段 0 —— 地址读点的分类（勘察，不改代码）

> 原案是"把 96 处地址构造收拢成一处函数"。**实地勘察之后这一案作废** ——
> 见下面的理由。留着它会是纯粹的搬砖。

#### 为什么原案低价值

`Endpoint` 的形状一改，**TypeScript 会在每一处字面量上报错**。21 处手工构造
本来就是编译器的清单，包一层构造函数并不多买任何安全性。

#### 真正的危险在**读**，不在写

43 处读 `endpoint.traceid`。阶段 1 之后这个字段**还在，但意思变了**
（今天指容器，将来指节点自己）—— 它们会**继续编译通过，静默地换掉含义**。

#### 所以阶段 0 的产出是两样东西

**（一）一条给阶段 1 的命名纪律：**

> **阶段 1 必须给字段改名，不许复用 `traceid`。**
> 比如 `{ instance, port }`。这样每一处读都报错，**编译器就是那份清单**。

这与全项目一贯的判据是同一条：**让静默的变响。**

**（二）读点分类**（勘察结果，阶段 1 照它逐条处理）：

| 类别 | 问的是什么 | 阶段 1 之后 |
|---|---|---|
| **A 执行位点** | 谁处理这条消息 | = 节点自己的 traceid |
| **B 容器作用域** | 属于哪个容器（截断 / 授权 / 视口裁剪） | = 前缀匹配或取父 |
| **C 显示** | 格式化成 `实例/节点.端口` | 收成一处格式化函数 |

**B 类清单**（少数，但每一条都要人看）：

- `runtime.ts` `#truncate` 第 2 步 —— ⚠️ **地雷**
- `control.ts` `send` 授权 / `messages(actor, trace)`
- `state/snapshot.ts` 视口裁剪 `inScope`
- `scene/build.ts` 信号流的 `inScope`

**那颗地雷**：

```ts
// #truncate 第 2 步：未消费消息 → 丢弃
if (msg.target.traceid !== trace) continue;
```

阶段 1 之后容器的节点各有自己的 traceid，`!==` 会**漏掉它们名下的消息** ——
截断一个容器，它节点的排队消息还活着，而 `checkInvariants` 的
「TERMINAL ⇒ 无在途消息」会当场判违规。这条必须与阶段 1 同一个提交里改成子树匹配。

#### 顺带勘察到的

`MessageQueue.liveFor(trace)` **只有它自己的单元测试在用**，没有生产消费方。
阶段 1 之后它还需要从"按实例"变成"按子树"。先记着，本轮不删。

### 阶段 1 —— 节点成为实例（§10 已定案，闸门已开）

#### 前置：节点 id 必须先是合法的路径段（已完成）

节点 id 原来有自己的字符集，比 traceid 段宽 —— 允许大写与下划线：

```
NODE_ID_PATTERN       /^[A-Za-z_][A-Za-z0-9_-]*$/
TRACE_SEGMENT_PATTERN /^[a-z0-9](?:[a-z0-9-]*[a-z0-9])?$/
```

今天不打架，因为节点 id 从不进 traceid（spawn 的段由调用方给）。
**阶段 1 一落地就打架**：`nodes: { Coder: … }` 今天完全合法，那时造不出实例路径。

两套并一套，宽的那套不能留 —— 判据是**实例身份必须存得下**：
Windows / macOS 默认不区分大小写，`Coder` 与 `coder` 落到同一个目录，
`objects.ts` 的碰撞检测会当场拒写，于是一棵合法的容器树变成写不进去的树。
那个检测是给对象 id 兜的**诊断**，不该升格成实例身份的**准入**。

实测代价为零：全仓 80 个用例文件没有一个节点 id 用到放宽的那部分。

容器实例化时把模板的 `nodes` 每个物化成一个**默认实例化**的子实例。
地址 `{traceid, node, port}` ≡ `{traceid/node, port}`，由阶段 0 那一处转换。
执行记录、义务、对象命名空间随之落到节点自己名下。

- **性质**：**破坏性**（head 格式）。全线最大的一步
- **验收**：`RunSnapshot.commits`、`phaseOfNode`、`Cell.result` **三块补丁全部删掉**，
  而可见性用例一条不改仍然绿

> 用"能删掉三块补丁"作验收，是因为那三块正是这一格空着的补偿。
> 补偿删得掉，说明空格填上了。

### 阶段 2 —— 容器有端口

`ContainerTemplate.ports`；`entry` / `exit` 由容器端口表达；`AliasBinding.node` 去掉。

- **性质**：破坏性（模板语法）
- **验收**：`entry` / `exit` / `AliasBinding.node` 三个字段删除，别名用例不改仍绿

### 阶段 3 —— 内网边并进路由

`edges` → 路由表里目标为本地的条目；`stageOutputs` 三分支收成"解析 + 排期"。

- **性质**：破坏性（模板语法）
- **验收**：**M4（`validateSignalPayloads`）整个删掉**，而信号用例不改仍绿
  —— 地址与协议同属一个宏定义，没有两半要对账

### 阶段 4 —— 协议缝

`stageOutputs` 的三条内置协议变成具名可选配，加 §6.2 的六条契约与
`acceptedPick` 式的结构性复核。

- **性质**：加法（接口变更，不改数据）
- **验收**：三条内置协议**写得进这个契约**。写不进就是契约错了，不是协议错了

### 阶段 5 —— 头只装活的（消息与执行已完成）

终态的消息 / 执行 / 实例归 append-only 对象库；`keepConsumedMessages` 消失。

- **性质**：破坏性（head 格式），但可与阶段 1 合并
- **验收**：跑 5000 条消息后 `head.json` **不随历史增长**，而全部历史仍可查

**已达成**（执行 → `$exec`、消息 → `$msg`）：head.json 在 M=200…5000 之间
稳定在 0.7–0.8 KiB，而 5000 条消息与 5000 条执行记录都还查得到。
`keepConsumedMessages` 与 `MessageQueue.prune` 一并删除。

顺带收掉一处**没人发现的耦合**：场景的染色窗口原来"恰好"等于头里保留的
那 200 条已消费消息 —— 一个持久化 GC 旋钮在暗中决定渲染语义。现在快照给完整历史，
窗口只由 `build.ts` 的 `WINDOW` 说了算。

也顺带暴露一个**用例抓不到的排序 bug**：落库那半按「实例 × 节点 × 版本」取回来，
是按节点分组而非投递序，而下游取的是"最后 N 条"。夹具只有一两个节点所以全绿。
`Runtime.messages()` 现在显式按发号排序。

**剩下**：终态**实例**尚未归库（`InstanceRegistry` 仍全量在头里）。
它与阶段 1b 强耦合 —— 节点成实例之后实例数会涨一个量级，届时一起做。

### 阶段 6 —— 队列按端口分

- **性质**：内部实现
- **验收**：`experiments/2026-09-07-perf` 的 drain **每条成本不再随 M 增长**
  （今天 M=200→1600 时是 0.108ms → 0.472ms）

### 顺序与风险

```
0 ──→ 1 ──→ 2 ──→ 3 ──→ 4
      └──→ 5 ──→ 6
```

- **0 是勘察**，产出命名纪律与读点分类，不改代码；已完成
- **1 是唯一真正大的一步**（L2/L5 已在 §10 定案）
- 2/3 在 1 之后是机械的；4 必须在 3 之后（协议的第一个参数是地址）
- 5/6 与 2/3/4 并行不冲突

head 破坏性变更按 §5.1 既有立场可接受：「**热更新彻底没有。**改模板 → 新版本
→ 新实例。不做热恢复，做重建。」

---

## 14. 这份文档不做什么

- 不给实现计划。字段怎么迁、用例怎么改，等 §10 定了再排。
- 不动现有代码。目前一行都没改。
- 不承诺分布式。§7 只是给工具层留出位置，多租户的实际语义没有定义。
- 不重新设计沙箱与执行面。§14 那一层不受本模型影响。
