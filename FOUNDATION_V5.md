# Nodeflow V5 基础设计 —— 嵌套容器运行时

> 状态：**第二次整理稿（2026-08-18）**。取代 `FOUNDATION_V4.md` 作为概念入口。
>
> - **V4（`nodeflow_*.py` + 339 条测试）降级为行为参考**：执行面 / 版本面 / 上下文估算面继续有效并直接继承（§15）；**编排面的对象模型被本文推翻**。
> - 本文的直接上游是最初草稿 `nodeflow.md`。V4 曾把"容器"从四种含义砍到三种，砍掉的那一种（traceid 层级 + 触发宿主）是本轮重建的起点。
> - 归档资料见 `archive/`；实施进度见 `V5_WORKPLAN.md`。

**状态标记**（贯穿全文，用来防止文档与代码再次悄悄漂移）：

| 记号 | 含义 |
|---|---|
| ✅ | 已实现，且有测试钉住 |
| 🚧 | 部分实现，缺口在正文写明 |
| 📋 | 已定案，未实现 |
| ❓ | 未定案 |

---

## 0. 为什么重做，以及四次归约

一句话诊断：

> **V4 把"消息传递"当第一性，于是"容器"被挤成一个形容词；容器一旦不是实体，它的职责就散成 `ContainerSlot` / `SubscriptionDefinition` / `children` / `pool_cursor` / `overflow` 五套并存的机制，每套各长一份规则、一条失败路径、一批测试。**

V5 把第一性换成**嵌套与生命周期**。之后发生了四次归约，每次都是把 N 套并存的机制**证明成一套**：

| # | 归约 | 消掉了什么 |
|---|---|---|
| 1 | **容器 is-a 实例** | slot / subscription / children / pool_cursor / overflow → 一棵实例树 |
| 2 | **资产即变量** | 卡片系统 + 四段上下文编译器 + 运行时预算裁剪 → 一套变量 |
| 3 | **等待即锁（网关账本）** | callback WAITING / child OPEN / approval AWAITING / 订阅 / PAUSED / 定时器 → 一张锁表（5 种 → 2 种） |
| 4 | **版本历史即状态** | 策略节点 / 表达式语言 / 节点内 persistent / 多消息原子消费 / 内核时钟 → ObjectStore 的版本历史 |
| 5 | **agent 就是一条命令行** | 内核工具（`read_artifact`/`publish`/`spawn`）/ 工具桥 / `kernel_tool` 线协议 / backend 矩阵 → **端口 + 变量 + 沙箱外的 git** |

第 4 次最省，值得展开：节点内 `persistent` 与「等齐 N 条消息再原子消费」，本质是在**重新发明 ObjectStore 已有的东西**——

> **版本历史 = 累加器 + 计数器 + 择优的候选集。**

`plan@3` 就是第三轮；三路各写一版，读到满三条就是等齐。而且它比节点内状态好：已版本化、已可观测、已进因果记录、跨实例可见。节点内 persistent 是个藏起来的私有状态 —— 正是本项目一路在消灭的那类东西。

第 5 次归约的判据同样是"少一套并存机制"：早期稿想让内核**中介** agent 的每个工具调用，
于是要声明工具集、建工具桥、定线协议。改成**在沙箱里跑一条命令行、从外面用 git 观察**之后，
这三样一起没了 —— 而且观测反而更强：**git diff 不需要 agent 配合，也骗不了**（§14.4）。

---

## 1. 命题与四个目标

> **把长任务切成多个短上下文的 agent 执行，用边编排它们、用版本化产物缝合它们，从而避免单 agent 上下文膨胀与注意力涣散。**

V4 的落点是四段数组 + 运行时按优先级裁剪 —— **裁剪是补救，不是构造**。
V5 的落点是**端口变量声明**：一个 agent 能看到什么 = 它的端口与 `bind` 段声明的变量集合，**注册期就是有限且已知的**。

> **推论：上下文预算从"运行时救火"变成"注册期拒绝"。**超预算的图注册不进去。

| # | 目标 | 主要落点 | 状态 |
|---|---|---|---|
| G1 | AI 经 MCP 自动搭建工作流 | 统一 json 配置；注册期 LLM 可读错误（`hertaloy validate`）；八部分开放为 MCP 工具 | 🚧 校验 + CLI 已有，MCP 未做 |
| G2 | 自动进化 | 容器版本管理（多层继承）；提案/审批；标注检索 | 📋 |
| G3 | 人有完整权限 | 根容器 MCP 全权；审批；强制截断；变更进版本可回滚 | 🚧 截断已有 |
| G4 | 画布渲染修改 + 实时进度 | `_layout` 独立版本；锁表直接渲染；观测投影 | 📋 |

---

## 2. 主线剧本与覆盖状态

所有对象必须能指回下面某一帧。指不回的，删。

**场景：给现有项目加一个「导出」功能。**

| # | 发生什么 | 逼出什么 | 覆盖 |
|---|---|---|---|
| 0 | 人在根容器处提需求（根暴露 MCP，人与助手 AI 同层） | 根容器唯一 | 🚧 根唯一 ✅ / MCP 📋 |
| 1 | 助手 AI `propose` 模板，**注册期校验通过才落提案** | G1 自我修正内循环 | 🚧 校验 ✅ / 提案流程 📋 |
| 2 | 人在画布改坐标 + 覆盖一条定义路径，**改坐标不产生新版本** | `_layout` 独立版本 | 📋 |
| 3 | 人批准 → `export-flow@1`；实例化为 `job-1` | 提案/审批；实例化 | 🚧 实例化 ✅ |
| 4 | Plan 节点由 3 张卡编译 | bind 段；稳定前缀 | ✅ |
| 5 | 有人把卡片改成 `@4`；**在途的 `job-1` 仍用 `@3`** | 实例 pin，活跃期不漂移 | ✅ |
| 6 | 向控制隧道发审批 REQUEST → 记一把 `request` 锁 | 审批不是新锁种 | 🚧 锁 ✅ / 控制面容器 📋 |
| 7 | 审批 REPLY 到达 → 锁销账 | 锁释放 | ✅ |
| 8 | 容器工具按 `plan@2` 建 **3 个 coder 实例**并各派一份活 | 容器嵌套；扇出 | ✅ |
| 9 | coder#2 向 `skill.discovery` 发 REQUEST | 网关；隧道寻址 | 🚧 handler ✅ / **agent 侧待沙箱（§14）** |
| 10 | 发现服务回复 → skill 作为 long 变量进上下文 | 资产即变量 | ✅ |
| 11 | metrics 节点**订阅自己子树** | traceid 作用域过滤 | ✅ |
| 12 | 三路各写一版 `results`；merge 用 `collect` 汇聚 | **版本历史即汇聚** | ✅ 跨实例已钉住 |
| 13 | 判定节点选 `again` 沿回边返工；**epoch 是版本号** | 循环靠回边 | ✅ |
| 14 | **epoch 2 的变量只含失败切片**，稳定前缀未动 | **命题核心** | ✅ |
| 15 | 人强制截断 `job-1`，级联 + 反向清账 | 截断六步 | ✅ |
| 16 | 助手 AI 检索历史标注 → `propose` 改进版 | G2 进化闭环 | 📋 |

第 14 帧是整个系统存在的理由。取舍冲突时优先保它。

---

## 3. 第一性：容器 is-a 实例

```
实例  = 由某个模板创建、有生命周期、由 traceid 标识的运行对象
容器  = 实例的一个子类：它自己有内网（可含节点、边、其他容器）
```

因为容器是实例，容器可被容器包含 —— **嵌套是定义的直接推论，不是额外机制**。

### 3.1 根容器：递归的终止条件 ✅

> **C1 —— 根容器唯一，不由任何模板创建。**

- 根的 json 是**启动配置** `ObjectVersion(kind="root_config")`；编辑产生新版本，**重启生效，不做热 reload**
- 根**自身含边**，本身就是一个工作流；内网里的容器实例各自是具体工作流的根
- **层级完全均质**：不存在"不在容器里的节点"
- 顶层助手 AI 与人是根容器**网关的外部调用方** —— 位置由结构给出，不用另设

### 3.2 traceid ✅

> **C2 —— traceid 是实例路径，容器是层级发生器。**

```
job-1 / coder-2 / review-1
```

必须**可前缀匹配**，且匹配落在**段边界**（`job-1` 不得捞到 `job-10`）。它承担：

| 用途 | 状态 |
|---|---|
| 实例身份与所有权 | ✅ |
| 队列订阅的作用域过滤 | ✅ |
| 观测投影的子树查询 | ✅ |
| 强制截断的反向清账 | ✅ |
| **对象命名空间**（§7.7） | ✅ |
| **权限 scope**（§11） | ✅ |

**同一套前缀机制被复用六处** —— 每次要"按范围过滤"时先问它够不够，不要发明新索引。

> **traceid 不表达因果**：扇出后子消息 traceid 相同却各有前因；汇聚时一条输出有多个前因。
> 因果由 **RunSnapshot** 承担（§13）—— 每次提交记录的输入集→输出集就是因果边。
> 所以信封不存 `causation_ids`：同一事实在提交记录里已经有了，存两份必然漂移。

---

## 4. 实体表

### 4.1 唯一的一等公民：实例

| 实例形态 | 有内网 | 可持锁 | 状态 |
|---|---|---|---|
| **容器实例** | ✅ | ✅ | ✅ |
| **节点实例**（handler，`agent` 段可选） | ❌ | ❌ owner 唯一是容器 | ✅ |
| **边实例（纯转发）** | ❌ | ❌ 结构上够不着 | ✅ 暂不物化 |
| **边（非纯转发）** | ✅ **就是容器实例** | ✅ | 📋 |
| **消息实例** | ❌ | ❌ | ✅ |

> **非纯转发的边不是"特殊边"，它是容器。**它有并发与竞争，就需要生命周期和边界，那正是容器的定义。**复杂度往下压一层，不往旁边加一类。**

**纯转发边暂不物化**：此刻无状态、永不持锁、随容器生死（C3），物化空对象是纯开销。等运行时动态边出现（容器工具编辑内网）再物化。

### 4.2 非实例的东西

| 对象 | 说明 | 状态 |
|---|---|---|
| **ObjectVersion** | 版本分配唯一权威。承载卡片 / 契约 / 模板 / 提案 / 产物 / 标注 / run 快照 / layout | ✅ |
| **定义模板** | 是 ObjectVersion 的一个 kind | ✅ |
| **变量** | 节点实例内的数据，不是独立实体 | ✅ |
| **锁** | 网关账本的一行 | ✅ |
| **Principal** | `kind:id`，必须由可信边界注入 | ✅ |

**定义层全部并入 ObjectStore**，不再有 V4 那 14 张平行注册表 —— 那五张表（卡片/契约/策略/变换/主题）是同构的，而模板早已在 store 里。并进去之后：重复注册守卫 → 内容寻址幂等；提案/审批泛化到所有 kind；"定义层不落盘"这条限制消失。

唯一例外：**内置 handler 是函数存不了** —— 但它不是定义对象，是内核能力常量表。

---

## 5. 容器定义的八个部分

统一 json 配置；每部分都应开放为 MCP 工具（G1）。

| # | 部分 | 职责 | 状态 |
|---|---|---|---|
| 1 | **实例创建模板** | 本容器可创建哪些实例 | ✅ `children` 槽 |
| 2 | **网关** | 内外交换：隧道 / 订阅 / 请求回复 | ✅ |
| 3 | **容器工具** | 从节点内建子容器并派活（`ctx.spawn`） | ✅ 建实例；📋 编辑模板 |
| 4 | **内网** | 实例间通信 = 边 | ✅ 静态边 |
| 5 | **版本管理** | 多层继承；编辑进版本可回滚 | ✅ §5.1 |
| 6 | **权限管理** | 谁能创建、编辑、截断 | ✅ §11 |
| 7 | **生命周期** | 关闭条件、级联 | ✅ §9 |
| 8 | **观测投影** | 只读不裁决 | 🚧 锁表/快照 ✅ / 内网形态 📋 |

### 5.1 版本管理：eager 物化继承 ✅

> **C4 —— 实例终身 pin 创建时的定义版本，永不迁移。** ✅

容器定义是**路径寻址**的（`nodes/coder/ports/in/vars/task`），所以子容器可以只覆盖若干路径。覆盖的解析时机两种，**必须选 eager**：

| | 何时解析 | 后果 |
|---|---|---|
| **Eager（选它）** | 实例化时解析成完整定义，存为 `ObjectVersion(kind="materialized")`，实例 pin 它 | 改基模板不影响已有实例 |
| Lazy | 每次读走继承链 | **让 C4 的 pin 变成谎言** —— 实例说自己定版了，读出来的却会变 |

物化产物本身版本化 ⇒ 可回溯、可 diff、可作画布真相源；`provenance.derived_from` 记住基定义，**继承链可追**（支持多层）。

**实现比设计稿更严一格：在注册时物化，而不是等到实例化。**这让"注册即完整校验"继续成立 —— 合并结果必须过与基定义**同一套**连接期校验（覆盖不是逃生舱），实例化只负责 pin 一个已验证的 ref。

覆盖的路径规则：**中间每段必须已存在**（否则 `nodes/codr/...` 这种拼写错误会静默造出假节点），**末段可以是新的**（这样才能加节点、加边）。

**推论：热更新彻底没有。**改模板 → 新版本 → 新实例。这与"暂停即截断、恢复即重建"是同一哲学：**不做热恢复，做重建。**

---

## 6. 节点只有一类

```
handler  —— 唯一节点种类。带 `agent` 段走执行面 backend，不带走内置 handler。
```

> **判别式 `kind: "handler"`，只有一个值。**agent 由 `agent` 段的存在性判别，不引入枚举。

### 6.1 策略节点为什么被删掉 ✅

V4 与本文早期稿都把策略节点当一等对象，为它设计了 readiness × selection × output 矩阵、表达式语言、节点内 `persistent`、多消息原子消费。**全部收回。**

| 它原本要干的活 | 没有它怎么做 |
|---|---|
| 条件路由 | 内置 handler 是受信代码，条件随便算 |
| 模型驱动判定（帧 13） | agent 节点 + 两个 emit 端口 |
| **循环 epoch 计数** | **对象版本号就是计数器** |
| **汇聚等齐** | 各写一版，读 `history` 满数才走 |
| **择优** | 读 `history` 挑最高 |
| 扇出建子容器 | 归容器工具：`ctx.spawn(slot, segment, payload)` ✅ |

中间三行是关键：**消息降级成"通知"，内容在资产里** ⇒「多消息原子消费」这面内核承重墙**不需要建**，commit loop 保持单消息。

**代价（要认）**：自由度从"AI 写 DSL 程序"变成"AI 选配已注册 handler + 配图"。这不是妥协 —— **DSL 是让 agent 构造逻辑，选配 handler 才是选择**，后者才是第一不变量原本的意思。换来两条写进指南的约定：

1. **确定性不再由类型保证**。表达式语言能保证纯函数，handler 是真代码保证不了 —— 靠约定与评审，重放能力打折。
2. **画布看不见节点内部逻辑**。但画布主体本来就是图，节点内部从来不是它的展示对象。

### 6.2 端口 = 变量的出入口 ✅

端口不是"消息收发口"，是**变量槽**（草稿 line 49/51 的原意）。

- 每端口最多一个 servo —— 天然成立，servo 是端口的属性
- **servo 只出现在 receive 端口**，用 `direction` 判别式联合强制
- `direction=emit` 的端口集合 = `allowed_emit_ports` ← 第一不变量的落点

emit 端口四种模式互斥：

| 模式 | 声明 | 行为 |
|---|---|---|
| 内网边 | 都不声明 | 按模板的边路由 |
| 网关广播 | `tunnel` | PUBLISH，0..N 订阅者，不记锁 |
| 网关请求 | `tunnel` + `callback` | REQUEST，恰好 1 订阅者，记 `request` 锁 |
| 网关回复 | `reply: true` | 回复本次 REQUEST，销账 |

**handler 仍然只写端口名** —— 走内网还是走网关由**端口声明**决定，不由 handler 选。第一不变量与 M1 不因为加了网关而破口。

### 6.3 servo = 映射表，不是程序 ✅

参考 FastAPI 的依赖注入：**签名即契约，框架负责把外面的东西填进签名**。

```jsonc
"vars": {
  "task": {"from": "$.plan.tasks[0].description", "type": "short"},
  "spec": {"from": "$.plan.tasks[0].specRef",     "type": "ref", "max_tokens": 800}
}
```

> **S1 —— servo 是纯提取，输出变量集 = 键集，编译期已知。**

路径语言是**最小自定义子集**（`.name` / `[n]` / `[*]`），零依赖。不引入 JMESPath / JSONPath：S2 要求 servo 无控制流，而 **filter 就是控制流**；不需要 filter 就不需要整个路径语言。

### 6.4 逻辑在受信 handler 里 ✅

> **S2 —— servo 无控制流。**一切条件、分支、累加在 handler 代码里，没有内核 DSL，也没有节点内状态。

| | servo（声明层） | handler（代码层） |
|---|---|---|
| 职责 | json → 变量 | 变量 + 版本历史 → 输出 |
| 控制流 | 无 | 有（是真代码） |
| 信任级别 | 任何人可写 | **受信**：服务端注册 |

**循环仍然靠内网回边**，不靠语句里的 for —— 这是 V4 唯一被测试证明对了的循环设计。

#### handler 的只读世界 ✅

```
ctx.read(ref)              精确版本
ctx.history(objectId)      版本列表  ← 汇聚 / 择优 / 计数全靠它
ctx.put(name, kind, body)  写资产到自己命名空间，随提交事务
ctx.collect(prefix, name)  跨实例汇聚
ctx.spawn(slot, seg, load) 建子容器并派活 —— slot 必须是已声明的子槽
ctx.port / ctx.requestId   本次消息从哪个端口进来、是不是 REQUEST
```

`ctx.spawn` 的 `payload` 投到子槽声明的 **`entry` 端点**。没有 entry 就只能建空壳 ——
"给 coder-1 派 task-1、给 coder-2 派 task-2"因此成立，而**入口仍然是声明出来的，
不是 handler 编的**（第一不变量）。子槽 `entry` 的存在性走**跨模板校验**：
纯结构校验留在 contracts，需要解析引用的校验放 kernel（只有它持有 store）。

**回程 `exit`：子进终态 → 往父投一条通知。**`entry` 指**子模板**，`exit` 指
**本模板**——方向相反，注册期按这句校验。没有 `exit`，`settle()` 只标终态、释放
父的 `child` 锁，**不叫醒任何人**：三路汇聚要靠人手动往 merge 节点发消息才跑得起来。

通知只带"谁、从哪个槽、什么状态"，**不带子容器的产出**——内容在资产里，父用
`ctx.collect` 取（C5：版本历史即状态，消息降级成通知）。

由此得到一条使用约束：**幂等要靠版本历史，不能靠"只会收到一条"**。同批 settle 的
三个子会投三条通知，且产物那时都已齐备，三条都满足"够三份了"。正确写法是
`if (ctx.history("epoch").length > 0) return {}` —— 又一次落回 C5，节点里仍然没有状态。

`ctx.port` 不是可有可无：**多入端口的节点靠它区分"新任务"与"回复到了"**。缺了它，REQUEST 的回复落回同一节点，handler 会再发一次请求 —— 自己给自己造无限循环。这是写端到端场景才照出来的缺口。

agent 拿不到 ctx —— 它走**已声明的内核工具**。两条路信任级别不同，规则一致：受信代码直给，不受信的必须编译期声明。

---

## 7. 变量、上下文与资产

### 7.1 资产即变量 ✅

卡片（skill/mcp/rules/prompt）、产物引用、上游 payload、运行期发现的资料 —— **全部是变量**。普通 handler 也能引入长变量；agent 只是"包装过的 handler"，没有第二套上下文系统。

### 7.2 三种类型 ✅

| 类型 | 内容 | 计入预算 | 必须声明 `max_tokens` |
|---|---|---|---|
| `short` | string / number / bool | 否 | 否 |
| `long` | 大块文本 | **是** | **是** |
| `ref` | `object_id@version` | 解引用后按 long 计 | **是** |

> **`long` / `ref` 必须带 `max_tokens`。**运行期发现的内容没有静态上界，不声明就无法在注册期求和，B1 会退化成 V4 的"调用前检查"。

不加第四种类型：流是执行面的事件通道，不是调用的输入。

### 7.3 位置即绑定时机 ✅

**不设 `bind: compile|runtime` 字段**，用两个 schema 让结构强制：

| 位置 | 时机 | 允许的来源 |
|---|---|---|
| `bind` 段 | 编译期（进 prompt 稳定前缀，不变量 X） | `card` / `literal`，**恰好一个** |
| 端口 servo 的 `vars` | 运行期（缓存断点之后） | `from`（路径），**只此一种** |

于是"编译期变量从运行期 payload 取"这类错误在 schema 层就不成立。

### 7.4 作用域 ✅

变量作用域 = **节点实例**。同节点多入端口共用一张表 ⇒ **命名冲突注册期检测**。跨节点不共享变量，跨节点靠边。

### 7.5 预算 ✅

> **B1 —— 上下文预算在注册期校验，不在运行期裁剪。**

```
注册期：Σ(所有 long/ref 声明的 max_tokens) ≤ node.budget.tokens
        超了 → 拒绝注册，错误指出哪个变量、哪个端口
        声明了 long/ref 却不给 budget → 同样拒绝（否则 B1 不可执行）

运行期：实际填充超过声明上界 → 直接失败
        不截断、不降级、不按优先级裁剪
```

**运行期只有失败一种处理，没有兜底裁剪。**V4 那套优先级截断整个不要 —— 它是"预算算不准"的补丁，声明上界之后预算是准的。超上界说明声明写错了或图切错了，那要人改。

`usage.compactions` 保留为**观测**（供发现估算系数漂移），不参与裁决。

### 7.6 资产写入 —— 运行期内容进入版本层 ✅

变量原本只有编译期（card/literal）和运行期路径两种来源，**运行期发现的内容不属于任一种**（帧 7/10）。资产写入补这个缺口：

```
消息 payload  ──►  ObjectVersion  ──►  下游以 `ref` 变量引入
```

它同时是第 4 次归约的载体：**内容进了版本层，消息就降级成"通知"**。约束不变：`ref` 解引用照样受上界约束；版本仍由 store 分配；**不得伪造内核保留 kind**（受信不等于能污染因果记录）。

### 7.7 对象命名空间 ✅

对象命名空间**已经是一个文件系统**，只是还没有 mount：

```
job-1/coder-1/results        实例私有        ✅ 已强制
rules/ prompt/ skill/ mcp/   卡片库          约定，未强制
run/ annotation/ layout/     内核保留        ✅ 已强制
workspace/                   真实文件系统    ❓ 未做的 mount 点
```

最后一行是 Linux 思路能给的最大红利：**`read_file` 与 `read_artifact` 变成
同一个操作在不同 mount 上**，§12 文件控制与本节资产就此合并。记着，先不做。


**曾有的缺陷（已修）**：`ctx.put("results", …)` 写的是**全局** object_id，两个实例写同名对象会落到同一个对象上（探针实证 `results@1` / `results@2` 互相污染）。汇聚测试之所以绿，只因测试里单实例 —— 而汇聚正是"策略节点不需要"的承重论据。

**定案：object_id 是路径，落在 traceid 命名空间里。**

```
job-1/coder-1/results@1     实例命名空间下的产物
job-1/coder-2/results@1
rules/py-strict@3           全局资产（卡片），不带 traceid
run/job-1@7                 内核 kind，保留前缀
```

于是：

| 操作 | 语义 |
|---|---|
| `ctx.put(name, …)` | 自动前缀 `<本实例 traceid>/<name>`，**写不出自己的命名空间** |
| `ctx.history(name)` | 读自己命名空间下的 `<name>` |
| `ctx.collect(prefix, name)` | 读某 traceid 前缀下所有叫 `<name>` 的对象 ← **跨实例汇聚靠它**（帧 12） |
| `ctx.read(ref)` | 精确全局引用（读卡片、读别人的产物） |

`..`、前导 `/`、空段一律拒绝 —— **命名空间是安全边界，不是命名约定**。

这是**同一套前缀机制的第五次复用**，并且顺带给出权限的抓手：实例只能写自己 traceid 前缀下的对象 = 对象存储上的行级安全（§11）。

---

## 8. 传递：内网边 / 网关 / 隧道 ✅

> **M1 —— 编排权威属于内网边。**消息送达已声明端点后，容器内路由完全由边接管。消息不选边、不创建边。
> **M2 —— 队列是独立索引空间。**寻址 = **隧道标签 ∩ traceid 前缀**。
> **M3 —— callback 落回已声明端点，不重选边。**

| | 内网边 | 队列 / 隧道 |
|---|---|---|
| 寻址 | 静态声明的端口对 | 隧道标签 + traceid 作用域 |
| 关系 | 设计期固定 | 运行期订阅，收发双方无静态关系 |
| 作用域 | 容器内 | 不限 |
| 地位 | 编排主干 | 松耦合通道 |

**隧道 = 标签，不是第三种机制** —— 它是队列的寻址语法。隧道回答"是什么"，traceid 回答"在哪儿"，两者正交。

订阅作用域必须支持相对形式（`$self` / `$self_subtree`）：模板会被实例化成 `job-1`、`job-2`……，写死绝对 traceid 换个实例就失效，与帧 11 矛盾。

**订阅不物化为实例对象**：模板声明 + 实例终身 pin ⇒ 当前订阅是**派生**的，按需扫一遍 OPEN 实例即可。没有退订簿记，也就没有幽灵订阅。

---

## 9. 锁与终止

### 9.1 强制截断是第一位的保证 ✅

> **强制截断永远可用、永远成功。**未完成的等待不再等，附加结束状态。

| | 自然完成 | 强制截断 |
|---|---|---|
| 触发 | 三谓词满足 | 显式 close / 父级级联 |
| **依赖锁正确吗** | 是 | **否** |

推论：**锁设计错了不会卡死系统**，只会导致本该自动回收的没自动回收 —— 安全方向的失败。所以锁只承担「回收提示 + 展示」，不承担正确性。

三层兜底，对应三个现实类比：引用计数 GC（锁表）→ 环检测 GC（死锁检测）→ 关机 / `taskkill /T`（强制截断）。

### 9.2 锁 = 网关的账本，2 种 ✅

> **L0 —— 内核无时钟**：无 `tick`、无 `Date.now`、无 `setTimeout`。时间只存在于执行面。
> **L1 —— 锁只在网关穿越时产生；内核专有，2 种，零配置。**
> **L2 —— owner 唯一是容器**；容器内实例（含纯转发边）永不持锁。

| 锁 | 何时记 | 何时销 |
|---|---|---|
| `request` | 发出 REQUEST 且带 callback | 对应 REPLY 到达 |
| `child` | 创建子容器实例 | 子实例进终态 |

早期稿有 5 种，另外三种被证伪：`approval` **本来就是 `request` 锁**（人是服务方）；`timer` 因 L0 不存在；`manual` 因强制截断永远可用、暂停不承担正确性而删除。

**2/2 全是跨边界的，这不是巧合，是定理**：容器内网是有限且完全可见的图，"还有没有东西能动"**本地可判定**；只有跨网关的等待不可判定，才必须记账。

锁记录是**双向**的（`holder` + `waitingOn`），一个字段同时给出：强制截断的反向清账、死锁等待图、画布上那根"卡住的连线"。`originNode` **仅供展示，不参与调度门控** —— 等回复的节点本来就没有 QUEUED 输入，天然不可调度。

### 9.3 终止 ✅

> **L5 —— 容器可终止 ⟺ 三谓词同时为真：**无非终态消息 ∧ 无活跃 execution ∧ 锁表为空。

前两条是**内网本地扫描**，第三条是**网关账本**。可判定性正好切在网关上。

> **C3 —— 内网实例不自行判定终止，决策权在容器。**否则第一条消息传完边就没了，图散架。

**自然终止**（`settle`）：三谓词满足 → 进终态 + **销掉父容器对它的 `child` 锁**，自底向上收敛。没有这个提交点，完成的子容器不释放父锁，主线闭不上。

**强制截断六步**：

```
0. ★ 推进实例 generation          ← 气密性唯一依据，不靠 cancel
1. 取消在途 execution              (best effort)
2. 未消费消息 → 丢弃，留计数        ← 不留记录则消息凭空消失
3. 自己持有的锁 → 作废
4. ★ 自己在别处造成的锁 → 反向释放  ← 漏了父容器永远等一个已死的子
5. 子实例 → 级联（固定级联，不给选项）
6. 写终态记录
```

> **L3** —— `backend.cancel` 是 best effort；真正保证的是 generation：apply 时校验未变，**迟到的 apply 必然失败**。
> **L4** —— 终态是语义，回收是存储策略，**两者分开**。永远不为正确性删，只为存储删；终态实例留着就是画布回放的素材。

**死锁只报警**：等待图有环即死锁，做成投影供画布高亮，由人来断，不自动裁决。

---

## 10. 事务、并发与隔离

### 10.1 提交是一次事务 ✅

一次提交要同时改：消息状态、锁账本、pending 请求表、下游消息、产物版本、RunSnapshot —— **必须原子**。

在此之前"零部分提交"只做到"零部分**排期**"：排期算好了才提交，但提交本身是一串顺序 mutation，第 4 步抛异常会留下半状态。现在攒 changeset → 一次施加 → 抛了整体回滚。

> 实现能这么简单，是因为内核里所有可变容器装的都是**冻结对象**：Map/数组浅拷贝即完整快照。这是坚持深冻结换来的红利。

### 10.2 竞争分三层，只有中间一层归内核

| 竞争在哪 | 归谁 | 状态 |
|---|---|---|
| 两个 agent 改同一个文件 | tool executor / 外部文件锁 | 📋 |
| **两个执行抢同一节点 / 同一批消息** | **内核**：generation + claim 集 | ✅ |
| 一个容器下同时跑多少执行 | 容器 `concurrency` 字段 | ❓ 未定 |

**资产不需要锁** —— append-only + 内容寻址 ⇒ 结构上不可能丢失更新。**这是不做资产锁的唯一理由**，改成可变资产就必须重新设计。

**残留风险（写进指南）**：

> **聚合判定只能在同步 handler 里做，不能在 agent 里做。**
> agent 在 `execute` 期间读到的 `history` 是快照，`apply` 时可能已变；而节点级 fence 管的是"同一节点被重复 claim"，**不管"两个不同节点写同一个对象"**。这是 stale read，append-only 挡不住。

### 10.3 隔离级别 ✅

| 阶段 | 级别 |
|---|---|
| 当前（单线程驱动） | **可串行化** —— 由结构给出，不靠加锁 |
| 并行调用之后 | **节点级乐观并发（OCC）** |

三段式：**claim（同步临界区）→ execute（await，临界区外）→ apply（同步临界区）**。`await` 是 JS 里唯一让出点，所以"execute 在提交锁外"是**结构保证**。

**并行驱动器不需要单独做**：`#busy` 已挡住同节点并发 claim，调用方直接 `Promise.all([stepAgent(), stepAgent()])` 即可。所谓驱动器是 ~20 行便利封装，不是新能力。

### 10.4 状态不变量断言 ✅

跨四套状态机（实例 / 消息 / 执行 / 锁）的约束，以前散在各处 if 里没有一处声明。现在集中成断言函数，测试每次提交后跑：

- 消息 `CLAIMED` ⟺ 存在引用它的 `RUNNING` 记录
- 实例 `TERMINAL` ⇒ 无在途消息 ∧ 无在途执行 ∧ 无持有锁
- `child` 锁存在 ⇒ 子实例存在且未终态

这是 DBMS 里 CHECK constraint 的位置。**延迟约束**（如汇聚饥饿）也归这里，只报警。

---

## 11. 权限与信任边界 ✅

### 11.1 操作分三类

借 DBMS 的 DDL / DML / DQL 分层 —— 它天然对应"改定义 / 改运行状态 / 只读"，而这三件事的权限要求本来就不同：

| 类 | 操作 | 典型主体 |
|---|---|---|
| **DDL** | `define` · `propose` · `approve` · `publish` | human / 经审批的 agent |
| **DML** | `send` · `run` · `control` · `truncate` · `settle` | human / agent / service |
| **DQL** | `query` · `snapshots` · `locks` · `observe` | 全部 |

### 11.2 授权 = (principal, scope, 操作类)

`scope` **复用前缀机制**（第六次复用）：

| scope 形式 | 管什么 | DBMS 类比 |
|---|---|---|
| 路径前缀 `nodes/coder/*` | 哪些定义路径可改 | 对象级 GRANT |
| traceid 前缀 `job-1/` | 哪些实例可操作、**能写哪些对象**（§7.7） | **行级安全 RLS** |

权限是**数据**（scope + 操作集），不是代码：换策略只改授权表，内核与控制面代码不动。**默认拒绝** —— 没有匹配的授权就是不允许。

**检查放在控制面，不穿进内核签名。**三条理由：

1. §11.3 要求 `Principal` 必须由可信边界注入 —— 那个边界就是控制面，注入一次即可
2. **Runtime 保持纯引擎**：它不知道谁在调它，也就不可能被"payload 里自称的 actor"骗到
3. 内核方法签名不因为加权限而变形

"根容器 MCP 全权"（G3）因此就是一条数据：`{principal: "*", scope: "*", ops: ["DDL","DML","DQL"]}`。

### 11.3 其余

- **根容器编辑：MCP 全权**（G3 的决定）。安全网不靠限制，靠版本可回滚
- `Principal` 必须由可信边界注入；`arguments` 里自称的 `actor` 永远不可信 ✅

> **首要不变量 —— Agent 只能在预先声明的选项中选择，永不构造地址、能力、契约或锁。**

覆盖面：端口 ✅、内核工具 📋、隧道标签 ✅、容器模板 ✅、子槽（`entry` + `exit`）✅、锁 ✅、内网名 ✅ —— 一条规则七处应用。

---

## 12. 文件控制 📋

草稿要求"rules 规定根、编辑范围限定具体范围、越界 git 临时存档"，V4 整个推给 agent 的 tool。**理由对，落点错** —— 结果是文件范围在画布上不可见、不进版本。

| 层 | 做什么 | 状态 |
|---|---|---|
| **声明** | `bind` 段的 `workspace` / `scope` 变量，画布可见、随容器进版本 | ✅ 机制在 |
| **强制** | **沙箱**（§14.5）—— 不是 tool executor。有原生工具的 agent 拦不住，只能靠隔离 | ✅ docker 运行器 + 出网三档 |
| **观察** | **沙箱外的 git**（§14.4）——**内核仍不认识 git**，是适配层在用 | 📋 |

早期稿把强制放在 tool executor 上，那是错的：**外部 agent 用的是自己的工具，
executor 拦不到它**。唯一真强制是沙箱边界。

---

## 13. 观测与画布 🚧

- **真相源只有容器定义 json**，画布是视图，状态永远可重建
- **`_layout` 独立于语义指纹**：单独 kind 的 ObjectVersion，`object_id = layout/<template_id>`（**按 template_id 不按 ref**，跨版本继承）📋
  - V4 的 bug：layout 按 ref 键、只在 register 时写、不落盘 → 挪个节点要发新版本，重启后布局全丢
- **RunSnapshot** ✅ —— 每次提交写 `ObjectVersion(kind="run")`，body 含 `consumed[]` / `produced[]`。**`consumed → produced` 就是因果边**；`seq` 只由提交推进（claim 单独写 ExecutionRecord），所以快照序列无空洞
- **观测投影**要能回答：子树状态 ✅、锁表 ✅、**当前内网形态** 📋（边可运行时改，画布必须查"现在的边"）、提前遍历（固定深度 1）📋

---

## 14. 执行面：沙箱 + 命令行

> **agent 就是一条命令行。**`claude <args>` / `codex <args>` / `hertaloy agent <args>`
> **三者平权**，没有谁是特例。内核不认识它们各自的工具。

这一节取代了早期稿的"backend 矩阵 + 内核工具桥"。那套想**替代/中介** agent 的工具，
现在的做法是**注入 + 自由 + 记录**：给它上下文，让它用自己的工具，从外面观察它干了什么。

**第五次归约**：内核工具（`read_artifact` / `publish` / `spawn`）→ **端口 + 变量 + 观测**。
agent 的编排面动作已经能用端口表达（emit 到 `tunnel+callback` 端口就是发 REQUEST），
所以那套工具桥与 `kernel_tool ⇄ kernel_tool_result` 线协议**整个不需要**。

> 第一不变量管的是**编排面**（地址 / 能力 / 契约 / 锁），不是执行面。
> agent 用自己的 `read_file` 不构造任何地址；它 emit 到哪个端口才是编排面的事，
> 那个仍然是声明出来的。两者不冲突。

### 14.1 四个命令行面 📋

| 面 | 跑在哪 | 干什么 |
|---|---|---|
| `hertaloy <控制面命令>` | 宿主机 | 定义 / 实例 / 观测 / 截断；MCP server；卡片资产管理 |
| **`hertaloy agent`** | **沙箱内** | 我们自己的 agent 实现，与外部 CLI 平权 |
| `claude` / `codex` / … | 沙箱内 | 外部 agent，内核不认识它们的工具 |
| 沙箱运行器 | 宿主机 | `local` / `wsl` / `docker` —— 同一形状：隔离环境里跑 argv + 挂一个目录 |

**同一个二进制两种角色**：`hertaloy` 在宿主机上是控制面，在沙箱里是 agent。
这不是巧合 —— 平权要求我们自己的 agent 也只是"一条命令行"。

### 14.2 沙箱契约 📋

内核与 agent CLI 之间是一份**目录契约**，像 Docker 的 entrypoint 约定：

```
沙箱/
  workspace/              真实项目文件（工作树，.git 在外面）
  .hertaloy/
    context/              ← 注入（按 profile 渲染）
    request.json          ← 允许的 emit 端口、预算、traceid
    emit.json             → agent 写这里表达输出（端口名 → 载荷）
    artifacts/            → 放这里的文件被收成版本化资产
```

不变量都不破：`emit.json` 的键校验 ⊆ `allowed_emit_ports`（第一不变量）；
变量落成文件后上界照算（B1）。

### 14.3 适配 = 把卡片渲染成各家认识的文件 📋

**不同 agent CLI 认不同的文件约定，适配层就是渲染器 —— 不是特例代码，是配置。**

| 卡片 | `claude-code` | `codex` | `hertaloy-agent` |
|---|---|---|---|
| rules | `CLAUDE.md` | `AGENTS.md` | `.hertaloy/context/rules.md` |
| skill | `.claude/skills/<n>/SKILL.md` | 追加进 `AGENTS.md` | `.hertaloy/context/skills/` |
| mcp | `.mcp.json` | — | `.hertaloy/context/mcp.json` |
| session | `--resume <id>` | 各家自己的 | 原生 |
| **教它写 emit** | **注入一条 skill** | 同左 | 原生懂 |

最后一行是关键：**让外部 agent 学会我们的输出契约，靠的就是 skill 注入机制本身**。
草稿 line 3 那句「之后调用直接上下文尾部补充，**并落在 agent.md**」说的就是这件事。

### 14.4 记录靠沙箱外的 git 📋

```bash
git --git-dir=<沙箱外的记录仓> --work-tree=<沙箱>/workspace  diff
```

`.git` 在沙箱外，工作树在沙箱内 —— **agent 看不到任何 `.git`，我们照样能观察**。
这是 git 原生能力（等价写法：`GIT_DIR` + `GIT_WORK_TREE` 环境变量），不需要第三方工具。

**比自报的 `observations` 强得多**：不需要 agent 配合，也骗不了。
`observations` 字段仍然保留为补充（agent 主动汇报的不可干预内部工具调用），但主力是 git。

| 需求 | 工具 |
|---|---|
| 沙箱不可见的版本观察 | **`git --git-dir` 外置**（原生） |
| 多 agent 并行改同一仓 | `git worktree`（共享 .git，各自工作树） |
| 写时复制层（真镜像层语义） | overlayfs（Linux） |
| 轻量进程沙箱 | bubblewrap（Linux） |

### 14.5 沙箱强度：两堵墙，要分开认 ✅

隔离不是一个布尔。**文件系统**和**网络**是两堵不同的墙，最常见的一档正是
"隔离了但能出网"——把它们合成一个"安全吗"，这一档就无法表达。

| 运行器 | 文件系统隔离 | **强制出网策略** | 平台 |
|---|---|---|---|
| `local` | ❌ 只是目录限定 | ❌ | 全平台 |
| `wsl` | ✅ | ❌ 与宿主机同一张网 | Windows |
| `docker` | ✅ | ✅ `--network` | 全平台（需装） |

`Runner.enforcesNetwork` 是**必填**字段，不给默认值：默认成 `true` 就等于让
"忘了实现"冒充"实现了"。backend 把 `isolates` 与 `networkEnforced` 双双写进
diagnostics —— 声明与事实由同一处产出。

**出网三档**（"内网 + 容器网"）：

| 策略 | docker | 谁能通 |
|---|---|---|
| `none`（默认） | `--network none` | 谁也不通 |
| `internal` | `--network <名>`，网建时带 `--internal` | **只有同网的兄弟沙箱** |
| `open` | `--network bridge` | 全通 |

内网名取**根 traceid**（`networkNameFor`）——前缀机制的第七次复用：同一次运行下
的沙箱互通，跨运行在网络层就是隔开的，不靠命名约定自觉。

**明确不做按域名放行**：docker 原生做不到，要做得起一个强制全流量经过的出网代理。
假装支持一个实际不强制的白名单，比没有更危险。

两条实现上的坑，都是真起容器才照出来的：

- **必须覆盖 ENTRYPOINT**。镜像的 entrypoint 会把 argv 接在自己后面（`alpine/git`
  的 entrypoint 就是 `git`，于是 `["cat","x"]` 变成 `git cat x`）。runner 的契约是
  跑**给定的**命令行，不是镜像作者设想的那条。
- **超时要 `docker kill` 容器**，杀本地 docker 客户端只是断开与守护进程的连接，
  容器照跑。与 WSL 那条"另一个内核里的另一棵树"是同一类错。

**`local` 只适合本机开发，不能当安全边界。**这条要写在使用文档最显眼处。

### 14.6 五种终止原因不能混成一种 ✅

判据是**"重试会不会有不同结果"**，不是"错在哪一层"：

| 类 | 成员 | 处理 |
|---|---|---|
| **可重试** | `FAILED`（真故障）· `INVALID_OUTPUT`（没按契约出） | 按 `maxAttempts` 重试 |
| **不重试·意图** | `CANCELLED` · `BUDGET` | 消息 DISCARDED |
| **不重试·确定错** | 变量超上界、卡片解析失败 | 直接失败 |

同步路径（受信 handler）不重试，agent 路径有 attempts —— 这个不对称是**故意的**：
受信代码失败是编程错误。

> **backend 是不可信边界** ✅：返回值必须运行时校验形状、`executionId` 串号、
> **禁止伪造内核保留 kind**；产物全部校验通过才提交。

---

## 15. 从 V4 继承与丢弃

### 直接继承（约 V4 代码的 40%）

执行面（三段式、`ExecutionRequest/Result`、线协议、driver、tool executors）、版本面（ObjectStore、内容寻址、lineage、V1–V4）、上下文估算（CJK 系数校准）、契约校验（JSON Schema 子集）。

### 明确删除

`ContainerSlot` / `SubscriptionDefinition` / `children` / `pool_cursor` / `overflow` / `collect_orphans` / `WARM_POOL` / 四种多余 node kind / 策略节点 / `AWAITING` / 消息的 `exit_port` 与 `mkind` / `CROSS_ALL` / `TOP_ONE RETAIN` / `strict_contracts` 双模式 / `transform.role` / `QueueInstance` 作为对象 / 五处隐式默认 / 三个散落魔数 / Backend 矩阵第四行 / Python↔TS parity 作为长期 CI 门禁。

### 已修的 V4 实质缺陷 ✅

| # | 缺陷 | 修法 |
|---|---|---|
| 1 | `propose` **完全不校验**，校验只在 approve 时触发 ⇒ 人成了 AI 的语法检查器 | 注册期跑全量校验 |
| 2 | `_layout` 按 ref 键、只在 register 时写、不落盘 | §13（📋） |
| 3 | 消息无因果链 | RunSnapshot 承担 |

---

## 16. 不变量总表与强制状态

**"强制方式"列是这份文档最该被追问的地方** —— 上次外部审核抓的 7 条 P1，全部出在"文档写了但代码没兑现"。

| 编号 | 内容 | 强制方式 |
|---|---|---|
| **首要** | Agent 只能在预先声明的选项中选择 | ✅ 端口白名单 / 子槽 / 隧道；🚧 内核工具未做 |
| **C1** | 根容器唯一，不由模板创建 | ✅ 代码 + 测试 |
| **C2** | traceid 段边界前缀匹配 | ✅ 代码 + 测试 |
| **C3** | 内网实例不自行判定终止 | ✅ 结构（无该 API） |
| **C4** | 实例终身 pin；继承 eager 物化 | ✅ 代码 + 反例测试（改基模板不影响已有实例）|
| **C5** | 版本历史即状态，不设节点内 persistent | ✅ 结构（已删字段） |
| **L0** | 内核无时钟 | ✅ 结构（无时钟依赖） |
| **L1** | 锁 2 种，内核专有，零配置 | ✅ 结构（无公开 API） |
| **L2** | 锁 owner 唯一是容器 | ✅ 类型 |
| **L3** | 截断靠 generation fence + 反向清账 | ✅ 代码 + 反例测试 |
| **L4** | 终态 ≠ 回收 | ✅ 结构 |
| **L5** | 三谓词终止 | ✅ 代码 + 断言 |
| **M1** | 编排权威属于内网边 | ✅ 结构（handler 返回值无目标字段） |
| **M2** | 隧道 ∩ traceid 前缀 | ✅ 代码 + 测试 |
| **M3** | callback 落已声明端点 | ✅ 注册期校验 |
| **S1** | servo 纯提取，变量集编译期已知 | ✅ 结构 |
| **S2** | servo 无控制流 | ✅ 路径文法 |
| **X** | 稳定前缀不因运行期发现而改变 | ✅ 结构（两个 schema） |
| **B1** | 预算注册期校验，运行期超上界直接失败 | ✅ 代码 + 测试 |
| **V1–V4** | 版本分配 / 独立 / 幂等 / 精确引用 | ✅ 代码 + 测试 |
| — | 提交是事务，抛了整体回滚 | ✅ 代码 + 测试 |
| — | 冲突域 = generation + claim 集 | ✅ 代码 + 反例测试 |
| — | 观测不裁决 | ✅ 结构 |
| — | 判别式 `kind: "handler"` 单值 | ✅ 类型 |
| — | **对象写入受 traceid 命名空间约束** | ✅ 代码 + 反例测试（`..` 越界被拒）|
| — | 授权 = (principal, scope, 操作类)，**默认拒绝** | ✅ 代码 + 测试 |

---

## 17. 持久化 ✅（对象库 + 可变头 + 目录锁已实现）

### 17.1 现状：一处都没落盘

先说事实，别从愿望开始。全仓唯一的磁盘写入在 `packages/sandbox/src/layout.ts`
与两个 runner 的 `mkdtempSync` —— **全都写在沙箱里，且随沙箱一起删**。
编排状态（对象库、实例树、消息队列、锁账本）只活在一次 vitest 或一次
`hertaloy run` 的进程内存里。进程一退，全没。

所以"我们在哪跑"目前的答案是：**没有在跑的系统**，只有跑一次就消失的进程。

### 17.2 第六次归约：状态只有两个形态

事务已经有 A / C / I：`transact()` 是原子性，`checkInvariants()` 是一致性，
单线程 drain 是隔离性。**持久化要补的只有 D**，不是一套新东西。

把内核里所有可变容器摊开，按"是不是状态"分类：

| 东西 | 住哪 | 是状态吗 | 形态 |
|---|---|---|---|
| 对象版本（资产 / 模板 / 卡片 / RunSnapshot） | `ObjectStore` | ✅ | **不可变、写一次** |
| 实例树（status / seq / generation / slot） | `InstanceRegistry` | ✅ | 可变 |
| 消息队列 + 投递顺序 | `Runtime` | ✅ | 可变 |
| pending 请求表 | `Runtime` | ✅ | 可变 |
| 执行记录（claim / apply） | `Runtime` | ✅ | 可变 |
| 锁账本 | `LockLedger` | ✅ | 可变 |
| 三个 seq 计数器 | `Runtime` | ✅ | 可变 |
| handler 代码 | `registerHandler` | ❌ **是代码不是状态** | —— |
| 沙箱目录 | 临时目录 | ❌ 缓存 | —— |
| git 记录仓 | 沙箱内 | ❌ 证据 | —— |
| 日志 | 尚无 | ❌ 观测 | —— |
| 密钥 | 尚无 | ❌ **绝不进对象库** | —— |

七行"是状态"里，**第一行是不可变追加，其余六行合起来是一个小可变头**。

> **归约**：落盘只有两种文件 —— **写一次的对象** 与 **一个可变头**。
> 不是九种配置各有各的存法。

这不是巧合：C5（版本历史即状态）早就把"节点状态"逐出了内核，剩下的可变量
只有编排自己的账。账很小 —— 实例数 × 常数 + 在途消息数，不随历史增长。

### 17.3 目录布局

对象 id 已经是 traceid 命名空间（`job-1/coder-1/result`），所以它**本来就是路径**。
落盘直接按段展开 —— 这正是当初"多层命名空间 / 文件形式，通过路径找"那句话的落点。

```
<state-root>/
  objects/<id 各段>/@<n>.json     不可变，写一次；文件名即版本
  runs/<root>/
    head.json                     可变头（全量，原子写）
    head.lock                     进程级排他锁 —— 见 17.8
    logs/<date>.jsonl             观测，不参与重放
    record/<traceid>.git/         沙箱证据，可选保留，不参与重放
```

对象库**不按 run 分目录**：模板、profile、工具定义是跨运行的全局对象，
资产是运行内对象，两者同在一棵树上只是前缀不同。回收也走前缀
（删 `objects/job-1/`）—— 前缀机制第八次复用。

**一个已知的布局坑**：RunSnapshot 的 id 是 `run/<traceid>`，前缀是 `run/` 而不是
`<root>/`。按 `job-1/` 回收会漏掉 `run/job-1/…`。要么改 id 规则，要么回收扫两个前缀 ——
先记下来，别让它在第一次 GC 时才被发现。

**路径管理必须认的三条**（这也是"后续落在 WSL/Linux"那个决定的又一个理由）：

- Windows 文件名大小写不敏感 → `result` 与 `Result` 会撞成同一个文件，
  而对象库里它们是两个对象。**state-root 必须落在大小写敏感的文件系统上。**
- Windows 保留名（`CON` / `PRN` / `AUX` / `NUL` …）会让合法 object_id 建不出文件。
- 路径长度上限：深嵌套 traceid + 长对象名会顶到限制。

结论：object_id → 路径需要一条明确的转义规则，且 state-root 建议落 Linux。
不写这条规则就落盘，等于把一批只在特定机器上复现的 bug 埋进去。

### 17.4 耐久性边界：claim 必须落，其余可以攒

"每次提交都 fsync"是最贵的做法，而且没必要。真正的判据是
**重放这次提交会不会产生第二次副作用**：

| 提交类型 | 重放代价 | 落盘要求 |
|---|---|---|
| handler 执行 | 无（见 17.5） | 可攒到静止点 |
| 路由 / 锁变更 / settle | 无 | 可攒到静止点 |
| **agent claim** | **真花钱、真产生外部副作用** | **必须当场落盘 + fsync** |

claim 之后、apply 之前崩溃，恢复时看到一条没有 apply 的 claim：generation fence
（L3）保证旧结果落不回来，所以只能重跑 —— 这一次的钱白花了。**这是已知代价，
不是 bug**；要省下它得在 claim 里一并记下沙箱路径，恢复时去捞现场，属可选优化。

反过来，claim **不落盘**才是真的坏：恢复后内核根本不知道有个 agent 在外面跑，
会再派一个，两个 agent 同时改同一份东西。

### 17.5 重放幂等靠内容寻址，不靠额外机制

`ObjectStore.put` 的 dedupe key 是 `object_id + 内容哈希`，同 id 同内容直接返回
已有版本、**不产生新版本**。所以从检查点重放 handler 时，写出的对象与第一次逐字节
相同 → 命中 dedupe → 版本号不涨。RunSnapshot 同理（`seq` 从检查点恢复，body 一致）。

> 幂等不是为持久化新加的机制，它是内容寻址**已经具备**的性质。

代价是一条**必须写进开发指南的 handler 契约**：

> **handler 必须是确定性的。** 同样的输入变量 + 同样的对象库状态 → 同样的输出。
> 不许 `Date.now()`、`Math.random()`、不许读进程外的可变状态。

L0（无内核时钟）本来就往这个方向推。真需要不确定性的（取随机数、读当前时间），
它就不再是 handler 而是 agent —— 走沙箱那条路，用 claim 保护。

### 17.6 不是状态的四样东西

混进来会让持久化层背上不该背的东西：

- **沙箱目录** —— 缓存。删了重跑能重建，不参与恢复。
- **git 记录仓** —— **证据**。它回答"agent 到底改了什么"，是审计与调试面，
  重放不读它。可配置保留到 `runs/<root>/record/`，默认随沙箱删。
  注意它与内核的对象版本是**两套 git 无关的历史**：观察结论（`Observation`）
  已经进了 diagnostics，那才是状态。
- **日志** —— 观测。与 `Usage` 同类：可以为零、可以丢、不参与裁决。追加写 jsonl。
- **handler 代码** —— 代码。两种：内建 handler 注册在代码里；**脚本 handler 是对象库
  里的脚本，跑在沙箱里** —— 第五次归约说"agent 就是命令行"，那么脚本 handler
  就是 `node script.js`，同一条路，不是第三种执行面。
  脚本进对象库的好处正是你要的那句：**改脚本 = 新版本 = 新实例**。

**明确不用 git 存对象库。**对象已经有版本号和内容哈希，套一层 git 等于在一份历史上
再叠一份历史，还要再引入一套并发模型和一套 GC —— 正是前五次归约在删的那类共存机制。

### 17.7 密钥：绝不进对象库

对象库是**不可变、内容寻址、按前缀可读**的。密钥一旦写进去就撤不回来：
删不掉、改不了、凡是有该前缀读权限的都看得见。所以：

- 密钥**永不是对象**。对象里只放引用（"用哪个密钥"），值在外部 provider。
- 值在沙箱物化时以环境变量注入，随沙箱一起消失。
- 日志与 observation 落盘前必须**脱敏**。

**审计中发现的一条真泄漏**（`packages/sandbox/src/runner.ts:155`）：

```ts
env: { ...process.env, ...spec.env },   // LocalRunner
```

`LocalRunner` 把**编排进程的整个环境**摊进每个沙箱 —— 编排器持有的所有密钥
（模型 API key、云凭据）对每个本地 agent 全部可见。`DockerRunner` 与 `WslRunner`
都只传显式 `-e` / `env K=V`，没有这个问题。

修法不是删掉那行就完了：agent CLI 需要 `PATH`、`HOME` 这些才能跑。要的是一份
**白名单**（`PATH` / `HOME` / `LANG` / `TERM` …）+ 显式注入，与另两个 runner 对齐。
这是一处待改，不是本轮顺手改掉的东西 —— 它会动到现有沙箱测试的前提。

还有一条回流路径要认：**git 记录仓会捕获 agent 自己写进文件的密钥**。
所以记录仓与日志同属敏感面，权限上不能与对象库同级。

### 17.8 两种锁，别混

你列的"状态/锁文件"其实是两个不同层的东西：

| | 内核锁（L1/L2） | 状态目录锁 |
|---|---|---|
| 是什么 | `request` / `child` 两种阻塞态 | `head.lock`，进程级排他 |
| 谁持有 | 容器实例 | 操作系统进程 |
| 防什么 | 实例过早消亡 | **两个 `hertaloy` 进程同时写一个 run** |
| 落盘 | 在 head 里（是状态） | 是文件本身（不是状态） |

第三种是你提过的"外部文件 lock"：那是**沙箱内 agent 之间**的协调，
内核不认识、也不该认识 —— 它发生在 workspace 里，属 agent 自己的事。

### 17.9 权限的自举

权限表如果是对象，改权限就要过 `ControlPlane`，而 `ControlPlane` 要读权限表判断 ——
成环。解法是分层：

- **根权限表来自启动配置**（外部文件，人给的）。它是信任的起点，不在对象库里，
  改它要停机改文件 —— 这正是"人有完整权限"该有的形状。
- **子作用域的授权是对象**，走版本历史、可审计、可撤销（发新版本）。

### 17.10 你列的九项，各自去哪

| 你提的 | 归到哪 |
|---|---|
| 文件与路径管理 | 17.3（含大小写 / 保留名 / 长度三坑） |
| git | 17.6 —— 记录仓是证据；对象库**不用** git |
| 快照 | 17.2 可变头 = 检查点；RunSnapshot 是因果权威，已是对象 |
| 流程配置 | 已经是对象（`container_template`），随对象库落盘 |
| handler 脚本 | 17.6 —— 对象库里的脚本，跑沙箱，改脚本即新版本 |
| 状态 / 锁文件 | 17.2 进 head；17.8 区分三种锁 |
| 敏感文件 | 17.7 —— 绝不进对象库；含一条待修的真泄漏 |
| 日志 | 17.6 —— 观测，追加写，不参与重放 |
| 权限文件 | 17.9 —— 根表在启动配置，子授权是对象 |
| 工具文件 | 已经是对象，物化时由 profile 渲染进沙箱（§14） |

### 17.12 实现中撞出来的一条：计数类写法必须把序号写进 body

C5（版本历史即状态）与内容寻址去重的交界处有一个尖角，**两个内置 handler 都踩塌了**：

`put` 对**同 id 同内容**返回既有版本、不产生新版本。所以"重复发生的相同事件"
在版本历史里只留下一条。后果不是数字算错，是**流程卡死**：

| handler | 原写法 | 实测 | 后果 |
|---|---|---|---|
| `loop` | `{ at: Date.now() % 1 }` | 写 5 次 → **1 版** | `rounds > 1` 永不退出 |
| `collect` | `{ value }` | 收 3 份相同结果 → **1 版** | 汇聚永远等不齐 |

`Date.now() % 1` 恒等于 0（毫秒数是整数）—— 一次想凑唯一值而失败的尝试。
而且就算它成功了也是错的：那会违反 17.5 的确定性契约，重放时对不上。

正确的序号来源是 `ctx.history(name).length + 1`：**既单调又确定**。

> 这条要进开发指南。它不是 bug 修完就算了 —— 凡是"数发生了几次"的模式，
> 都得显式写序号，因为内容寻址的默认行为是把重复**合并**掉。

### 17.13 明确不做

- **不做 WAL**（暂时）。可变头很小，全量原子写（临时文件 + rename）就够。
  WAL 是"head 大到重写成本压过来"才需要的优化 —— 那一天用**在途消息数**度量，
  不是靠感觉。
- **不做多进程并发写同一个 run**。一把 `head.lock` 挡住，比做分布式一致性便宜三个数量级。
- **不做对象库的跨机同步**。

## 18. 待开发清单

| 批 | 内容 | 依赖 | 备注 |
|---|---|---|---|
| ~~F~~ | ~~对象命名空间~~ | — | ✅ **已完成** |
| ~~B~~ | ~~eager 物化继承 + 路径覆盖解析~~ | — | ✅ **已完成** |
| ~~C~~ | ~~DDL/DML/DQL 分层 + 前缀 scope 授权~~ | — | ✅ **已完成** |
| ~~G~~ | ~~容器工具：从节点内建子容器~~ | — | ✅ **已完成** |
| **S** | **沙箱 backend**：契约目录 + 运行器（local/wsl/docker）+ 外置 git 观察 | — | §14，**执行面的地基** |
| **P** | **profile 渲染器**：卡片 → CLAUDE.md / AGENTS.md / .mcp.json / skills/ | S | §14.3，适配的全部内容 |
| **A2** | **`hertaloy agent`**：我们自己的 agent CLI（V4 的 openai-compat driver 转过来） | S | 平权的另一半 |
| **H** | 控制面容器（人是容器）→ 接通审批链路 | — | 帧 6 |
| **J** | `_layout` 独立版本 + 观测投影补齐 | — | G4 |
| **K** | MCP 工具层（控制面开放出去） | — | G1 |
| **D** | 开发指南文字部分（可运行例子 + CLI 内置库已有） | — | 含"聚合只在同步 handler 里做" |
| — | 持久化、崩溃恢复、claim 接管 | 全部 | 未排期 |
| — | 服务端、实时事件、画布 | K | 未排期 |

**开工顺序：~~F~~ → ~~B~~ → ~~C~~ → ~~G~~ → S → P → A2。**

内核侧四批已完成。接下来是执行面：**S（沙箱 backend）是地基** —— 它打通契约之后，
P（profile 渲染）与 A2（自己的 agent CLI）都只是往上填，`claude` 也能立刻接上去试。

### V4 迁移纪律（批 I）

V4 的实现能省很多事，但它**带着 V4 的对象模型假设和已知 bug**。迁移必须守四条：

1. **先写 V5 侧契约测试，再迁实现** —— 与 Task 1–3 同样的顺序。测试用 V5 的词汇写，不照抄 V4 的断言。
2. **不整文件复制，逐函数读** —— 手边开着 §16 那张表，每迁一段就问：它落在哪条不变量上？答不出的部分十有八九是 V4 的遗留假设。
3. **错误分类先对齐再迁** —— V4 的终止原因语义与 §14.1 的「可重试性」判据不完全一致，先改语义再搬代码。
4. **`archive/` 是只读证据，永不 import** —— 已在 README 定过，迁移时尤其容易破例。

最危险的是**线协议**（`kernel_tool` ⇄ `kernel_tool_result`）：它绑定的是 V4 的内核工具集，而 V5 的内核工具还没定形（§18 开放项）。**建议线协议最后迁**，等工具集定了再动。

---

## 19. 开放项

**已定（原开放项）**：路径语言用最小自定义子集；不加第四种变量类型；固定提前遍历深度 1；无内核时钟；不做暂停（要停就截断）；不做并行驱动器（能力已有）。

**待定案（❓）—— 资产注入与动态打包**

这是当前最大的未定项。三条已有倾向，未落地：

| 问题 | 倾向 |
|---|---|
| **不同注入方式**（rules 全文 / mcp 名称+摘要 / skill 压缩索引） | 加一个可选 `select` 字段，**复用 servo 的路径语言**读卡片的某一面。零新机制 |
| **动态打包怎么限定** | 限定**搜索的界**而不是结果：`search: {prefix, tags, limit}` + 单个 `max_tokens` ⇒ 预算贡献 `limit × max_tokens`，**注册期仍可算，B1 不破** |
| **skill 分级**（全局/项目/会话，草稿 line 3） | 就是 `PATH`：搜索路径按序，首个命中胜出 |

**仍开放（❓）**：

- 容器 `concurrency` 字段要不要做（限住子树同时执行数，LLM 限流用）
- `workspace/` mount：把文件系统并进对象命名空间
- token 估算系数是否按 backend 分档
- 内核工具（agent 侧的 `read_artifact` 等）的声明形式
- 非纯转发边何时物化为容器实例

---

## 20. 验收方法

每加一个持久对象，追问：

> **删掉它，剧本的哪一帧无法正确表达？**

每加一个机制，再追问：

> **这个机制是在减少并存的机制数量，还是在增加？**

四次归约都是减法。任何让实体或机制数量重新上升的提案，需要先证明它不能被压进已有的某一层。

**第三条（本轮新增）**：

> **文档写了「不变量 X」，§16 那张表里它的"强制方式"是什么？**
> 答不出具体的代码位置或测试，它就只是愿望 —— 上次外部审核的 7 条 P1 全部出在这个缝里。
