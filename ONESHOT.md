# Hertaloy 架构设计概要（ONESHOT）

> **这份文档是自包含的。** 它不依赖任何其他文件 —— 只读这一篇，应该就能照着
> 设计出形状一致的实现，或者判断一个提案该不该接受。
>
> **与 `MODEL.md` 的分工**：`MODEL.md` 是**带证据的定案**（完整论证、字段表挂代码
> 位置、漂移账、迁移路线）；本文是**不带证据的规格**（该是什么形状，照着它做）。
> 同一批事实，两种功能。**凡两者冲突，以 `MODEL.md` 为准** —— 本文是它的规格化
> 摘录，漂移只可能是本文没跟上。
>
> 用法三种：
> 1. **设计指导** —— 写新模块、评审新提案时照 §10 与 §11 逐条过。
> 2. **一次投喂** —— 整篇交给一个模型，看它能否据此做出形状一致的东西。
> 3. **边界测试** —— §10 是一张"不许做"的清单；给模型一个任务，看它踩不踩。

---

## 1. 一句话架构

> **把长任务切成多个短上下文的 agent 执行，用容器图编排它们、用版本化产物缝合
> 它们，从而避免单 agent 上下文膨胀与注意力涣散。**

推论（整个系统的存在理由）：**上下文预算从"运行时救火"变成"注册期拒绝"**。
一个 agent 能看到什么 = 它的端口与声明的变量集合，注册期就是有限且已知的。

类比一句话记住：

```
容器           = 虚拟机（可嵌套虚拟机）
网关/资源/工具  = 带钩子的模拟 OS
模板           = 存储（磁盘上的可执行文件）
实例           = 内存里的进程
消息           = 被"量化"的连接 —— 有生命周期的那一段
名字           = 宏定义：使用处只写名字，正文在别处
```

---

## 2. 本体：只有容器

| | |
|---|---|
| **B1 本体** | 只有**容器**一类元素。容器自身含模板 / 网关 / 容器工具 / 安全工具。**不存在容器之外的实例，也不存在"节点"这种东西。** |
| **B2 运行** | 模板是存储，实例化后是内存里跑的一条命令；同一模板允许并行多实例。**实例是一条命令的一次执行，命令会结束。** |
| **B3 通信** | 不声明"边"，而是按网络协议**占用容器端口**，经内网环回或容器间路由。嵌套地址 = 逐层索引。 |
| **B4 组织** | 容器构建本身也是命令行 ⇒ 嵌套与子流程是推论，不是额外机制。 |
| **B5 资产** | 大段信息（字符串 / 文件 / 提示词 / 工具代码）给一个索引 → 表示为变量 → 模板构命令时注入。 |

### 2.1 唯一的关系：声明 → 实例

这个关系套在每一层，而且**只有三行**：

| 声明（存储 / 磁盘） | 实例（运行 / 内存，有生命周期） |
|---|---|
| 容器模板 | 容器实例 |
| 连接声明（端口 + 路由） | 消息 |
| 子实例声明 | 子实例 |

**设计指导**：任何时候发现自己在写"一个永远不会被实例化的声明字段"，
那就是把一类实体做成了字段 —— 停下来，把它变成第三行。

### 2.2 位点持久，执行瞬时

全篇最重要的一对词：

| | 是什么 | 生命周期 | 标识 |
|---|---|---|---|
| **位点** | 一个可寻址的执行点 = 实例路径 | **持久**：声明在模板里，一个实例都没起时也在，画布画得出 | 路径字符串 |
| **执行** | 位点上的一次命令运行 | **瞬时**：命令结束它就结束 | 执行 id |

对应 inetd / systemd socket activation：监听地址常驻，每个连接交给一次处理。
**一个端口能服务无限连接，并发单位是处理进程，不是端口。**

### 2.3 拓扑在存储里，不在实例里

这是最容易搞错的一条。**图不是由活着的东西拼出来的。**

拓扑（谁能连谁）写在模板里，所以：

- 画布在**一个实例都没起**的时候照样画得出完整的图
- 实例退出不会让图散架 —— 图从来不由它承载
- 因此不需要"内网实例不许自行判定终止"这类规则：实例该结束就结束

**设计指导**：判断一个拓扑表示法行不行，用这句 ——
**画布必须画得出还没发生的连接。** 运行时涌现的拓扑（actor 的能力传递、
连接即实体）不能作为拓扑的唯一表示。

### 2.4 「默认起 / 按需 spawn」

子实例声明表里有一个标记：容器构建时就起，还是按需 spawn。
它**只回答"构建时起不起"**，不表达两种生命周期，也不参与终止判定。

---

## 3. 对象模型

> 这一章是"照着设计"的核心。**任何新提案先在这张表上落字段，落不下就是过度设计。**

### 3.1 声明侧（存储）

| 类 | 字段 |
|---|---|
| `ContainerTemplate` | `ports` `routes` `children` |
| `Port` = Receive \| Emit | receive: `direction` `servo?` `contract?`<br>emit: `direction` `contract?` `alias?` `callback?` `reply?` `unavailable?` `protocol?` |
| `ObjectVersion` | `object_id` `version` `kind` `content_hash` `body` `provenance` |
| `Provenance` | `instance?` `execution_id?` `derived_from[]` |
| 标识 | 路径 / 引用 / 别名 / 端口名 / 主体 / 授权，全是带正则的字符串 |

**三个字段就是一个容器的全部声明**：暴露什么端口、端口怎么连、里面有哪些子实例。

### 3.2 运行侧（实例）

| 类 | 字段 |
|---|---|
| 容器实例 | `traceid` `templateRef` `status` `generation` `seq` `slot?` `bindings[]` |
| 消息 | `id` `target` `payload` `state` `failure?` `alias?` `source?` `requestId?` `attempts` |
| 执行记录 | `executionId` `instance` `status` `termination?` `claimed[]` `generation` `usage?` |
| 义务 | `kind` `holder` `waitingOn?` `key` —— **纯派生，不存** |
| 调度候选 | `message` `position` —— **纯派生** |

### 3.3 状态机只有四个

| 状态机 | 值 |
|---|---|
| 实例 | `OPEN` `TERMINAL` |
| 消息 | `QUEUED` `CLAIMED` `CONSUMED` `FAILED` `DISCARDED` |
| 执行 | `RUNNING` `SETTLED` `VOIDED` |
| 终止原因 | `DONE` `CANCELLED` `BUDGET` `INVALID_OUTPUT` `FAILED` |

**"义务种类"不是状态机**，是派生描述符。加新的等待形态时加的是**事实来源**，
不是往枚举里再塞一个字符串然后满世界找记账点。

---

## 4. 内核边界

### 4.1 每样东西先归三类之一

```
实例能选择不用它吗？
  ├─ 能                            → 位点（被寻址、被调用）
  └─ 不能 → 换掉它破不变量吗？
              ├─ 破               → 底座（内核，在所有容器之下）
              └─ 不破，但强度可声明 → 环境（包着实例，实例看不见正文）
```

| 类 | 成员 | OS 对应 |
|---|---|---|
| **位点** | 网关、容器工具、协议、安全工具（脱敏/扫描类） | 系统调用 |
| **底座** | 队列机制、提交事务、义务枚举、别名解析、执行记录、**授权决策** | 内核态 |
| **环境** | 沙箱、git 观察、脱敏执行、资源别名 | 地址空间 / 命名空间 / fd 表 / 挂载表 |

**安全必须切两半**：安全**工具**（脱敏、凭据扫描）可以是位点；授权**决策**不能 ——
主体必须由可信边界注入，引擎保持纯净：**它不知道谁在调它，也就不可能被骗。**

### 4.2 什么能开放替换，什么不能

判据不是"能不能被替换"，而是 **换掉它，哪条不变量会破**：

```
换掉队列     → 投递一次、冻结对象、order 语义 全破 → 不开
换掉执行记录 → 孤儿判定破                        → 不开
换掉义务枚举 → 义务派生与终止判定破               → 不开
换掉别名解析 → 可见性与跨租户约束破               → 不开
换掉先跑哪条 → 一条都不破                        → 开
```

前四样是**组件**（内部分解，为了可测与可读），只有最后一样是**缝**。
**把内部分解当成可插拔点，等于把"我能替换它"误当成"别人应该被允许替换它"。**

对应 eBPF：受限语言 + 验证器，程序造不出指针、只能返回决策；而它只能挂 hook，
换不掉调度器本体。

### 4.3 递归终止条件

队列、调度、协议都可以是"规则文件启动成实例"—— 那启动它们的是什么？
**必须有一层不是模板实例。** 与"根容器唯一、不由任何模板创建"同形：
内核就是那个底座，**它不是容器**。不写这条，B4 会无限退化。

### 4.4 内核对执行面零知识

沙箱、工作区、profile、镜像、隔离强度 —— 这些是"怎么跑"，编排内核**不认识它们**。
可验证的判据：

> 内核不 import 执行面包；且 `argv` / `workspace` / `capabilities` / `profile` /
> `image` / `isolates` 在内核的非注释代码中零命中。

需要在注册期校验执行面字段时，走**注入的校验器**（外层把执行面的校验函数传进来），
不要让内核直接认识那些字段。

---

## 5. 地址与通信

### 5.1 路径就是地址

`job-1 / job-1/coder / job-1/coder/review-1` —— 实例路径，**段边界前缀匹配**
（`job-1` 捞不到 `job-10`）。一套前缀机制复用九处：身份与所有权、可见范围、
子树查询、截断级联、对象命名空间、权限 scope、观测对象名、内网名。

**要"按范围过滤"时先用它，不发明新索引。**

地址是**一段**：`{ instance, port }`。容器由取父派生，不在地址里单独存一份。

路径**不表达因果**：因果由快照的 `consumed[] → produced[]` 承担。
同一事实不存两份，存两份必然漂移。

### 5.2 端口属于容器，是槽，单一占有

- **端口属于容器**，不属于实例。实例占用容器端口。
- **一个端口单一占有。** 要多个监听者就开多个端口。
- **端口是模板声明的槽** —— 实例只能占用已声明的端口，占不到就是错。

第三条必须显式写反。"网络协议"这个比喻自带一个危险的默认：**进程可以 bind 任意
空闲端口**。照搬就破第一不变量。正确的形态是：**连接也要有槽 —— 模板声明可以连
哪些，运行时实例化其中若干。**

扇出归**路由表**，不归端口：一个别名解析出 N 个地址（DNS 多条 A 记录），
每个地址是一个独占端口。零解析不算错误。

### 5.3 两个轴，别混

```
端口数    = 接口宽度（能连什么）    ← 声明期定，写死
实例段数  = 并发度（能同时跑多少）  ← 运行期定，由 spawn 决定
```

**并发的挡板按实例路径挡，不按端口挡。** 在一个位点上多开端口换不来任何并行；
要并行只能多开实例段。

用端口数表达并行度会把一个运行期的量硬编进声明：改并行度就要改模板、发新版本、
起新实例 —— 而并行度恰恰是最该能调的那种东西。

### 5.4 一律经网关，同容器内是环回

```
实例 --send--> 自己容器的网关
                 ├─ 目标在本容器 → 环回到本地端口
                 ├─ 目标在子树   → 下行
                 └─ 目标在外面   → 上行到公共祖先，再下行
```

**内网边不是第二套机制，是环回口。**

**路径是派生的，不是走出来的。** 源与目标一确定，经过哪些网关就算得出来；
不必让消息每层物理停一次。判据：**能派生的就不要记账。**

### 5.5 网关两档

网关是一个**保留位点**（走与观测对象同一套保留名约定，不发明新索引）。
地址一直在，实例起不起是另一件事：

| | 消息 | 提交次数 | 并发 |
|---|---|---|---|
| **默认** | 路径派生，零跳 | 1 | 不涉及（没有执行） |
| **声明代理** | 真停一次 | **N** | **该位点串行** |

进第二档的条件是**协议的性质，不是架构的选择**：需要中途**改写**过路消息
（而不只是观察）的协议必须走存储转发。像明文穿代理与 TLS 穿代理的区别。

串行是这一档的第二半代价，必须一起标价。解法与 §5.3 一致：多开网关位点段。
对应 netfilter 内核态 hook vs NFQUEUE（Linux 给的解正是多队列）。

### 5.6 方向三分

最容易读出矛盾的地方。**是三样东西，三个方向：**

| 什么 | 方向 |
|---|---|
| **名字**（别名解析） | **向上**到公共祖先 |
| **数据**（产物、工作区） | **向下**拷贝 / chroot |
| **通信**（跨容器、兄弟、向上） | **横向**，经消息 |

跨容器、兄弟、上层要读东西时，**造实例去那边处理，不开放权限**。
这比"读可以跨树但要鉴权"少一套机制：读类工具**根本不需要 scope 裁剪逻辑**，
因为它不跨命名空间。对应进程隔离 + IPC，而不是共享内存加权限位。

---

## 6. 生命周期与终止

### 6.1 三条底线

- **内核无时钟。** 时间在执行面。
- **义务全部派生，没有记账点。**
- **owner 唯一是容器。**

### 6.2 四种义务，事实来源本来就有

| 义务 | 权威来源 |
|---|---|
| `message` | 消息状态 ∈ {QUEUED, CLAIMED} |
| `execution` | 执行记录状态 === RUNNING |
| `request` | 待回复请求表 |
| `child` | 子实例状态 === OPEN（**只对 spawn 出来的产生**） |

枚举函数是**纯函数**，每次现算、只读，顺序稳定，观测方可以依赖。

### 6.3 终止判定

> **可终止 ⟺ 名下义务为空。** 一行，没有递归合取项。

**义务不承担正确性** —— 强制截断永远可用；义务算错只导致"该自动回收的没回收"，
是安全方向的失败。这是"等待策略可以外置"成立的前提。

**终态 ≠ 回收。** 永远不为正确性删，只为存储删（僵尸进程：需要一个"已死但表项
还在"的状态，等人收尸）。

### 6.4 强制截断六步

外层事务，级联子树：

```
0 推进 generation（气密性的唯一依据）
1 best-effort cancel
2 未消费消息丢弃留计数
3 自己发的请求销账
4 自己承接的请求代服务方发了结通知（请求必得一个了结）
5 子实例级联
6 写终态
```

气密性靠 generation fence，**不靠 cancel 成功了没** —— 后者只是 best effort，
远端可能已经在返回路上。**冲突域 = generation + claim 集，两半都查。**
（对应 PID 重用防护。）

### 6.5 循环与回边

纯 dataflow 一旦有环，EOF 传不下去（Kahn 网络的老问题），而常驻节点已经没有了。

**判定落在容器的命令行里** —— 它是程序，可以判定。
**不要给端口加环检测**，那是凭空多一套机制。

循环计数用**对象版本号**，不用节点里的变量。

### 6.6 三段式

```
claim（同步临界区，当场落盘）→ execute（await，锁外）→ apply（同步临界区）
```

- **提交是一次事务**：消息状态、实例状态、待回复表、下游消息、产物版本、
  因果快照一起原子落。
- **claim 必须当场落盘**：崩在 claim 与 apply 之间，恢复后要知道外面有个 agent
  在跑（让出 CPU 前先存好上下文）。
- **崩溃恢复 = 孤儿认领**：拿到锁时看到的 RUNNING 必然是孤儿，复用既有失败路径
  计数重试，**不新增状态机**。
- **"本进程在驱动谁"这张表绝不能落盘** —— 句柄表是每进程的。它不落盘，
  正是孤儿能自动显形的原因。

---

## 7. 版本与命名空间

### 7.1 版本历史即状态

对象库：内容寻址、append-only、版本分配唯一权威。定义层全部并入它，
**没有平行注册表**。资产不需要锁 —— append-only 结构上不可能丢失更新。

节点里**没有 persistent 状态**：汇聚靠读版本历史，计数用版本号。
（进程无状态、状态在文件系统。）

而这条正是 §2.2 的伏笔：**一个无状态的东西为什么要常驻。**

### 7.2 命名空间就是文件系统

写：`put("results", …)` 实际写 `<本实例路径>/results` —— **写不出自己的命名空间**，
`..` / 前导 `/` / 空段一律拒绝。这是对象存储上的行级安全（chroot）。

读也要有同样的约束（这半最容易漏）：

```
history / put    → chroot 到自己
collect          → prefix 必须以自己的路径为前缀
read(ref)        → 只能读 (a) 自己子树下的，或
                          (b) 经声明注入进来的那批 ref（按引用比对）
横向 / 向上       → 不给权限，造实例 + 发消息
```

(b) 不是破口：传的是精确版本引用，对象库 append-only，所以那是一份不可变快照 ——
**给出去的是一个窄能力，不是一把钥匙。**

### 7.3 保留 kind 与保留名

内核保留 kind 受信 handler 与执行面**都不得伪造** —— 受信不等于可以伪造，
那会污染因果记录。

对象命名空间的保留名：`<instance>/$exec`（执行观测）、`<instance>/$msg`（消息落库）、
`<traceid>/$run`（因果快照）。网关代理位点若实现，走同一套。

### 7.4 已知边界

**聚合判定只能在同步 handler 里做。** agent 执行期间读版本历史是快照，
提交时可能已变。这不是限制，是分工判据：
**要新鲜状态的那一步就该是 handler，不是 agent。**

---

## 8. 工具面

### 8.1 五类，按"内核必须为它做什么"分

| 类 | 内核必须提供的保证 |
|---|---|
| **读** | 命名空间裁剪 |
| **写（入事务）** | 命名空间强制 + 保留 kind 拦截 + 随提交回滚 |
| **造实例** | 槽已声明 + 入口已声明 + 义务记账 + 随提交回滚 |
| **排期（不提交）** | 返回的地址必须**恰好是** resolve 给它的那批（按引用比对） |
| **fiat（明认破不变量）** | 单独操作类 + 外层事务 + 留记录 + 自述三件事 |

**新工具先落行，再问那一行的保证够不够。** 五类够用，不需要第六类。

### 8.2 三条纪律

1. **chroot 向下** —— 读写都不跨命名空间，跨就发消息。
2. **按引用比对** —— 排期类返回的目标必须是给它的那批。
   **安全性靠结构，不靠作者自觉。** 一个写坏的调度器/协议造不出新地址。
3. **只排期不提交** —— 排期类是纯函数，返回 plan，零部分提交。

### 8.3 fiat 类要自述三件事

1. 它**破了哪条**不变量
2. 它需要**哪一类授权**（单独的操作类，不复用别人的）
3. 它留下**什么记录**（效果必须可观测，不能静默）

强制截断是先例：fiat、单独授权、结果有记录。
（对应特权系统调用：kill / mount / setuid 各有各的 capability。）

### 8.4 agent 面：独立，而且必须独立

**agent 的工具不是 handler 的工具**，这不是分层偏好，是三段式的推论：
handler 的工具是在事务里的函数调用，而 agent 在 execute 期间跑，
那时内核不在临界区。agent 的工具要么得进一个已经不存在的事务，要么得自己开一个
新事务，后者当场破掉"提交是一次事务"。

所以 agent 侧是**写日志文件**，内核在提交期读它。没有 IPC，没有回调，没有通道。

**读改在注入期解决，不要令牌。** 令牌是「路径 + 凭据」形状：agent 从不可信边界
内部递出一个字符串，服务端验票 —— 撞上"主体必须由可信边界注入，不能自称 uid"。
正确形状是**预绑句柄**：

| 要读的 | 怎么给 |
|---|---|
| **产物** | 注入期预绑那批精确版本引用 |
| **容器与实例状态** | 注入期写入观测投影的快照 |
| **执行期间的新鲜状态** | **不给** —— 那是 handler 的活 |

要读更多 = 模板声明得不够 → 改模板 → 新版本 → 新实例。

对应 **fd 表**：内核在 exec 时把能力装进进程，进程只能用、造不出新的。
**第一不变量「只能选不能构造」就是 fd 模型，不是令牌模型。**

令牌属于**控制面**（人/AI 从外面进来、服务端多会话），那一侧主体由可信边界注入。

---

## 9. 扩展面

### 9.1 扩展按相位分，不按功能分

因为**能力上界是相位给的**。同一个功能挂在不同相位，能干的事完全不同 ——
"是什么工具"回答不了"它能干什么"，"挂在哪"才能。

```
注册模板 ──┬─ ① 注册期      校验器：只能拒绝
           │
spawn ─────┼─ ② 实例化期    物化器：把声明展开成实例本地副本
           │
claim ─────┼─ ③ 注入期（入） 环境注入：只能往环境加
  ↓ execute（锁外，内核不在临界区）
apply ─────┼─ ④ 收口期（出） 出口过滤：产物收名、脱敏、命名空间强制
           ├─ ⑤ 排期期      纯函数：只返回 plan
           ├─ ⑥ 提交期      事务内
           └─ ⑦ 观测期      只读外送
```

| 相位 | 能做 | 绝不能做 | 失败语义 | OS 对应 |
|---|---|---|---|---|
| ① 注册期 | 拒绝注册 | 改写声明 | 不落版本 | ELF 加载校验 |
| ② 实例化期 | 物化成实例本地副本 | 引入声明里没有的东西 | spawn 失败 | fork 的 COW + fd 表继承 |
| ③ 注入期 | 往环境加 | 碰编排状态 | 执行失败（可重试） | exec 建环境 |
| ④ 收口期 | 过滤、改名、强制命名空间 | 放行未过滤的 | **必须阻断落库** | egress filter |
| ⑤ 排期期 | 返回 plan | 提交、造地址 | 本次提交拒绝 | 调度器 hook |
| ⑥ 提交期 | **只因落盘失败**否决 | 因策略否决 | 整次提交回滚 | fsync |
| ⑦ 观测期 | 读、外送 | 裁决、阻断 | **不影响任何事** | auditd |

**④ 与 ⑦ 必须分开**：脱敏不是观测。观测失败无所谓，脱敏失败必须阻断 ——
否则密钥进不可变对象库，而那是删不掉的。

### 9.2 相位不是状态机

**不要给实例加 `phase` 字段。** 相位是**代码位置的分类**（挂点在哪个函数里），
不是实例上的一个状态。加了会立刻造出两个凭空的问题：这个字段进不进快照、
崩溃恢复时它是什么。相位是调用栈的属性。**七个相位落零个字段。**

### 9.3 准入三问

1. **挂哪个相位？** 一个扩展只能挂一个。既要改环境又要否决提交的，那是两个扩展。
2. **失败语义是哪一档？** 选错档是最常见的错：监控挂成阻断，脱敏挂成不影响。
3. **留什么记录？** 效果必须可观测，不能静默。

### 9.4 「改写」没有相位

注入有七个挂点，**改写一个都没有**，这是故意的：

```
改模板 → 新版本 → 新实例。不做热恢复，做重建。
实例终身 pin 创建时的定义版本，永不迁移。
```

"改写一个运行中的容器"在本模型里**不可表达**。唯一例外是 fiat 工具，
而它必须自述三件事。

---

## 10. 执行面与环境类

**agent 就是一条命令行。** 各家 CLI 平权，内核不认识它们各自的工具。
做法是**注入 + 自由 + 记录**。

唯一契约是一个窄接口（`run` + best-effort `cancel`），执行面是**不可信边界**：
返回值运行期三查（形状校验、执行 id 防串号、禁伪造保留 kind），
**产物地址由内核命名空间强制，不是 agent 报什么写什么**。

### 10.1 环境类的实现形式

| | 落点 |
|---|---|
| **声明** | 模板，**只能选强度，不许选"要不要"** |
| **强制** | 执行面，**两堵墙分开认**（文件系统隔离 / 网络强制各一个必填字段，不许合成"安全吗"） |
| **结果** | 可寻址的对象，但**不可调用**（进执行观测对象） |

第三条是「观测不裁决」的对偶：**裁决者不可调用，结果可查。**

"只能选强度"的理由：**假装支持不强制的东西比没有更危险。**
所以不允许 `sandbox: false`，只允许选隔离强度，且最弱那档必须如实报"我不隔离"。

一句话原则：**能力声明在模板里（可读、可渲染、agent 碰不到），能力使用在工具里
（可记录、有范围）。放进 agent 自己能调的工具面，就等于让它松开自己的笼子。**

### 10.2 能力单调不增

> **子容器声明的强度只能等于或严于父容器。**

理由是虚拟机模型本身：**一台 VM 不能给它的客户机比自己更高的权限。**
强制点在**执行面的校验器**（注册期调用），不在内核 —— 内核不认识这些字段。
例外走 fiat 层：提权是 fiat 操作，自述三件事。

对应 Linux `no_new_privs` / capability bounding set：做成进程的一个位，
一旦置上不可清除且被子进程继承。

### 10.3 工作区与观察

- 模板只写**名字**，真实位置由执行面配置的注册表给 —— 与密钥同构：
  **模板放取值方式不放值。**
- 物化选「预先复制」不选「只读挂载」：沙箱一次性，多一个挂载面多一个攻击面。
- **交接方向**：工作区只能沿树**向下**传一份拷贝。父给子可以（拷贝，不共享），
  子取父不行，跨父的兄弟不行。
- git 观察在**沙箱外**，记录仓不挂载，agent 够不着。物化与渲染**先于**基线，
  于是 diff 里只剩 agent 干的事。
- 脱敏是④收口期的最后一道；**真正的保证来自"密钥只经 env 注入、不落任何配置文件"。**

---

## 11. 设计边界：不许做的事

> 每条都是踩过或推导出来的。用它测一个提案（或一个模型）越没越界。

| # | 不许 | 因为 |
|---|---|---|
| 1 | 把一类实体做成"永远不会被实例化的声明字段" | 那正是"声明 → 实例"第三行空掉的形态，下游会冒出生命周期/记录/命名空间/作用域四个空洞 |
| 2 | 让实例自由 bind 任意端口 | 破第一不变量。端口是**槽**，只能占已声明的 |
| 3 | 用端口数表达并行度 | 并发挡板按**实例路径**挡；多开端口换不来并行，还把运行期的量硬编进声明 |
| 4 | 给端口加环检测 | 循环判定归容器的命令行，加检测是凭空多一套机制 |
| 5 | 给实例加 `phase` 字段 | 相位是代码位置的分类，不是持久状态。加了就要回答"进不进快照" |
| 6 | 让 agent 带令牌回调内核 | 主体必须由可信边界注入。令牌是从不可信边界内部递出来的字符串 |
| 7 | 把队列/执行记录/义务枚举/别名解析做成可替换的规则文件 | 换掉它们各有不变量会破。**能替换 ≠ 该开放** |
| 8 | 让授权决策成为可寻址位点 | 等于承认"可以发消息问我有没有权限"，结构保证当场失效 |
| 9 | 在编排内核里认识沙箱/工作区/镜像/隔离强度 | 破零知识边界。要校验就走注入的校验器 |
| 10 | 开放跨子树的读权限 | 跨容器/兄弟/上层应当**造实例去处理**，不是发一张通行证 |
| 11 | 往提交钩子挂策略判断 | 那是耐久性钩子，只允许因**落盘失败**否决。挂策略会表现为"偶发提交回滚" |
| 12 | 让 `sandbox: false` 可表达 | 只能选强度。假装支持不强制的东西比没有更危险 |
| 13 | 热更新运行中的容器 | 改模板 → 新版本 → 新实例。实例终身 pin 定义版本 |
| 14 | 让消息真走 N 跳 | 路径可派生。只有需要**改写**过路消息的协议才付存储转发的价（含串行） |
| 15 | 往"义务种类"枚举里加值 | 加的是**事实来源**，不是枚举值 —— 否则要满世界找记账点 |
| 16 | 新增状态机或字段前不查对象表 | 先在 §3 落字段，落不下就是过度设计 |
| 17 | 在不变量表里写行号 | 行号会漂，符号名不会 |
| 18 | 把"顺手全量序列化"用在持有进程本地状态的对象上 | 驱动表落盘 = 孤儿判定静默失效，而且不报错 |

---

## 12. 自检清单

任何新机制、新字段、新工具，先过这四句：

1. **删掉它，哪个真实场景无法正确表达？**
2. **它是在减少还是在增加并存的机制数量？**
3. **它声称的不变量，强制方式在哪个代码位置、哪条测试？**
4. **它要落在 §3 对象表的哪一行？**

再过这三句（如果它是扩展）：

5. **挂哪个相位？**
6. **失败语义是哪一档？**
7. **留什么记录？**

判断该不该开放替换时，只问一句：

8. **换掉它，哪条不变量会破？** —— 一条都不破才是缝，否则是组件。

---

## 附：一眼对照表

| 这里的东西 | OS 里的名字 |
|---|---|
| 容器 | 虚拟机 / 进程 |
| 模板 → 实例 | 磁盘上的可执行文件 → 内存里的进程 |
| 位点持久 / 执行瞬时 | inetd / socket activation |
| 终态 ≠ 回收 | 僵尸进程 |
| 孤儿认领 | 孤儿进程 + init 收养 |
| generation fence | PID 重用防护 |
| claim 当场落盘 | 让出 CPU 前先存好上下文 |
| 写不出自己的命名空间 | chroot |
| 主体由可信边界注入 | 不能自称 uid |
| 预绑句柄 | fd 表 |
| 能力单调不增 | `no_new_privs` / capability bounding set |
| 资源别名只给 note 不给 path | fstab / 挂载表 |
| 网关两档 | netfilter hook vs NFQUEUE（多队列解串行） |
| 位点 / 底座 / 环境 | 系统调用 / 内核态 / 地址空间与命名空间 |
| fiat 工具 | 特权系统调用 |
| 七相位 | ELF 校验 / fork COW / exec 建环境 / egress / 调度 hook / fsync / auditd |
| 排期类的按引用复核 | eBPF verifier |
| 队列不开 | eBPF 换不掉调度器本体 |
| 造实例而不开权限 | 进程隔离 + IPC，而非共享内存 |
| 版本历史即状态 | 进程无状态、状态在文件系统 |
