# V6 重审：第十二次归约提案

> 状态：**提案（2026-09-17）**，不是定案，不取代 `MODEL.md`。
> 依据：一次七视角的文档与代码勘察 + 四份互相独立的重设计（用户框架直译 / 最小机制归约 /
> OS 原语对位 / 迁移务实）。提案里每条"删"都指向一处**已核实的事实**
> （零使用 / 走不到 / 恒为常量 / 实测失败 / 剧本无帧），每条"加"都指向一个**剧本帧或一次实测失败**。
>
> 本文回答 `ITERATION.md` 第十三轮阶段 0 的 Q1–Q7，并推翻其中两条（Q1、Q3）。

---

## 0. 一页结论

**第十二次归约 =「执行即纯函数」**：执行体不能观察、也不能改变任何没有注入给它的东西；
它的全部输出是两张清单 —— `emits`（发出去的消息）与 `puts`（写下来的产物）。

这一刀直接兑现六条重审方向里的五条：

| 方向 | 判定 | 关键论据 |
|---|---|---|
| 1 文档冗余 | **成立，且已造成漂移** | ONESHOT 的 38 个三级小节全部能映射回 MODEL；「字段收敛」一件事有五处五个数；MODEL §3 字段表 5 行里 3 行的形状代码里不存在 —— 判据第四句今天**无法执行** |
| 2 不再分 handler / agent | **成立，但判别式不会消失，它换了位置** | 二分的真实内容是事务边界，而事务边界 ⟺ 执行是否跨过 `await` ⟺ **返回类型**。它属于 runner 的实现性质，不属于模板作者的选择 |
| 3 模板 = 变量 + 命令合成 + 注入 + 工具调用 | **成立，但"工具调用"那一格应当为空** | 前三格今天分散在 contracts 与 sandbox 两个包、命令合成完全不存在；第四格可全部派生，不需要字段（§4.4） |
| 4 端口与网关简洁 | **成立** | 「网关两档 / 保留位点 / protocol」代码零实现零待办；`external` 指向真实外部地址会让此后每一步 `step` 抛异常 |
| 5 图实体只有容器与实例 | **成立，且能收得更狠：只有一类实体** | 容器与程序是同一张实例表上的两种**派生读数**，不是两类记录 |
| 6 警惕状态机 | **成立，而且比你说的还严重** | 「状态机只有四个」经不起逐值核对：`VOIDED` 经公开 API 走不到，`SETTLED`/`CONSUMED` 只写不读，`generation` 只取 0 或 1，`claimed[]` 恒为单元素，`#driving` 在生产驱动里恒为空 |

**作者已定的四条**（2026-09-17）：容器不带 `exec` 段（§1.4）；子容器描述内联在父模板里、
`ChildSlot.template: Ref` 退场（§1.4）；基底与声明面的分界 —— 只有容器基底与命令行工具
自己实现的部分不声明，其余全声明且对 agent 开放（§3.4）；终止与失败通知基线走普通消息，
"系统事件原语"归优化章节（§4.2）。

**已实测的一条**（§2.3.1）：命名空间根下沉到位点，964 条里 29 条变红 ——
26 条机械更新 + 3 条真断裂（全部是帧 12 的跨位点汇聚），scene / contracts / sandbox 零红。

可核对的净账（口径见 §9）：内核状态机 **4 → 2**，内核枚举值 **24 → 8**，
驱动入口 **7 → 2**，投递路径 **8 → 5**，连接面字段位 **约 27 → 约 20**，
并且吃下三块今天完全不存在的能力：**容器变量、命令合成、稳定前缀/易变变量的结构性分离**。

---

## 1. 本体：一类实体，两种读数

### 1.1 只有 `Instance`

```
Instance = { traceid, defRef, status, bindings[] }
```

四个字段。`generation` 与 `seq` 的去留见 §6.3（我的建议与三份提案不同）。

**容器与程序不是两类记录，是同一张表上的派生读数：**

```
isContainer(p) = parentTrace(p) === null || !(lastSegment(p) in def(parentTrace(p)).nodes)
```

零字段派生。对位 Linux：PID 1 与 PID 4242 在同一张 task 表里，`is_init` 是 `pid==1` 算出来的，
不是一个字段。

### 1.2 「容器也是一条命令」怎么表达 —— 我的建议与"用户框架直译"那份不同

那份提案让容器同时拥有 `children` 与 `run` 两段（一台 VM 自己也是一条命令）。
我**不建议**这样，理由是你自己那句话的后半段：「但是此处分开」。

- **本体上承认同源**：只有一份 `Definition`，只有一张实例表，只有一条生命周期规则
  （`可终止 ⟺ 名下义务为空`）。这已经把"容器也是实例"表达完了。
- **形状上不给容器 `exec` 段**：要执行就声明一个节点位点。

代价是零（多写一个节点名），收益是消掉一整类二义：消息投给容器端口时，"这是转发还是消费"
不需要再判一次；命名空间的根、并发挡板的粒度、义务的 holder 三者不会在"容器自己也在跑"
的情况下打架。对位 systemd：unit 文件描述服务，`init` 自己不承担业务。

### 1.3 `nodes` 与 `children` 合并成一张表

今天两张表的差别只有**上界与创建时机**：`nodes` 恰好 1 个、随容器物化；`children` 0..N、按需 spawn。
合并成 `children`，静态/动态**不用字段表达**，由"是否被本容器的连接表引用"派生 ——
这条与今天 `validateContainerTemplate` 只在 `tpl.nodes` 里查 `PortRef.node` 的行为**逐字一致**，
不是新约定。

### 1.4 子容器描述内联在父模板里（作者定，2026-09-17）

> **子容器的全部模板描述保留在上层父容器模板中直接声明，不通过资产传递。**

这条定了 `ITERATION.md` 的 Q2，而且比 Q2 给的两个选项都更狠：**`ChildSlot.template: Ref`
整个退场**。一份模板对象 = 一整棵子树的描述。

四个直接后果：

1. **跨模板校验变成本地校验。** 今天 `validateChildEntries` 必须先把 `children[slot].template`
   这个 Ref 解析出来、读到子模板，才能校验入口端点存在 —— 注册期要跨对象读。
   内联之后子树就在手里，一次遍历全查完。
2. **子实例不再需要自己的 `templateRef`。** 它的定义是父的定义的一段，
   C4（实例终身 pin 定义版本）由父那一次 pin 承担，**少一个字段**。
3. **复用改走覆盖层。** 今天"同一个 coder 模板被多个父引用"靠 Ref；内联之后靠
   `TemplateOverlay`（`extends` + 路径 `override`，今天已有且注册期已 eager 物化）。
   覆盖路径天然指得到子树深处（`children/coder/nodes/plan/budget/tokens`）。
   **归约读数：嵌套走内联，复用走继承 —— 而今天是一件事（Ref）同时兼着这两个用途。**
4. **代价要认**：子树里改一个字，整份父模板出新版本。这与「改模板 → 新版本 → 新实例」
   同一条哲学，但会让根模板这类大对象的版本增长变快。真实成本取决于模板体量，需要实测。

**"容器也是一条命令"因此有了形状上的答案**：容器模板做的事是
**把资产（卡片、文件）提取成变量 → 把变量配置进它所描述的各个位点的命令行**。
命令属于位点，容器负责"提取与配置"这一层。所以容器不需要 `exec` 段 ——
§1.2 的建议按本条定案，不再是待拍板项。

### 1.5 消息 / 执行记录 / 义务各是什么

| | 分类 | 落点 |
|---|---|---|
| 消息 | **运行记录**，不是图实体 | 在途住队列；终态落 `<instance>/$msg` |
| 执行记录 | **运行记录** | 在途住头；了结落 `<instance>/$exec` |
| 义务 | **纯派生视图**，本体里不存在 | `outstanding(facts)` 纯函数 |

「在途 vs 了结」**由它住在哪表达**，不由 `status` 字段表达 —— 这是 §6 能把两个状态机整个删掉的根据。

---

## 2. 执行统一：一条路径，判别式从声明侧搬到返回类型

### 2.1 形状

```
#pickWork    → 候选：QUEUED 消息，目标实例 OPEN，且该位点名下无在途执行（N2）
#prepare     → 入站契约 → 变量提取 → 编上下文        （纯函数，今天两条路径已共用）
runner       → 由 exec.use 在宿主注册表里挑；内核不解析 argv 内容
out = runner.run(request)
  ├ 非 Promise → claim 与 apply 合成一次 transact，不写在途记录
  └ Promise    → transact#1 落 claim → await → transact#2 apply
#apply       → 不可信边界三查 → 出站契约 → 路由排期 → puts/spawns → 一次提交
```

`#commitSync` 整个消失；`claimAgent` / `applyAgentResult` 作为跨进程接管的公开 API 保留，
内部走同一个 `#apply`。`#pickWork` 不再吃 accept 谓词。

### 2.2 为什么返回类型是对的判别式（这是本提案最关键的一条）

沿用规则 23ee8dc 说「同步事务与外部三段式有实际差异，不强行合并」—— **这条是对的**。
错的是它被推广成了两条代码路径、两套失败语义、两套工具面。

真正的差异是：**是否存在"记录说在跑、但没人在跑"的窗口**。而在 JS 单线程 + 同步事务下，

> 窗口存在 ⟺ 执行跨越了 `await` ⟺ `run()` 返回 Promise

这是结构性等价，可以由**编译器与运行时**承担，不需要模板作者声明。而今天
`node.handler !== undefined` 这个判别在模板上 —— 一个内置 handler 哪天加了 `await`，
声明侧完全看不出来，耐久性保证会静默失效。

**它还是单向安全的**：谎报异步只是降级走慢路径；谎报同步在 JS 里构造不出来。

**必须配一条 runner 作者契约**（`§17.5` 的确定性契约的延续，我补的）：
同步 runner 必须无外部副作用；一旦有 I/O，就返回 Promise。内核无法强制这条，
但它可以被钉成文档纪律 + 一条测试（同步 runner 里发起真实 I/O 的样例必须在评审中被拒）。

### 2.3 聚合与计数的正确性 —— §7.4 可以整条删掉

MODEL §7.4「聚合判定只能在同步 handler 里做」**没有任何强制点撑着**，
它靠的是"BuiltinHandler 是同步函数 + 调用与提交在同一个 transact 内 + JS 单线程"。
而它的**真实成因不是 agent，是命名空间的根**：`#handlerContext` 把 `put`/`history`
的根取成 `containerOf(input.target)` —— **容器**。所以"同一容器里两个节点写同一个对象"
才成立，才需要新鲜读。

修法是两条**有强制点**的不变量，替换一条口头规矩：

- **N1 写者唯一**：对象命名空间的根从容器下沉到**执行位点**（`namespacedId(record.instance, …)`）。
  `$exec` / `$msg` 今天已经是位点根了，这只是让产物跟上。
- **N2 位点串行**：一个位点同时至多一次在途执行。强制点在 `#pickWork` 的**候选过滤**
  （而不是像今天 `#driving` 那样挑中之后才判忙、不跳到下一个候选 —— 顺手修掉队头阻塞 C-4），
  且判据改成**持久事实**（头里有没有这个位点的在途记录），不再是进程本地 Set。

有了 N1 + N2：自读自写**不可能读到过期值**，无论执行体同步还是跨了 `await`。

**跨子树汇聚（帧 12）另有论证，而且这里有一个四份提案都没完全说透的坑：**

活性成立 —— 对象库 append-only ⇒ `collect` 结果单调；每个子实例终态触发一次通知；
最后一次叫醒必然看得全（产物提交的事务早于通知入队的事务）。

**但幂等不成立**：读到 3 份与读到 4 份都会发 `done`。今天靠"每个子只触发一次 + 自己写一版
再数版本"挡着，而那个写法有一个已实测的塌陷 —— 内容寻址去重会把"重复发生的相同事件"
合成一版，导致循环永不退出、汇聚永远等不齐（所以今天必须把序号写进 body）。

**结论：判定必须写成边沿触发，不是电平触发。** 落点是给每次执行注入一个内核算出的
**到达计数** `arrivals[port]`（该位点该端口已消费的消息数，由 `$msg` 派生，不是新账），
判定写 `arrivals === expect` 而不是 `collected.length >= expect`。
这一条同时删掉 `loop` 那个 epoch 标记对象、以及"计数类写法必须把序号写进 body"这个陷阱。

**净结论：不需要读集复核，不需要乐观并发，不需要 generation 之外的任何冲突检测。**

### 2.3.1 N1 的代价已实测（2026-09-17）

探针：把命名空间的根从容器下沉到执行位点（4 处读点，7 行），跑全量。
完整记录见 [experiments/2026-09-17-n1](./experiments/2026-09-17-n1/README.md)。

> **964 条里 29 条变红：26 条机械更新（断言里写死了对象 id 前缀），3 条真语义断裂。
> scene / contracts / sandbox 三个包一条不红。**

| 包 | 红 / 总 |
|---|---|
| kernel | 13 / 331 |
| cli | 12 / 198 |
| state | 3 / 87 |
| mcp | 1 / 19 |
| scene · contracts · sandbox | **0** / 336 |

**3 条真断裂同一个根因，而且正是帧 12**：`merge` 是容器 `job-1` 的一个**位点**，
`coder-1` 是 `job-1` 的**子容器**。容器根时代两者同在 `job-1` 名下，
merge 读 coder-1 的产物算"读自己的子树"；根下沉之后，从 `job-1/merge` 看
`job-1/coder-1` 是**兄弟**，被 `withinNamespace` 挡住：

```
collect("job-1") 跨命名空间："job-1" 不在 job-1/merge 的子树内
read job-1/coder-1/results@1 跨命名空间：…不在 job-1/collect 的子树内
```

**这条实测把 `gather` 从"我认为更好"升级成"唯一剩下的形状"**：N1 之后合法的跨位点读
只有一种 —— 父容器名下、指名某个子槽的汇聚。它在声明期完全表达得出
（`vars: { results: { gather: { slot: coder, name: result } } }`），
不需要运行期接受任意前缀。**授权来自容器、取数由内核在注入期代做** ——
这正好解释了它为什么该是注入，而不是执行体自己调的工具（§2.5）。

推论：`ctx.collect` 不是被裁剪，是被**替换**。PA1 那次给 `read`/`collect` 加的命名空间裁剪，
在 N1 之后会把帧 12 一并挡住；与其放宽裁剪，不如让跨位点汇聚根本不走运行期读。

另一条实测收获：`ref-and-frame14` 那条红的**只是断言**，运行期自洽 ——
`ctx.put` 返回的 ref 经 emit 传给下游、下游 servo `type:"ref"` 解引用成功。
**引用跨位点传递这条链路没有断**，所以 N1 不影响"消息只带地址、内容走对象库"。

### 2.4 失败语义统一成一套

今天：同步路径越界端口抛 `InvariantError` 整步回滚、`INVALID_OUTPUT` 不重试；
agent 路径两者都判 `INVALID_OUTPUT` 按 `maxAttempts` 重试。

统一取**不可信那一套**：所有 runner 都是不可信边界，输出一律过三查，越界端口判为输出违规。
理由：受信与否是 runner 实现的性质，而结果检查的成本是常数；保留"受信可以省检查"
等于把二分从声明侧搬到检查侧。

重试与否**不由错误种类决定，由"重试会不会改变结果"决定**：同步 runner 是确定性的，
`retries` 恒为 1（重试无意义）；这条同样由返回类型派生，不需要字段。

### 2.5 `ctx` 五个能力的去向

| 今天 | 新 | 理由 |
|---|---|---|
| `read(ref)` | **删** | 已被 `bind.card` 与端口变量的 `ref` 解引用覆盖；src 里零使用 |
| `history(name)` | **注入期预绑**（端口变量的一种来源） | 自己的命名空间，注入期就能算全（N1 + N2） |
| `collect(prefix,name)` | **注入期预绑**（`gather` 来源） | 同上；顺带把跨子树读的裁剪从运行期判定变成声明期判定 |
| `put` | **输出清单 `puts`** | 结构性堵掉今天同步路径的"零部分提交漏口"（ctx.put 已写、出站校验再失败、随 FAILED 一起提交） |
| `spawn` | **输出清单 `spawns`** | 帧 8 的扇出从此对两类 runner 对称（今天 agent 根本做不到） |
| `port` / `requestId` | **进 `ExecutionRequest.inbound`** | 今天 `ExecutionRequest` 没有入站端口，而 `HandlerContext.port` 的注释写明：缺了它多入端口节点会自造无限循环 |

`emits` 与 `spawns` 都必须是**列表**（今天 `emissions` 是 map，一个端口只能一份载荷）——
帧 8 要给三个 coder 派三份**不同**的活，这是硬需求。字段数不变，map → list。

---

## 3. 容器模板 = 一份配置文件

### 3.1 四段

```yaml
# 段 1：容器变量 —— 实例化时钉死（C4 同一条论证），整份模板可 ${} 引用
vars:
  model: { literal: "claude-opus-5" }
  brief: { card: "brief@3", max_tokens: 2000 }    # 有上界 ⇒ 计入 B1
  repo:  { resource: "main-repo" }                # 宿主句柄，不占上下文 ⇒ 不计入

# 段 2：程序（位点）
nodes:
  plan:
    ports:
      in:     { direction: receive, vars: { task: { from: "$.task", max_tokens: 1500 } } }
      assign: { direction: emit }
    bind:   { rules: { card: "coding-rules@2", max_tokens: 3000 } }   # 稳定前缀（不变量 X）
    budget: { tokens: 8000 }
    exec:
      use: container          # ← 内核解释：挑哪个 runner
      with:                   # ← 内核只做两件事：插值、扫凭据。不理解任何键名
        image: "node:20-alpine"
        argv:  ["claude", "-p", "--model", "${vars.model}", "--file", "task.md"]
        env:   { ANTHROPIC_API_KEY: "$ANTHROPIC_API_KEY" }
        files: { "task.md": "${rules}\n\n# 任务\n${task}" }
        mounts: { workspace: { resource: "${vars.repo}", base: "main" } }
        limits: { wallClockSeconds: 900, network: internal, retain: on-failure }
  merge:
    ports:
      wake: { direction: receive, vars: { results: { gather: { slot: coder, name: result } } } }
      out:  { direction: emit }
    exec: { use: collect, with: { expect: 3 } }    # 进程内 runner，零沙箱开销

# 段 3：子容器槽
slots:
  coder: { def: "coder@3", max: 8, wires: [ ... ] }

# 段 4：连接（内网边与别名是同一张表）
wires:
  - { name: "plan.assign", slot: coder, port: in, fresh: true }
  - { name: "plan.done",   node: merge, port: wake }
  - { name: "metrics",     node: metrics, port: sink }
  - { name: "plan.audit",  node: audit, port: in, inherit: false }   # 原 selfBindings
```

### 3.2 四块能力各自的落点

**变量**：`vars` 段 + 节点 `bind` + 端口 `vars`，五种来源共用一个 `Var` 形状
（`literal` / `card` / `resource` / `from` / `gather`）。「位置即绑定时机」（不变量 X）保持不变。
`ITERATION.md` Q1（执行体字段上移到容器）**不采纳**：执行体属于位点，不属于容器。

**命令合成**：一个纯函数 `interpolate(json, bag)` —— 递归扫 `exec.with` 的每个字符串，
把 `${名字}` 换成变量袋里的值。这是今天完全不存在的能力，但它替换掉的更多：
今天 vars 有**四条渲染路径**（`writeContext` 写 vars.json、`profile.render` 再写一遍同一路径
（漂移 A-12）、两个 profile 渲染进 `CLAUDE.md`/`AGENTS.md`、自家 agent 读 vars.json 拼提示词），
插值之后只剩一条，整个 `profile` 层（3 个硬编码 TS 对象、模板只能写名字且注册期不校验）随之退场。

**替换语义必须是 execve 数组语义，不是 shell 展开**：`${}` 只在单个字符串内部替换，
替换结果不再分词、不 glob、不做命令替换 —— 注入安全是结构性的，不靠转义。

**注入**：三个落点全在 `with` 里（`files` / `mounts` / `env`），由 runner 解释，内核零知识。

**工具调用：这一格应当为空。** 你要的「执行体能调哪些容器工具」我的判断是**全部可派生**：
能发哪些端口 = `allowedEmitPorts(ports)`（今天已是第一不变量的落点）；能建哪些子容器 =
本容器 `slots` 声明过的；能写哪些产物 = 只能写自己位点的命名空间（N1）。
判据：剧本帧 0–16 里没有一帧需要"禁止某个节点建实例"。agent 自己的 MCP 工具
（今天写死在 `backend.ts` 的 `request.json.tools`）搬进 `with.tools`，由 runner 解释。
**这是对你描述的一处偏离：机制数从 +1 字段 +1 校验分支降到 0。**

### 3.3 与 §4.5「内核零知识」怎么共存

判据不变且更严：kernel 非注释代码里 `argv|workspace|capabilities|profile|image|isolates` 零命中。
插值函数不提这些名字 —— 它只做"JSON 字符串按变量袋替换"，一个通用操作。

| 段 | 谁解释 |
|---|---|
| `vars` / `ports` / `bind` / `budget` / `slots` / `wires` / `exec.use` | 内核 |
| `exec.with` | 内核只插值 + 扫凭据，不理解键名 |
| runner 注册表 / 资源注册表 | 宿主，不进对象库 |

**校验落在今天已有的那条缝**：`registerContainerTemplate(..., validateExecutionSpec)`。
宿主把每个已注册 runner 的 `with` schema 接进去（今天接的是 `checkAgentSpec`，同一个位置）。
于是今天落在可重试档 A-6 的**五类确定性错误**（workspace.from 的节点名、资源名、profile 名、
env 的 `$NAME` 缺失、handler 名）全部变成注册期拒绝。

**凭据扫描扩到整份模板正文**（`vars` 现在也能装字面量），并加一条新纪律：
**插值结果不再扫**（值来自宿主环境或对象库，可能合法地含凭据）；配一条测试钉住
`$exec` 正文里不得出现 `with` 的任何字段。

### 3.4 基底与声明面的分界（作者定，2026-09-17）

> **容器不需要声明的，只有"容器基底"：默认注入、环境相关配置与对应的安全策略，
> 以及命令行工具自己实现的那部分。其余基本都要声明，且要对 agent 开放。**

这条把 §3.2 那个"工具调用格为空"的结论讲清楚了 —— 它为空不是因为工具不重要，
而是因为**工具属于基底**：每个容器一落地就带着那套调用方式（今天是 `toolkit.ts` 每次执行
无条件装进沙箱的 `emit` / `progress`），不需要逐模板声明；而 `claude` / `codex` 自己的
Read/Write/Bash 属于"命令行工具自己实现的部分"，本来就不该进我们的模板。

于是分界线是两条，而不是一条：

| | 声明 | 注入给 agent |
|---|---|---|
| **基底**：容器工具的调用方式、沙箱与隔离强度、出网策略、脱敏、git 观察 | ❌ 不逐模板声明（只能选强度，见下） | ✅ **如实告知**（"你跑在什么策略下、有哪些工具"） |
| **命令行工具自己实现的部分**（各家 CLI 的内建工具） | ❌ | ❌ 不是我们的事 |
| **其余一切**：变量、命令、注入的文件、资源、端口、连接、子槽、预算 | ✅ 必须声明 | ✅ 必须开放 |

由此得到一条可执行的设计原则，我把它写进基线：

> **声明即注入（基底除外）。** 模板里声明过的东西，注入期必须让执行体看得见；
> 反过来，执行体看得见的东西，除基底外都必须在模板里找得到出处。

两条推论：

1. **"开放"是可读可用，不是可改。** 能力强度（隔离、出网、墙钟、retain）声明在模板里，
   agent 读得到（它才好决定花多少力气），但碰不到 —— 放进 agent 自己能调的工具面，
   就等于让它松开自己的笼子。这条今天已经是对的（`AgentSpec.capabilities` 的注释），
   本提案只是把它升格为原则的一部分。
2. **今天有两处违反"声明即注入"，都要修**：`AgentSpec.context` 写进
   `.hertaloy/context/` 却**没有任何 profile 的说明文字提到这些文件**，自家 agent 也只读
   `vars.json` —— 声明了但没告知；`capabilities.wallClockSeconds` 会生效却**从不告知 agent**
   （内核那个旋钮在生产路径上是死的，C-8）—— 强制了但没告知。

---

## 4. 端口与路由：五个原语

| # | 原语 | 形状 |
|---|---|---|
| 1 | **地址** | `Endpoint {instance, port}` —— 今天的形状一字不改 |
| 2 | **发出** | 输出清单里的 `{port, payload}`，**地址不在输出里**（M1 结构性） |
| 3 | **连接** | `wires`：一个名字 → 0..N 个端点 |
| 4 | **要回执** | emit 端口的 `await`（有它 = 记一份 request 义务，回程落本节点声明的端口） |
| 5 | **系统事件** | 内核代投的 `$sig`，一种原语吃掉三件事 |

### 4.1 `wires`：边即绑定（合并 `edges` 与 `bindings`）

今天 `stageOutputs` 里内网边走 `edges.filter(...)`、别名走 `resolveAlias(...)`，
**两套代码回答同一个问题**："这条 emit 去哪"。合并成一张表、一套解析：

```ts
Wire = { name, inherit?: false, 目标二选一：{node, port} | {slot, port, fresh?, max?} }
```

- `selfBindings` **删**（= `inherit: false`，而 `MaterializedBinding.inherit` 今天就存在；
  真实用例只有 `aliases.test.ts` S5 两条）
- `slots.<s>.wires` **留**（可见性靠放置是承重设计，而它就是同一张表放在另一处，零新增机制）
- `external` **删**（实测：指向真实外部地址会让此后每一步 `step` 抛「未知实例」；
  唯一用例的地址是同一棵树里的本地实例。剧本 0–16 无跨租户帧）
- `MessageEnvelope` **删**（含死契约 `in_reply_to`，全仓 src 零构造）

**解析改最近优先**（今天是"收集所有祖先同名绑定的并集"，导致两层绑同名注册能过、
到运行期才炸"要求恰好 1 个目标、实际 2 个"）。扇出不受影响 —— 扇出来自**单条 wire 的
slot 展开**，不来自多层并集。配一条注册期告警：同名 wire 在同一继承链上出现两次。

### 4.2 终止与失败通知：基线走普通消息（作者定，2026-09-17）

> **基线按今天的做法：内核代投的通知与普通消息同形，形状由声明方给。
> "一种系统事件原语吃掉三件事"是沟通协议的优化措施，归 §8，不进基线。**

基线只保留一条规则，它替掉今天分在两处的校验：

> **凡内核代投的载荷，注册期必须能被目标端口的 `contract` 与变量提取吃下 —— 一处校验，一个函数。**

今天这是两处：`validateSignalPayloads`（M4，管子实例终止通知那个内核硬造的
`{slot, traceid, status}`）与 `checkGatewayMode`（管 `unavailable` 必填）。合并成一处。

同时补两个今天的真漏洞（与基线形状无关，属于纯修复）：

- **截断也要发终止通知**。今天 `#notifyParent` 只有 `settle()` 一个调用点，
  被截断的子实例不通知父 —— 这条语义不在任何文档里，帧 15 的反向清账因此是半截的。
- **外部投递要能销账**。今天人从外部往 callback 端口投消息销不掉 request 义务
  （`#commitPlan` 只按 `plan.resolved` 删 pending），而 MCP `send_message` 的说明写着
  "人的放行也走这条"。修法：外部注入携带 `requestId` 时走同一条销账路径。

**为什么"最简单的是靠差事和错误响应"是对的**：一次请求得到一个回复，
回复可以是正常结果也可以是错误 —— 这不需要第二套机制。`unavailable` 的存在不是因为
需要"系统事件"，而是因为**内核要替一个已经死掉的服务方回一条**，而它不知道该回什么形状。
让声明方给形状（今天 `unavailable` 的做法）就够了，代价是每个 REQUEST 端口多一个必填字段。

### 4.3 REQUEST / REPLY：保留，收成一个字段

**保留。** 删掉它，帧 6/7/9/10 没有落点 —— 服务方必须能回到**发起的那一个实例**，
而执行体输出里没有地址（M1），所以回程地址只能由内核从"触发这次执行的那条消息"里取。
而且 `request` 义务是"请求方等回复期间不能终止"（L5）与死锁等待边的两个来源之一。

归约：`checkGatewayMode` 的 4 条互斥 refine → 1–2 条；`await` 的有无决定记不记义务、
`reply` 的有无决定回程从哪来，两者与"目标是本地还是别名"**正交**
（副作用：本地目标也能 REQUEST，今天必须走别名）。

**还要修一个实测断点**：人从外部往 callback 端口投消息**销不掉 request 义务**
（`#commitPlan` 只按 `plan.resolved` 删 pending）。修法：外部注入携带 `requestId` 时走同一条销账路径。

### 4.4 子实例入口：容器端口取代 `entry`

`ChildSlot.entry` **删**。改成子模板自己声明容器端口（`ports: { in: { to: {node, port} } }`）——
"可执行文件的 stdin 由调用者的 unit 文件决定"是反 OS 的，而且导致同一个子模板在不同父那里
入口不同、注册期要跨模板校验。

这条正好兑现你说的"容器有很多可配置的入口"，并且**零跳**：容器端口是一层转发，
在入队前把 `<child>/in` 重写成 `<child>/plan/task`（创建时就能物化），不产生额外消息与执行记录。
`ITERATION.md` Q3（spawn 载荷投哪）随之消失。

---

## 5. 别名系统：一个概念，两个命名空间，不合并

今天有三套名字：路由别名（进模板）、资源别名（宿主注册表 `resources.json`，不进对象库）、
`workspace.source`。`resources.ts` 的头注释已有定案："同一条纪律，两个命名空间"，
理由是**跨信任边界**。

**我建议维持这条定案，但把它写进基线**（今天它只活在一处代码注释里）：

| | 展开成 | 何时展开 | 展开到哪 |
|---|---|---|---|
| `wires` 的名字 | 端点 0..N | **晚**（发送时解析活实例，M2/A1 靠它） | —— |
| `vars.*.card` | 对象正文 | **早**（pin 到精确版本，C4 靠它） | **上下文**（占 B1 预算） |
| `vars.*.resource` | 宿主目录 / 仓库 | 晚（执行时物化） | **环境**（不占预算） |

后两行合流到同一个 `vars` 段，是本提案对"别名系统作为容器入口"的落地：
**模板里只写名字，正文在别处** —— 而"进上下文还是进环境"由来源类型决定，不需要额外字段。
`workspace.source` 因此并入 `resource`，不再是第三套。

---

## 6. 状态机预算

### 6.1 最终清单

| 保留 | 值 | 为什么不能派生 |
|---|---|---|
| `InstanceStatus` | `OPEN` / `TERMINAL` | 终止是一个需要被记住的**决定**（L4：终态 ≠ 回收，僵尸进程） |
| `Termination` | **收成 3**：`DONE` / `RETRYABLE` / `FINAL` | 内核只需要回答"应用输出吗""重试吗"。人读的原因（取消 / 预算 / 配置错 / 格式错）降为 `$exec` 里的字符串 |

其余全部删或降级：

| 删 | 依据 |
|---|---|
| `MessageState`（5 值） | **位置即状态**：在队列里 / 在某条在途记录的 claimed 里 / 在 `$msg` 里。`CONSUMED` 与 `CLAIMED` 今天就没有分支读或可派生 |
| `ExecutionStatus`（3 值） | `RUNNING` = 在头里，了结 = 在 `$exec` 里；`SETTLED` 只写不读；`VOIDED` 经公开 API 走不到 |
| `NodeDefinition.kind` / `ClaimOutcome.kind` / `CommitEvent.kind` | 单值死判别 / 内部判别式 / 5 值里只有 `claim` 被消费 |
| `MessageEnvelope` | 全仓 src 零构造 |

漂移表 A-6 提议给 `Termination` 加 `CONFIG` —— **不采纳**：确定性配置错应当在**注册期**
就被拒（§3.3），而不是在终止原因枚举里再加一个值。

### 6.2 一条可执行的纪律

> **新增枚举值之前，必须答出它被哪一行分支读。** 答不出就是分类，分类归 `$exec` 的
> 诊断字符串或渲染层，不进内核。

配一条机械检查（可写成脚本）：内核 `as const` / `z.enum` 里的每个值，
在非测试代码中至少有一处**比较或分支**读它。今天这条脚本会当场抓到 `SETTLED`、`CONSUMED`、`VOIDED`。

### 6.3 `generation`：不是取舍问题，是顺序问题

三份提案都建议删 `generation`（实测只取 0 或 1，且被 `status !== "OPEN"` 蕴含）。
作者的判断是"按理来说应该没有冲突"—— **在单进程里这是对的**，这也正是它今天走不到的原因。

它防的是只在"外部执行"下才存在的一个时序：

```
t0  人下令截断 job-1     → 推进 generation 0→1，写 TERMINAL
t1  但 coder 的进程还在跑（cancel 只是 best effort，可能已经在返回路上）
t2  进程返回结果，宿主调 applyAgentResult
t3  内核必须拒绝：这个结果属于"上一代"
```

**为什么今天走不到**：`#truncate` 在推进 generation 之前，已经先把该容器的在途记录收口成
SETTLED。于是 t3 撞在更早的一道检查 `#closedResult`（记录不是 RUNNING 就拒）上，
根本到不了 `#apply` 里那两条 generation 复核分支。所以 `VOIDED` 这个值的用例
**只能靠直接往对象库塞一版构造出来**。

真实局面是：**两道锁锁同一扇门，外面那道先生效，里面那道从没被叩响过。**

所以这不是"删不删字段"的取舍，是顺序：

1. **先补反例用例**：截断之后再调 `applyAgentResult`，必须被拒。
   今天这条用例构造不出来（要先放开一个测试钩子），这本身就是问题 ——
   一条承重不变量没有可运行的断言。
2. **用例绿了再删字段。** 删的时候若用例变红，说明外层那道锁有漏洞，当场看得见。

跨进程执行租约（欠账 D-6）上马之后，迟到 apply 的窗口会变大不会变小，
这条用例正好是那时候的回归网。

## 7. 不变量与剧本帧

**失去对象（不是被删）**：无 —— 本提案不取消任何承重不变量。

**需要重述的四条**：

| | 今天 | 新 |
|---|---|---|
| §7.4 聚合判定 | 「只能在同步 handler 里做」（无强制点） | **删除**，由 N1 写者唯一 + N2 位点串行 + 边沿触发的到达计数替代（都有强制点） |
| M1 编排权威 | 「属于路由表」 | 不变，但结构保证升级：输出清单里没有地址位置 |
| M4 信号吃得下 | 计划"整个删掉" | **改为合并成一处**（§4.2）——「删掉且用例仍绿」这条验收今天不成立 |
| A2 名字必须绑上 | 文档说"含内网边"，代码不覆盖 | `wires` 合表后真正覆盖，一行判定 |

**新增两条（都有强制点）**：N1 写者唯一（`namespacedId(record.instance, …)`）、
N2 位点串行（`#pickWork` 候选过滤 + 在途表按位点唯一索引）。

**剧本帧**：0–16 全部可表达。三处形状变化 ——
帧 8 的扇出从 `ctx.spawn` 循环改为 `spawns` 列表（两类 runner 对称，今天 agent 做不到）；
帧 12 的汇聚从 `ctx.collect` 改为端口变量的 `gather` 来源 + 到达计数判定；
帧 13 的 epoch 从"自己写标记再数版本"改为注入的到达计数（顺带消掉"序号必须写进 body"的陷阱）。
**帧 14（命题核心）不受影响**，而且 `bind` / `vars` 与端口变量的分离让"稳定前缀"
第一次成为结构事实而不是口头约定。

---

## 8. 优化机制

| # | 机制 | 预期效果 |
|---|---|---|
| 1 | 队列按 `(instance, port)` 索引，调度变成"哪些端口有待收且位点空闲" | 消掉 `#pickWork` 的 O(M) 全表扫（实测 M=200→1600 时每条 0.108 → 0.472 ms）。**这个二次项是"没把连接当成有身份的东西"的直接后果** |
| 2 | 同步 runner 合并 claim/apply 为一次提交 | 每条消息的事务与落盘次数 2 → 1；内置执行不再经沙箱（今天 `noop` 在测试模板里被引用 64 次，每次走沙箱是目录分配 + toolkit 安装 + 14 次同步 git + 一个一次性容器） |
| 3 | `#pickWork` 挑中即跳过忙位点（候选过滤而非事后判忙） | 修掉队头阻塞 C-4；并发度由位点数决定，与端口数无关 |
| 4 | 路由在实例创建时物化成 `Endpoint` | 解析从 O(E) 扫边表降到 O(1) 读；路由零跳继续成立 |
| 5 | 到达计数取代"写一版再数版本" | 汇聚判定从 O(版本数) 降到 O(1)，且消掉内容寻址去重那个塌陷 |
| 6 | 头只装活的、终态归 append-only 对象库 | 已完成（V6 阶段 5）。全量重写的规模与在途工作量成正比，与历史无关 |
| 7 | 模板解析按版本缓存 | 已完成（drain 快 36–42 倍） |
| 8 | 注册期拒绝取代运行期救火 | 9 类确定性错误从"跑三次 agent 才失败"变成注册期拒 —— 这是**省钱**，不只是省时间 |
| 9 | `execFileSync` → 异步；git 观察按声明开关 | 今天每次执行 14 次同步 git 调用会阻塞所有在途 agent 的超时定时器（A-9） |
| 10 | **沟通协议优化：一种系统事件原语吃掉三件事**（子实例终止 / 请求对端消失 / 被截断），载荷形状由内核定，对位 `siginfo_t` | 基线走普通消息即可（§4.2）。这条是优化：它让 `unavailable` 必填的理由消失 —— 内核自造载荷过不了目标端口提取、消息进 FAILED、请求方根本不被叫醒（实测过的静默失败）在结构上不可能发生；同时子实例身份不会因为声明方写了常量载荷而丢失 |
| 11 | 汇聚的无效叫醒：N 个子实例终态触发 N 次，前 N−1 次白跑 | 用到达计数做边沿判定之后（第 5 条），白跑的成本降到一次注入 + 一次比较；要进一步消掉，可让内核只在"计数达标"时才投递 —— 但那是把判定挪进内核，与「逻辑在执行体里」冲突，**不建议** |

---

## 9. 文档结构与防漂移

**三份文件，一套编号：**

- `MODEL.md`（基线，约 600 行）：终态形状与规则。剧本帧表**迁入**（判据第一句依赖它，
  而它今天锚在设计史 FOUNDATION §2 上，且帧 0/帧 9 状态已过期）。删掉设计史与路线（约 18%）。
- `ITERATION.md`（台账）：路线、欠账、漂移、批次记录。今天路线在**三处**各登记一份
  （MODEL §12 / ITERATION 第十三轮 / V6 §13），至少三四套编号并存。
- `archive/`：V6_MODEL、FOUNDATION_V5、V5_WORKPLAN、MODEL 的附录 A/B/C。

**ONESHOT 的处置**：不能简单删 —— 它有一条 MODEL 没有的判据
「**画布必须画得出还没发生的连接**」（这是端口模型胜过 actor/ocap 的那条判据）。
先把这句迁进 MODEL §1.2 成为第五句判据，然后 ONESHOT 要么删、要么降为**生成物**
（从 MODEL 抽取，不手工维护）。

**删掉四类无代码对应的叙述分类**：位点/底座/环境（源码"底座"零命中）、五类工具
（代码授权只有 DDL/DML/DQL 三类，且"truncate 是 fiat 先例"与代码不符 —— 它的操作类是 DML，
与 send/spawn 同类，**fiat 这一类今天零个合格成员**）、网关两档、七相位里的四个
（七个相位只有三个有真实注入点：`validateExecutionSpec` / `scheduler` / `onCommit`）。

**机械防漂移（这是关键 —— 纪律靠人守就会漂）**：

| 脚本 | 抓什么 |
|---|---|
| 字段表 ↔ zod schema 机械核对 | MODEL §3 的每一行必须对得上一个导出 schema 的字段集。今天这条会当场抓到 3 行不存在的形状 |
| 枚举纪律 | 每个枚举值至少一处分支读。今天会抓到 `SETTLED` / `CONSUMED` / `VOIDED` |
| §4.5 零知识 grep | 已有判据，写成脚本接进 CI（PC4） |
| 不变量表符号名存在性 | 表里每个符号名必须在 src 里存在。今天会抓到 M3 的位置写错（实现在 contracts，表里写 routing.ts） |
| 文档计数 | "六步"这类数字与代码步数对不上就报（今天是 0–6 共 7 步） |

---

## 10. 落地路线

**破坏性集中在一批，其余批次纯增或双接受。**

| 批 | 内容 | 验收（可证伪） | 破坏性 |
|---|---|---|---|
| **0** | **最小第一步**：把 `collect` / `loop` 两个内置改成"注入到达计数 + 边沿判定"，不动任何契约 | 两个内置不再调 `ctx.put`/`history`；`fanout-merge` 与 `patterns` 用例不改仍绿 | 无 |
| 1 | 命名空间根下沉到位点（N1）+ `#pickWork` 候选过滤（N2）+ 跨位点汇聚改 `gather` | **已实测**：29/964 变红（26 机械 + 3 真断裂，全部是帧 12 的跨位点汇聚）。验收：3 条改写成 `gather` 之后不改语义仍绿；新增反例：位点 A 读不到位点 B 的同名对象 | 行为破坏（已落库对象的 id 前缀，不迁移） |
| 2 | 返回类型判别 + 一条执行路径；`HandlerContext` 退场，`puts`/`spawns`/`inbound` 进契约 | `step`/`drain` 与 `claimAgent` 系列合并；越界端口在两类 runner 上语义一致；零部分提交漏口有反例 | 内部 API |
| 3 | `wires` 合表 + `$sig` + 容器端口；删 `edges`/`selfBindings`/`external`/`entry`/`exit`/`MessageEnvelope` | 别名与内网边用例不改语义仍绿；截断也发 `child_exit` 有用例；外部投 callback 能销账有用例 | 模板语法 |
| 4 | 模板四段 + `${}` 插值 + `profile` 层退场 + 校验器加厚 | A-6 档里的确定性错误清零（9 条）；`$exec` 正文不含 `with` | **磁盘与语法破坏集中在这里** |
| 5 | 状态机收敛（删 `MessageState` / `ExecutionStatus` / `seq`）+ 队列按端口索引 | 枚举纪律脚本零告警；drain 每条成本不随 M 增长 | head 格式 |
| 6 | 文档重排 + 五条防漂移脚本接进 CI | 三条命令退出 0；字段表核对脚本零告警 | 无 |

**批 0 是刻意选的**：一两天做完，直接证伪或证实 §2.3 那条论证（本提案最关键、也最弱的一环），
且不动磁盘格式、不动模板语法、不挡后续任何一条路。

---

## 11. 拍板状态（2026-09-17）

| # | 事项 | 状态 |
|---|---|---|
| 1 | 容器要不要 `exec` 段 | **已定：不要。** 容器负责"把资产提取成变量、把变量配置进位点的命令行"；命令属于位点（§1.4） |
| 2 | 子容器描述内联还是引用 | **已定：内联。** `ChildSlot.template: Ref` 退场，复用改走 `extends` 覆盖层（§1.4） |
| 3 | 基底与声明面的分界 | **已定：** 只有容器基底（默认注入、环境与安全策略）和命令行工具自己实现的部分不声明；其余全声明且对 agent 开放（§3.4） |
| 4 | 终止 / 失败通知的形状 | **已定：** 基线走普通消息（差事 + 错误响应），形状由声明方给；"一种系统事件原语"归优化章节（§4.2、§8） |
| 5 | N1 命名空间根下沉 | **已量，建议一次切**：29/964（26 机械 + 3 真断裂，全部是帧 12 的跨位点汇聚），scene/contracts/sandbox 零红。跨位点汇聚改声明式 `gather`（§2.3.1） |
| 6 | `generation` 删不删 | **待定 → 建议改成顺序问题**：先补"截断后迟到的 apply 必须被拒"这条今天构造不出来的反例用例，用例绿了再删字段。不需要预先判断有没有冲突（§6.3） |
| 7 | 可见性放置留几档 | **待定**（五件事里最轻）。`selfBindings` 明确删（= `inherit:false`）；`slots.<s>.wires` 留或删都行 —— 它今天只有 3 条用例、剧本无帧，而且那条声称"另一支看不见"的用例**没有针对性断言** |

### 端口数字化的附注（作者 2026-09-17 提出）

作者提出"把端口设成数字形式，类似 TCP，别名只是 DNS 那一层"。结论分两半：

- **表示形式随便选**：端口名换成数字只是字符集变化，不影响任何不变量。
- **动态分配不能要**：端口在本系统里不只是地址，它还是**变量的出入口** ——
  `servo` 挂在端口上，`long`/`ref` 变量的上界也挂在端口上，B1 的注册期预算求和
  就是把一个位点所有 receive 端口的声明加起来。一个动态分配出来的端口**没有声明处**，
  B1 的注册期求和当场失去意义 —— 而那是整个系统的存在理由。
  MODEL §5.2 早就把这条显式写反过：「"网络协议"这个比喻自带一个默认：进程可以 bind
  任意空闲端口。照搬就破第一不变量。」
- **但作者要的能力已经有了，只是没被当成端口看**：TCP 用临时源端口把回复对回那一次调用，
  这里对应的是 `requestId`（内核分配、进 `#pending` 表、对端消失时代投 `unavailable`）。

| TCP | 这里 |
|---|---|
| 四元组 (src ip, src port, dst ip, dst port) | (请求方实例, `requestId`, 服务方实例, 端口) |
| 临时源端口，内核分配 | `requestId`，内核分配（`#requestSeq`） |
| 连接表 | `#pending` 待回复请求表 |
| 对端消失 → RST / EPIPE | 服务方终态 → 代投 `unavailable` |

**所以"动态端口"在这里的正确形状是一次调用的相关 id，不是一个新的可 bind 地址。**
而"别名只是 DNS"完全成立，那正是第九次归约的原话（一个名字解析出 N 个地址 = 多条 A 记录），
§4.1 的 `wires` 合表就是把今天绕过别名的内网边也收进这张表。

---

## 附：本提案推翻的既有结论

| 出处 | 原结论 | 本提案 |
|---|---|---|
| MODEL §7.4 / ONESHOT §7.4 | 聚合判定只能在同步 handler 里做 | **删除**。成因是命名空间根取成了容器，不是 agent 的异步性 |
| MODEL §8.4 / ONESHOT §8.4 | "内核在 apply 期读 journal" | **事实错误**。内核 src 里 journal 零命中，读它的是 sandbox |
| ITERATION Q1 | `handler?`/`agent?` 二选一上移到容器 | **不采纳**。执行体属于位点；二分由返回类型承担 |
| ITERATION Q3 | spawn 载荷投给子模板已声明的 receive 端口 | **消失**。子容器自己声明容器端口（socket activation） |
| ITERATION Q4 | 子实例保留位点 `$exit` 的一条路由 | **不进基线**。通知走普通消息；三合一归优化章节（作者定） |
| ITERATION Q2 | 默认起的子实例：内联模板还是引用，两者都允许 | **改为只内联**（作者定，§1.4） |
| MODEL §12.3 | `validateSignalPayloads`（M4）整个删掉、用例不改仍绿 | **不成立**。`gateway.test.ts` 有两条用例靠它抛错；M4 应合并不应删除 |
| 沿用规则 23ee8dc | 同步事务与外部三段式有实际差异，不强行合并 | **部分保留**。差异真实，但它属于 runner 性质、由返回类型表达，不该长成两条代码路径 |
