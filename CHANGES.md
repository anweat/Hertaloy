# 变更记录

> **只记较大范围的改动** —— 一次归约、一次破坏性迁移、一次边界移动。
> 每批提交、每条漂移、每个待办**不进这里**：提交进 git，漂移进代码注释（`TODO(drift):`），
> 待办进你自己的脑子或 issue。
>
> 判据：**一条记录如果不改变"这个系统是什么"或"怎么跟它相处"，就不该在这里。**

---

## 2026-09-17 · 文档规制切换

三条线（代码 / 基线文档 / 台账）并行维护改成一条主线。规则见 [README](./README.md#文档规制)。

- 新增 [REDUCTIONS.md](./REDUCTIONS.md)：归约史、被排除的路线、判据、OS 对照 —— 所有"为什么"集中到一处。
- 新增本文件：只记大改动。
- [ITERATION.md](./ITERATION.md) 进入退场流程：批次记录交给 git；漂移登记表逐条变成代码里的
  `TODO(drift):` 标记（**可 grep，不会与代码漂移**）；路线交给本文件。
- `MODEL.md` 将去掉全部状态标记（✅/🚧/📋）、路线与漂移账，只留"是什么"。
  实现与否由机械检查回答，不由标记回答。
- 新增 `scripts/docs-check.mjs`（`pnpm docs-check`）：符号名存在性 · 枚举值是否被分支读 ·
  `TODO(drift):` 清单 · 内核零知识边界。**首跑就独立复现了提案里靠人工核对得出的结论**
  —— `SETTLED` 与 `CONSUMED` 只写不读。

## 2026-09-17 · V6 重审提案（尚未落地）

[V6_REDESIGN.md](./V6_REDESIGN.md) —— 第十二次归约「执行即纯函数」的完整提案。
已定四条：容器不带 `exec` 段；子容器描述内联在父模板里；基底与声明面的分界；
终止/失败通知走普通消息。已实测一条：命名空间根下沉到位点，964 条里 29 条变红
（26 机械 + 3 真断裂），见 [experiments/2026-09-17-n1](./experiments/2026-09-17-n1/README.md)。

**落地之后**：形状并入 `MODEL.md`，论证并入 `REDUCTIONS.md`，提案文件进 `archive/`。

## 2026-09-12 · 第十一次归约：节点即容器

删掉节点、边、`entry`/`exit`、两个判别式、不变量 C3 与 M4。
`MODEL.md` 成为单一概念基线，`ONESHOT.md` 降为它的规格化摘录。

## 2026-09-08 · V6 阶段 1a/1b/5：地址收成一段，头只装活的

`{traceid, node, port}` → `{instance, port}`；终态执行与消息归对象库，
`keepConsumedMessages` 退场。同步节点也留执行记录。

## 2026-09-07 · V5 基线落地

内核编排、沙箱执行面、持久化与恢复、权限层、资源别名、MCP 工具层、只读观测服务
全部接通并有测试。详见 git 历史与 `archive/`。
