# 归约与判据

> **这里是"为什么"，[MODEL.md](./MODEL.md) 是"是什么"。**
>
> 一条纪律：**MODEL 里不写论证，这里不写形状。** 形状变了改 MODEL，
> 论证只在**归约发生时**追加一条，平时不动。这是文档量能降下来的前提 ——
> 今天的冗余不是写多了，是同一批事实在多处各维护一份。

---

## 1. 判据

每一次演进都是同一件事：**把 N 套并存机制证明成一套**。任何提案先过这几句：

1. **删掉它，主线剧本的哪一帧无法正确表达？**
2. **它是在减少还是在增加并存的机制数量？**
3. **它声称的不变量，强制方式在哪个符号、哪条测试？** —— 答不出的是愿望。
4. **它要落在 MODEL 对象表的哪一行？** —— 落不下就是过度设计。
5. **画布画得出还没发生的连接吗？** —— 运行时才涌现的拓扑不能作为拓扑的唯一表示。

如果它是扩展，再过三句：挂哪个相位（一个扩展只能挂一个）· 失败语义是哪一档 · 留什么记录。

判断该不该开放替换时只问一句：**换掉它，哪条不变量会破？** 一条都不破才是缝，否则是组件。

**新增枚举值之前，必须答出它被哪一行分支读。** 答不出就是分类；分类归诊断字符串或渲染层，不进内核。

---

## 2. 归约史

| # | 归约 | 消掉了什么 |
|---|---|---|
| 1 | 容器 is-a 实例 | slot / subscription / children / pool_cursor / overflow → 一棵实例树 |
| 2 | 资产即变量 | 卡片系统 + 四段上下文编译器 + 运行时预算裁剪 → 一套变量 |
| 3 | 等待即锁 | 五种等待 → 一张锁表（后被 #8 再归约） |
| 4 | 版本历史即状态 | 策略节点 / 表达式语言 / 节点内 persistent / 内核时钟 |
| 5 | agent 就是一条命令行 | 内核工具桥 / 线协议 / backend 矩阵 → 端口 + 变量 + 沙箱外 git |
| 8 | 锁账本即派生 | `LockLedger` 四处记账 → 一条「未了结的义务」枚举 |
| 9 | 隧道即别名 | 隧道标签 / 全树扫描 / 订阅 scope → 绑定表 + 向上解析 |
| 10 | Lock 即 Obligation 的第二套词汇 | `Lock` 类型与 `LockView` 整个删除 |
| 11 | 节点即容器 | 节点 / 边 / `entry` / `exit` / `selfBindings` / 两个判别式 / C3 / M4 / 递归 L5 |
| 12 | **执行即纯函数**（提案中） | handler/agent 二分 / `HandlerContext` 五条侧信道 / 两套驱动入口 / 两套失败语义 —— 见 [V6_REDESIGN.md](./V6_REDESIGN.md) |

---

## 3. 被排除的路线

**排除本身是信息。** 不留档的话，会有人从同一个起点再推导一遍同一条死路。

### 3.1 节点成为实例，但不许自行判定终止

它逼出三样东西：「线程 vs 子进程」的实体区分、递归三合取的 L5、`child` 义务的双职责。
这三样复杂度全部来自"实例常驻却不许自己判定终止"这一个假设；
一旦承认**实例是一条命令的一次执行**，三样一起消失。

朴素的"节点就是常驻子实例"是**可证伪地错的**：

```
节点成实例 ⇒ 每个节点给容器记一条 child 义务
C3（内网实例不自行判定终止）⇒ 节点永远 OPEN
⇒ 容器名下永远有 child 义务 ⇒ 任何容器都永远 settle 不了
```

不是不优雅，是死锁。

### 3.2 端口自由 bind（照搬 TCP 的动态端口）

"网络协议"这个比喻自带一个默认：进程可以 bind 任意空闲端口。照搬就破第一不变量
（只能选不能构造）。更硬的理由是：**端口不只是地址，还是变量的出入口** ——
servo 与变量上界挂在端口上，注册期的预算求和就是把一个位点所有 receive 端口的声明加起来。
动态分配出来的端口没有声明处，预算求和当场失去意义。

而"把回复对回那一次调用"这个真实需求已经有落点：**一次调用的相关 id**
（对位 TCP 的临时源端口），不是一个新的可 bind 地址。

### 3.3 用端口数表达并行度

并发的挡板按**实例路径**挡，不按端口挡。在一个位点上多开端口换不来任何并行，
还把一个运行期的量硬编进声明 —— 而并行度恰恰是最该能调的那种东西。

### 3.4 给 agent 发令牌回调内核

令牌是「路径 + 凭据」形状：从不可信边界内部递出一个字符串，服务端验票 ——
撞上"主体必须由可信边界注入，不能自称 uid"。正确形状是**预绑句柄**（fd 表模型）：
内核在建环境时把能力装进去，被执行的东西只能用、造不出新的。

### 3.5 注入 ref 的白名单

设想过"允许执行体读注入进来的 ref"。核实之后不做：`type: "ref"` 的变量在 prepare 时
已被解引用成正文，执行体从头到尾拿不到需要自己去读的外部引用。**白名单没有消费方。**

---

## 4. OS 对照

不是类比修辞 —— 每一行都对应一个已经做出的决定。**对不上的地方明说不是 OS，不要硬套。**

| 这里的东西 | OS 里的名字 |
|---|---|
| 容器 / 实例 | 虚拟机 / 进程 |
| 模板 → 实例 | 磁盘上的可执行文件 → 内存里的进程 |
| 位点持久 / 执行瞬时 | inetd / systemd socket activation |
| 终态 ≠ 回收 | 僵尸进程 |
| 孤儿 = 记录在跑而本进程没在驱动 | 孤儿进程 + init 收养 |
| 截断靠 generation fence，不靠 cancel | PID 重用防护 |
| claim 当场落盘 | 让出 CPU 前先存好上下文 |
| 写不出自己的命名空间 | chroot |
| 主体由可信边界注入 | 不能自称 uid |
| 预绑句柄 | fd 表 |
| 能力单调不增 | `no_new_privs` / capability bounding set |
| 资源别名只给 note 不给 path | fstab / 挂载表 |
| 造实例而不开权限 | 进程隔离 + IPC，而非共享内存加权限位 |
| 版本历史即状态 | 进程无状态、状态在文件系统 |
| 排期类的按引用复核 | eBPF verifier |
| 队列不开放替换 | eBPF 换不掉调度器本体 |
| fiat 工具 | 特权系统调用（kill / mount / setuid 各有各的 capability） |
| 子实例终止 / 对端消失 / 被杀 | SIGCHLD + siginfo_t / EPIPE / SIGKILL |

**对不上的两处**：「请求必得一个了结」在 OS 里没有对应物（`connect()` 之后对端不回就永远阻塞，
OS 靠超时，而内核无时钟）—— 所以它的触发点必须是内核能**本地判定**的事实；
消息去重靠内容寻址也没有 OS 对应物，它来自对象库的性质，别硬套传输层序号。

---

## 5. 这个模型解释了什么

一个模型是不是抓对了东西，一个检验是：**它能不能解释那些在没有它的时候就已经做对的决定。**
上表里从「终态 ≠ 回收」到「版本历史即状态」的十几条，全部是在各自的场景里独立做出、
各自有过论证的 —— 模型解释得通的地方，代码本来就是对的；
**模型解释不通的地方，代码正在反复打补丁**（例如 handler 与 agent 的两套失败语义）。
