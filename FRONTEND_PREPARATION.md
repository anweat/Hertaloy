# 前端准备：模板工作区与运行观察区

2026-09-07，基于 [第十一轮可见性复审](./experiments/2026-09-07-visibility-review/README.md)。
状态：可以开始信息结构和交互探索，尚未冻结视觉方案、技术选型或网页写入契约。

用户已经明确：主要操作模板，模板与运行分离；之前的设计稿和页面可以抛弃重做。
因此以当前内核事实和实测接口为基础，不继承旧页面长侧栏或旧设计稿中的布局约束。

## 两个工作区，各自回答一组问题

| 工作区 | 用户要完成的事 | 首屏应优先看见 | 与另一侧的连接 |
|---|---|---|---|
| 模板 | 找到定义、理解节点/端口/子槽、修改草稿、定位校验错误、注册新版本 | 当前版本或草稿身份、结构、选中项配置、校验反馈 | 查看选定运行实际使用的版本；由明确的版本发起新运行 |
| 运行 | 确认当前状态、定位阻塞/失败、看过程输出、读产物、判断可执行操作 | 运行身份与生命周期、实例层级、当前节点结果、待处理事项 | 跳到运行固定的模板版本；编辑从它派生草稿 |

注册成功产生实际 ref，既有实例仍固定在创建时的版本。模板保存和运行推进分别表达，
不能让用户以为修改草稿或注册新版就会改变正在运行的实例。

这两个工作区可以互相跳转、保留选择上下文，不要求把所有内容挤进一个画布。
画布用于理解结构与路径；日志、消息、结果正文需要独立且足够宽的阅读区域。

## 当前可以接什么

| 信息/动作 | 已有入口 | 前端消费要求 |
|---|---|---|
| 当前 run 的概况 | `/status` | 展示 lifecycle、在途执行/消息、结构化 blockers；不是全局运行列表 |
| 实例/节点与连接 | `/scene`、`/scene/stream` | 流是 NDJSON 差量，每次新连接先建空基线；不是旧稿的只推游标 SSE |
| 运行固定的模板及声明依赖 | `/templates`、`/definitions` | 精确 ref、usedBy、dependencies；未实例化子槽也可查看 |
| agent 的当前执行 | `Cell.execution` → `/execution?id=` | 状态、进度与证据来自同一次执行；旧失败进入历史区域 |
| 同步节点的当前结果 | `Cell.result` → `/message?id=` 或 `/object?ref=` | 没有 execution ID 也能看失败原因或成功提交；显式处理消息回收 |
| 产物/观测正文 | `/object?ref=` | 用精确引用读取；独立显示拒绝/缺席；原始 JSON 作为可展开详情 |
| 草稿校验 | `POST /validate-definition` | 展示 where/code/message；校验不落盘，不预留版本；需全库 DQL |
| 操作能否执行 | `/operations?scope=` | 权限、当前条件、通道支持与待填参数分开；条件预览不代表执行承诺 |
| 正式注册、发送、推进、截断 | CLI/MCP 现有命令 | HTTP 尚无这些写入口；设计网页操作时同时补写契约与认证 |

服务当前绑定一个 run 目录。traceid、executionId、messageId 与模板 ref 的选择和缓存，
都必须带上所属 run/连接上下文；它们不能直接当成跨运行的全局唯一标识。

## 首个可验证的交互切片

1. 打开已有运行，立即看到连接状态、运行生命周期与实例层级。
2. 选择节点，在“当前结果”阅读状态和原因；旁边可跳到对应的固定模板定义。
3. agent 正在跑时显示最新 journal 与 note；结束后显示结果、独立的观测与产物。
4. 同步 handler 首次失败能读消息原因；成功消息已回收时保留提交正文入口。
5. 从固定模板进入草稿，修改并纯校验；错误点击后定位到节点/端口/字段。

这条切片足以验证“模板操作与运行观察分离”是否好用。全局模板库、运行创建向导和正式
网页写操作可以接续实现，但需要先明确下一节的产品和通道选择。

## 开始详细设计前需要确定的三点

- **模板存在哪里**：独立于 run 的项目模板库，还是先导入/导出模板文件。当前 definitions 只给运行关联的闭包，不能冒充全局资产管理。
- **如何打开和创建运行**：由本地应用管理 run 目录与驱动进程，还是用户启动服务后连接。当前 HTTP 服务没有全局 run 列表或创建入口。
- **网页如何获得操作权**：采用明确的本机认证与写接口。现有首页内嵌 token 只是本机可信的读取边界，不是可直接扩成写权限的认证方案。

这些选择影响数据保存和操作流程；可先比较交互草图，再冻结字段、路由与页面布局。
尚不指定框架、节点渲染库或视觉风格。

## 界面必须说清的状态

| 容易混淆的情况 | 表达要求 |
|---|---|
| OPEN / TERMINAL 与运行成功 | 生命周期单列，TERMINAL 不等于成功 |
| 覆盖率与语义进度 | 实例 coverage 是近期窗口内触达的节点比例；子槽是已收口子实例比例；分别命名。agent progress/note 独立展示 |
| 没报进度与坏采集 | 未上报不画 0%；progressUnavailable 展示原因 |
| 当前成功与历史失败 | 当前结论优先，lastFailure 留在对应消息的历史信息中 |
| 没有结果、已回收、无权、断连 | 分别显示；不统一变成空面板或“尚未运行” |
| 同步结果的最近顺序 | 当前按消息投递序号，不能包装成任意调度器下的完成时间线 |
| 暂停与截断 | 当前没有暂停；截断说明逻辑栅栏及作用范围，不承诺外部进程一定立即停止 |

浏览器实测中，原始定义 argv、全部操作原因、窗口日志依次堆在侧栏，用户需要长距离滚动
才能从状态找到正文。新界面应优先给简明结论与定位入口，完整协议数据作为次级详情。

## 下一轮验收场景

- 模板：固定旧版、从旧版派生草稿、缺依赖、非法端口/agent 规格、无校验权限。
- 运行：首次失败、失败后成功、同步成功消息回收、agent 正在输出、坏进度、无可读现场。
- 选择：切换实例/节点/产物时旧请求迟到；同节点从失败切到成功；断流重连清除旧基线。
- 操作：有读取权但无写入权、HTTP 通道未开放、实例终态、驱动已被占用。
- 阅读：一份长模板、一条长失败原因、一个多级路径产物、多个实例同时活跃；关键信息不依赖原始 JSON 扫读。

旧 `FRONTEND_V5.md`、`RENDERING.md` 与 `OBSERVATION_V5.md` 保留为历史推演材料。
其中 `/api/snapshot`、游标 SSE、按快照过滤即可完整回放、全局锁地址等内容不作为当前实现承诺。
当前接口以 [DEVELOPING.md 第 8 节](./DEVELOPING.md) 与复验结果为准。
