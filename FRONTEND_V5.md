# 前端设计 —— 控制台（`apps/console`）

> 状态：**设计稿（2026-08-22）**，未实现。
>
> 从内核结构重新推导，不承接 `RENDERING.md` / `apps/canvas` 的结论。
> 结论若与它们重合，是被同一份内核逼出来的，不是抄的；分歧处在 §11 单列。
>
> 目标一句话：**让人看见进程、看懂因果、并且知道自己动一下的后果落在哪一层。**

---

## 0. 先认三件事，它们决定了前端不能长成通用节点编辑器

| 内核事实 | 前端被迫接受的后果 |
|---|---|
| **L0：内核没有时钟**，唯一单调量是消息序号 `msg-N` | 横轴只能是序号。**不做「预计剩余时间」、不做墙钟进度条** —— 系统里没有那个量 |
| **C4：实例终身 pin 创建时的定义版本** | 「改一下让它立刻生效」在跑着的实例上**不存在**。编辑器必须把这条说出来，而不是给个会骗人的保存按钮 |
| **§9.1：强制截断是第一位保证；没有暂停** | 界面上**永远有截断，永远没有暂停**。假装有暂停等于伪造一个内核状态 |

第三条是前端最容易犯的错：通用编辑器都有 ▶/⏸，照抄就等于在 UI 上凭空造出一个内核里不存在的状态机。

---

## 1. 第一性：一切可见之物都是一个**地址**

内核已经有三套地址空间，而且都可前缀匹配。前端不发明第四套。

```
job-1/coder-2      实例（含容器）—— traceid
job-1#plan         节点          —— traceid # nodeId
job-1~coder        子槽（声明）  —— traceid ~ slot
plan@3             对象版本      —— object_id@N
msg-17             消息          —— 序号
lock-4             锁            —— 账本一行
```

**选中 = 一个地址**。于是下面五件事全部免费、且必然一致：

| 用途 | 怎么来的 |
|---|---|
| URL / 深链 / 分享 | 地址就是路由 `#/job-1/coder-2` |
| 视口裁剪 | 视口 = traceid 前缀，裁剪 = 前缀查询 |
| 权限判定 | `decide(actor, opclass, 地址)` 直接吃同一个串 |
| 命令面板 | 输入地址即跳转 —— CLI 与 GUI 同一个入口 |
| 服务端取数 | `?scope=<地址>` 一个参数通吃 |

> **判据（可证伪）**：接上一个新后端或新视图时若需要**第七种可选中的东西**，
> 说明地址空间设计错了。不要靠「再加一个 id 字段」绕过去。

**对象化**这个要求的落点就在这里：不是「把界面做成卡片流」，而是
**屏幕上任何一个可点的东西，都能被写成一个内核认得的地址**。做不到的东西不该出现在屏幕上。

---

## 2. 归约表 —— 前端最容易多长出来的七样东西，各自压回哪里

和内核那四次归约同一种做法：证明 N 套并存的机制其实是一套。

| 想要的功能 | 天真做法（**不做**） | 压回哪里 |
|---|---|---|
| 实时进度 | 前端攒事件流、维护图状态 | `scene = f(snapshot)`，每次事件重算 |
| 回放 / 时间旅行 | 录制机制 + 回放缓冲 | **按序号过滤同一份快照**，纯函数，零新机制 |
| 审批中心 | 新的「审批单」实体 | `request` 锁的一次投影 + 一条 `send` 放行 |
| 结构编辑 | 前端图状态 + 保存时 diff | **覆盖层**：`extends` + `override` 路径，内核本来就是路径寻址的 |
| 布局保存 | 节点里存 `position` | `layout/<template_id>` 对象（那个 kind 已占位、**至今无生产者**，前端就是它的生产者） |
| 按钮能不能点 | 前端复刻一份权限规则 | 服务端把 `decide()` 的结果**连同 reason 原样下发** |
| 增量更新 | 增量事件协议（add/remove/patch） | 事件**只带游标**，前端重取快照 |

最后一条值得展开：**事件不携带数据**，所以事件通道与快照**在原理上不可能漂移**。
代价是每次重取一份快照 —— 而快照被刻意设计成「每样东西一行」（§4），重取很便宜。

---

## 3. 分层：前端一行后端代码都不许 import

```
kernel · state · sandbox            (node 专属：fs / 子进程 / docker)
        │
        ▼
packages/server                     ★ 新增，唯一的 IO 与信任边界
  · 注入 principal（不从请求体取）
  · 只调 ControlPlane，永不碰 Runtime
  · HTTP + SSE
        │  纯 JSON
        ▼
contracts · scene                   (浏览器安全：零 node 内置)
        │
        ▼
apps/console                        ★ 新增前端
```

### 3.1 服务端的路由表 = 内核的操作表

`ControlPlane` 已经有一张 `OPERATION_CLASS`：`define / send / run / spawn / truncate / settle / query`。
**HTTP 路由一一对应，不多一条。**

```
GET  /api/snapshot?scope=job-1        → DQL
GET  /api/events                      → SSE，只推游标
GET  /api/object/:ref                 → DQL（正文按需，权限单独判）
GET  /api/history/:objectId           → DQL
GET  /api/why/:messageId              → DQL（causesOf）
POST /api/send | spawn | advance | truncate | settle   → DML
POST /api/define | validate           → DDL（validate 干跑，不落库）
```

> **判据**：新功能若需要一条内核里**没有对应操作**的路由，说明这个功能在内核里没有落点。
> 先回内核补操作，别在服务端造第二套语义。

### 3.2 三条硬规矩

1. **principal 由会话注入**。README 已列明「`--as` 是自报身份不是认证」——
   服务端是这条边界的收口处。v1 只绑 `127.0.0.1`、单主体，
   并且**界面右上角常驻显示当前主体与「未认证模式」字样**。不显示就等于骗自己。
2. **服务端不缓存图**。它只做：取 head → `exportSnapshot` → 脱存储编码 → 发。
3. **载荷不进快照**（现有 `exportSnapshot` 的选择，保留）。要看内容是一次独立 DQL，
   可能被权限拒 —— 界面要能表达**「这块你看不到」**，而不是显示成空。

---

## 4. 快照要补的字段（**一份格式，不是第二种格式**）

现在 `exportSnapshot` 只送 instances / templates / messages / records / objects。
下面这些是界面的主要信息源，全都已在内核里存在，只是没出口：

| 补什么 | 现在在哪 | 界面上是什么 |
|---|---|---|
| `locks[]`（kind/holder/waitingOn/originNode/since） | `LockLedger` | **阻塞的全部依据**：谁在等谁 |
| `blockers[]`（按实例） | `terminationBlockers` | 实例为什么收不了口 |
| `deadlocks[][]` | `locks.deadlocks()` | 环，红色，最高优先级提示 |
| `generation`（按实例） | `ContainerInstance` | 截断后的第二代，必须与第一代区分 |
| `records[].executionId` / `termination` | `ExecutionRecord` | 五种终止不能混成一种（`CANCELLED`/`BUDGET` 是意图，不是故障） |
| `objects[].provenance`（`derived_from` / `at_seq`） | `ObjectVersion` | 血缘 |
| `can{}`：每个地址上七个操作的 `{allowed, reason}` | `PermissionTable.decide` | **禁用按钮的理由不由前端编** |

### 快照的收口规则

> **快照里每样东西占一行；正文一律按需取。**

所以 `run` 快照的 `consumed/produced` 正文**不进快照**，因果视图是按需 DQL。
这条规则保证「每次事件全量重取」永远便宜，也就保证了 §2 最后那条归约成立。

### 内核的两处缺口（如实列出）

1. **实例没有出生/终止序号**。`scene` 现在拿「最早/最晚碰到这个 traceid 的消息」近似。
   后果：早于窗口（最近 200 条已消费消息）的出生点被截在左沿。
   生命周期是这版设计的主轴之一，**建议内核记两个整数** `bornAtSeq` / `endedAtSeq` ——
   与当初补 `Message.source` 是同一类缺口，同样是「渲染要的那件推不出来的事」。
2. **没有提交事件钩子**。服务端只能：自己驱动完就广播 + 轮询 head 的
   `objectCursor`/mtime 兜住别的进程（CLI / MCP）造成的改动。够用，但要写明它是兜底。

---

## 5. 一块画布，两套坐标 —— 进程可视化

同一批元素、同一份 `Scene`，只换「地址 → 位置」的映射。**切布局是同一批对象移动，不是换页**，
这也是「动态化」最值钱的那一下：人的空间记忆不被打断。

### 布局 A「包含」—— 回答「现在怎么了」

- 嵌套 = **几何包含**（父框套子框）。父必须等所有子终止才能收口，
  所以几何包含不是排版风格，**是那条不变量的直接可视化**。
- 节点是框里的块，**子槽是虚线位** —— 声明本身是信息，
  与「从没命中过的订阅仍然要画」同一条道理。
- 折叠**无损**：`ChildSlot.entry` 本来就定义了子树如何以单个端口示人。
- 阻塞：持锁的实例画一条指向 `waitingOn` 的连线；死锁环因此**长得就像一个环**。

### 布局 B「序号」—— 回答「发生过什么、谁比谁久」

- 横轴 = 消息序号，**等宽离散刻度**，不是墙钟。
- 每个实例一条带，子带在父带内部（同一条包含不变量，换了个方向表达）。
- **agent 执行是有宽度的一段；同步 handler 是一个刻点。**
  这不是缺陷是信息：有宽度 = 真的在沙箱外面跑，刻点 = 内核里一瞬间的事。
- **「现在」单独占最右一格**。不留这格，「在最后一个序号终止」和「还开着」宽度一样，
  生命周期这条轴当场废掉一半。
- 锁 = 从持有者带横着连到被等的带 —— 于是「卡住」在图上就是**一条不再长刻点的带**。

### 拒绝第三套布局

因果 DAG 不做成全局视图：几百次提交的全局 DAG 没人读得懂，而且它表达的信息
序号布局已经承担了。因果只在检视器里画**局部三层**（前因 → 这条 → 后果），按需取。

---

## 6. 检视器 —— 「对象化」的落点

选中任意地址，右侧一栏固定四段，**顺序不变**（位置稳定比信息密度重要）：

| 段 | 内容 | 数据来自 |
|---|---|---|
| **身份** | 地址、kind、模板 ref（精确到版本）、代数 generation | 快照 |
| **通道** | phase / activity / 生存区间 / 阻塞原因逐条 | 快照 |
| **因果** | 消息：前因后果三层；对象：`derived_from` 血缘 + 版本历史 | 按需 DQL |
| **操作** | 见 §7，按后果分层，禁用项直接显示 `decide()` 的 reason | 快照 `can{}` |

三条规矩：

- **色相 = 身份，明度锁死，饱和度随状态**。同一模板的所有实例同色，跨刷新跨进程稳定。
  异常不靠色相（色相被身份占满）：`failed` 用断口，`voided` 用收回。
- **`voided` 要收回，不要走完**。它是「结果作废、什么都没往下传」，
  和 `failed`（失败往下传）在动画上必须不同。它罕见，但它出现时最需要被看懂。
- **`env` 只显示 `$NAME`，永不显示值**。密钥绝不进对象库，界面也不做例外。

---

## 7. 微调 —— 三层后果，必须在按下去之前就分得清

这是整份设计里最实的一段。「微调」在这个内核里**不是一件事，是三件后果完全不同的事**：

| 层 | 写到哪 | 何时生效 | 可逆性 | 界面表达 |
|---|---|---|---|---|
| **视图层** | `layout/<template_id>`、`annotation` | 立刻，只影响看 | 完全可逆（版本回滚） | 直接改，不确认 |
| **定义层（DDL）** | 覆盖层 → 新的 `container_template` 版本 | **只影响下一个实例** | 可逆（切回旧 ref） | 保存前必过 `validate` |
| **运行层（DML）** | 消息 / 实例 / 锁 | 立刻 | **不可逆**（外部副作用只承诺 at-least-once） | 二次确认，写明后果 |

> `layout` 按 **template_id** 键而不是按 ref —— 所以**挪个节点不产生新的语义版本**，
> 也不会因为发版而丢布局。V4 正是按 ref 键 + 只在注册时写 + 不落盘，
> 结果挪一下就要发新版本、重启全丢。

### 7.1 定义层：覆盖路径编辑器，而不是拖拽连边

左栏是**物化定义**（只读，每行标注来自 base 还是 override），右栏是 override 路径列表。

- 保存前必跑 `validate` 干跑校验 —— **不落库**，失败的 issue 直接定位到路径。
  这条内循环 AI 和人共用一个出口。
- 顶部一条**预算求和条**（`declaredBudget`）。超预算时保存按钮直接禁用：
  **「注册期拒绝」在界面上就是一个变灰的保存按钮**，不是运行到一半才报的错。
- 第一版**不做拖拽连边**。边现在是模板里的静态声明，运行时动态边还没有；
  做一个拖得出来却无处可落的交互，等于在界面上凭空造出内核没有的能力。

### 7.2 正在跑的实例：编辑禁用，并且给出两条真出路

按钮变灰的同时写清：

> 该实例已 pin `export-flow@1`（C4：终身 pin，永不迁移）。
> 想让改动生效只有两条路：**① 截断重来 ② 改定义，等下一个实例。**

变灰而不解释，人只会以为界面坏了。

### 7.3 放行 / 审批：不是新机制，是两次已有操作

- 待办列表 = `request` 锁的投影（谁在等、等谁、从哪个节点发起、等了多少序号）。
- 放行 = 往 callback 端口 `send` 一条消息。表单由端口的契约生成。
- **界面上不出现「审批」这个实体**，因为内核里没有它。

### 7.4 危险操作把已知边界搬到操作现场

`truncate` 与任何可能触发外部副作用的 `send`，确认框里原样写：

> 外部副作用只能承诺 **at-least-once**。generation fence 拦得住迟到的内核 apply，
> **撤不回已发的邮件或已推的 commit。**

这句话写在文档里没人看，写在按下去之前的那一刻才有用。

---

## 8. 动态化：三个动的量，一条不许动的轴

| 动的量 | 从哪来 | 画成什么 |
|---|---|---|
| `phase` | 记录 + 实例状态 | 相位：idle / running / done / failed / voided |
| `activity` | **最近 200 条消息**里的命中数 | 亮度 / 线宽 / 流动粒子 |
| `certainty` | 隧道命中累积（边恒为 1） | 虚 ↔ 实、软 ↔ 硬 |

不许动的那条：**横轴永远是序号**。墙钟只允许出现在**执行卡片**上
（`Usage.wallClockSeconds` 是执行面的真实测量值），永不作为图的坐标 —— 编排面没有时钟。

三条动效纪律：

1. **补间是渲染器局部状态，丢了就丢了**。丢掉过渡动画后画面必须仍然正确。
2. **不造假动感**：不给同步节点最短显示时长，不给 agent 编「正在思考」的进度条。
   观测是给人做判断用的，编出来的动感会让人建立错误的时间模型。
3. **染色窗口必须写在界面上**（「最近 200 条消息」）。不写，人会把「窗口外」误读成「没发生过」。
   对象与 `run` 快照仍然全在，历史不丢 —— 只有流量染色是窗口内的。

---

## 9. 技术选型与一条自检

- **框架**：前端总共只有三个状态 —— **选中地址 / 视口前缀 / 当前布局**。
  用轻量响应式（Solid / Svelte / 甚至 signals + 原生）足够，**不要状态管理库**：没有状态可管。
  > **自检（可证伪）**：前端状态若超过这三项，说明有东西没走快照 —— 那就是第二个真相源。
- **渲染**：先 SVG/DOM。几百个 cell 完全够，且命中测试、键盘可达、CSS 变量主题全部免费。
  超过阈值（约 2000 元素）再上 Canvas —— 届时它是同一份 `Scene` 的第三个渲染器，不是重写。
- **颜色走 `style` 不走 SVG 属性**：presentation attribute 里 `var()` 不解析，
  写死具体值会导致主题一换 SVG 留在上个主题。
- **隐形加宽命中路径**（细线外包 ~20px 透明路径）：低 certainty 的隧道线很淡，不加宽根本点不中。
- **命令面板**：一切都是地址，所以「输入地址跳转」几乎免费，顺带给了键盘用户完整通路。

---

## 10. 分阶段与验收

每阶段都带一条**反向判据** —— 满足不了就说明设计错了，回头改设计而不是硬做。

| 阶段 | 做什么 | 验收 | 反向判据 |
|---|---|---|---|
| **0** | `packages/server` 只读 + 快照补字段 + SSE 游标 + 布局 A + 检视器只读 | 画面与 `hertaloy status --json` **逐项对得上** | 若需要前端自己算某个字段 → 快照缺口，回去补 |
| **1** | DML 操作 + `can{}` 权限位 + 待办（request 锁投影） | 剧本 **帧 6 / 7 / 15**（审批记锁 → 销账 → 强制截断）全程在界面上走通 | 若需要一条内核没有的操作 → 回内核补 |
| **2** | 布局 B（序号）+ 回放（序号过滤）+ 锁的等待连线 | 剧本 **帧 12 / 13 / 14**（三路各写一版 → 回边返工 → epoch 2 只含失败切片）在图上看得出来 | 若回放需要新的录制机制 → §2 那条归约不成立，重想 |
| **3** | 定义面：覆盖路径编辑 + validate + 预算条 + `layout` 对象 | 剧本 **帧 2**（改坐标不产生新版本）成立；成为 `layout` kind 的**第一个生产者** | 若挪个节点产生了新模板版本 → 又踩了 V4 的坑 |
| **4** | `proposal` 闭环：AI 提案 → diff → 人批准 → 一次 DDL | 剧本 **帧 1 / 16**（G2 进化闭环）打通；`proposal` 有了生产者与消费者 | 若「批准」需要新机制 → 它本来就该只是一次 `define` |

阶段 0 的验收方式是刻意选的：**同一份数据两个出口（CLI 与画面）能对账**。
这个项目被「两端各自都绿、中间那截没人走」咬过六次，对账是唯一便宜的防线。

---

## 11. 明确不做（以及与仓库现有前端稿的分歧）

**不做**：

- 暂停按钮（内核没有暂停；要停就截断）
- 预计剩余时间 / 墙钟横轴（L0）
- 前端类型注册表、前端图状态、UI/API 双格式（第二个真相源）
- 增量事件协议（事件只带游标）
- 显示 `env` 的值
- 第一版的拖拽连边自由编辑
- 给同步节点最短显示时长

**与现有稿的分歧**（现有稿指 `RENDERING.md` + `apps/canvas`）：

| | 现有稿 | 本稿 | 理由 |
|---|---|---|---|
| 组织中心 | 元素词汇（Cell/Card/Flow/Tether）先行 | **地址空间先行**，元素词汇是它的渲染形态 | 词汇解决「画什么」，地址解决「点了之后能干什么」—— 而本轮要的是微调 |
| 布局 | 层积（序号）为主，包含式被并入 | **两套坐标平权，可互相过渡** | 「现在怎么了」和「发生过什么」是两个问题，一套坐标答不好 |
| 编辑 | 待定 / DDL 按钮占位 | **覆盖路径编辑器**，明确不做拖拽连边 | 覆盖层是内核里真实存在的形状；拖拽连边要等运行时动态边 |
| 权限 | 画布标注 opclass | **`decide()` 的 reason 原样下发**，前端不复刻规则 | 前端复刻权限规则 = 第二个真相源，而且错的时候画面看着完全正常 |
| 传输 | 明确不进 scene 层（对） | 补上：**服务端是唯一 IO 与信任边界**，路由表 = 操作表 | 缺的那截正是「两端各自都绿」的经典发生地 |

一致的部分不重复列 —— 色相=身份、位置不进场景、颜色走 style、隐形加宽、
`scene = f(snapshot)`：这些被同一份内核逼出同样的答案，本稿照单接受。
