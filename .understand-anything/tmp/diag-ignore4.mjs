import { createRequire } from 'node:module';
import { resolve } from 'node:path';
import { pathToFileURL } from 'node:url';
import { mkdtempSync, writeFileSync, mkdirSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
const pluginRoot = 'C:/Users/anwea/.understand-anything/repo/understand-anything-plugin';
const require = createRequire(resolve(pluginRoot, 'package.json'));
const core = await import(pathToFileURL(require.resolve('@understand-anything/core')).href);
const dir = mkdtempSync(join(tmpdir(), 'igtest-'));
mkdirSync(join(dir, '.understand-anything'));
writeFileSync(join(dir, '.understand-anything', '.understandignore'), 'archive/*\n!archive/README.md\n');
const f = core.createIgnoreFilter(dir);
for (const p of ['archive/README.md','archive/foo.md','archive/docs/v4/x.md']) console.log(p, '->', f.isIgnored(p));
